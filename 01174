# 执行上下文

https://github.com/ceilf6/Lab/tree/main/JS/call-stack

栈内存 stack

执行上下文信息、原始数据类型、引用数据类型在堆内存中的地址

堆内存 heap

存放引用数据类型的真实数据

```jsx
function A(){
	const a = 1;
	const b = 2;
	return function B(){
		console.log(a)
	}
}
```

a 被 B 引用，
因此 A 的词法环境不能被销毁，
B 持有对该词法环境的引用，
形成**闭包**。

b 没被引用，
在 A 执行结束后变得不可达，
会被垃圾回收。

**函数**执行上下文：一个函数**运行**之前，创建的一块**内存空间**，空间中包含有**该函数执行所需要的数据**，为该函数执行提供支持。

执行上下文**栈**：**call stack**，**所有执行上下文组成的内存空间**。

栈：一种数据结构，先进后出，后进先出。

**全局**执行上下文：所有JS代码执行之前，都必须有该环境。

**JS引擎始终执行的是栈顶的上下文**。

# 执行上下文中的内容

![image.png](%E6%89%A7%E8%A1%8C%E4%B8%8A%E4%B8%8B%E6%96%87/image.png)

上面是 ES5 及之前的

ES6 后是

```jsx
Execution Context
├─ LexicalEnvironment     ← 词法环境 - 作用域链的核心
├─ VariableEnvironment    ← 变量环境 - var / function（历史原因）
└─ ThisBinding            ← this 指向
```

## **1. this指向**

从 this 指向中拿数据，进而运行函数

new > 显式(call, apply, bind) > 隐式 > 默认

1). 直接调用函数，this指向全局对象（默认）
2). 在函数外，this指向全局对象（默认）

3). 通过对象调用或new一个函数，this指向调用的对象或新对象（隐式规则 和 new规则）

```jsx
        console.log(this.name);
        var a = 2;
        function A() {
            this.abc = 123;

            function B() {
                console.log(this); // window: 直接调用函数，this指向全局对象
            }
            B();
        }
        var a = new A(); // this 跟随 new 绑定到了 A 实例上

        console.log(a.abc); // 123
```

## 2. VO 变量对象

Variable Object：VO 中记录了该环境中所有声明的**参数**、**变量**和**函数**

Global Object: GO，全局执行上下文中的VO

Active Object：AO，当前正在执行的上下文中的VO

创建变量对象步骤如下

1). 确定所有**形参**值以及特殊变量arguments 

全局环境的话就没有形参、这步忽略

2). 确定作用域中通过**var**声明的变量，将它们的值设置为**undefined**，如果VO中已有该名称，则直接忽略。并且 var 声明会**变量提升**

```jsx
console.log("=== d", d) // 不会报错，因为 var 有变量提升: 
// undefined
var d = "d";
console.log("=== d", d) // d
```

![image.png](%E6%89%A7%E8%A1%8C%E4%B8%8A%E4%B8%8B%E6%96%87/image%201.png)

3). 确定函数中通过字面量声明的函数，将它们的值设置为**指向函数对象**，如果VO中已存在该名称，则覆盖。

性质：当找不到一个属性变量时，执行上下文通过函数对象的隐式属性 [[Environment]] （之前是叫 [[scope]] ）指针连接外层词法环境形成作用域链，然后往上寻找

即使 **outer 执行完了，inner 仍然保留**对 outer 变量的**引用、导致GC不回收** ⇒ **闭包 [ ⇒ 内存泄漏风险 ]**

```jsx
function outer() {
    let count = 0;

    return function inner() {
        count++;
        console.log("count =", count);
    };
}

const fn = outer();  // outer 执行完了

fn(); // count = 1
fn(); // count = 2
fn(); // count = 3
```

```jsx
  var foo = 1;

  function bar(a) {
      var a1 = a;
      var a = foo;
      function a() {
          console.log(a); // 1
      }
      a1();
  }

  bar(3);

  /*

  GO:
      foo: undefined 执行=> 1
      bar: fn

  AO:
      a: 3 函数字面量覆盖=> fn-a 执行赋值=> 找到上一层的foo=1
      a1: undefined 执行赋值=> fn-a

  */
```

```jsx
        var g1 = 123;
        var a = 2;
        function A(a, b) {
            console.log(a, b); // 1, fn(被函数字面量声明覆盖了)

            console.log(g1, g2, g3) // 不存在属性，向上一层上下文找到了

            var b = 123; // 赋值覆盖了

            function b() { }

            var a = function () { } // 赋值覆盖了

            console.log(a, b); // fn, 123
        }
        var g2 = 456;

        var g3 = function () { }

        A(1, 2);
```

1. 形参 a, b 分别为 1, 2
2. 函数中有 var 声明，但是都 a, b 已经有了，所以忽略
3. 字面量声明 function b(){ } 
    
    var a = function 不是字面量声明
    

![image.png](%E6%89%A7%E8%A1%8C%E4%B8%8A%E4%B8%8B%E6%96%87/image%202.png)

```jsx
        this.a = 1
        var b;
        this.console.log(this.a, this.b)

        var c = function () {
            console.log(a)
        }
        var c = function () {
            console.log("get from Global Object => a:", a)
        }
        console.log("=== c", c)
        c()

        console.log(d) // 不会报错，因为 var 有变量提升
        var d
```

![image.png](%E6%89%A7%E8%A1%8C%E4%B8%8A%E4%B8%8B%E6%96%87/image%203.png)

## ES6 之后

不再是变量对象，而是被拆分为了

词法环境 + 变量环境

其中

### 词法环境结构

```jsx
LexicalEnvironment = {
  EnvironmentRecord,   // 当前作用域的变量表
  outer                // 指向外层词法环境（形成作用域链）
}
```
# IP地址类别和子网掩码

netmask 子网掩码必须是 连续的 1 后接连续的 0，这是因为需要连续的一段区间用于区分子网

例如 第一段为 201 是 C 类，也就是 /24 的，这时候我需要 20 个子网的话

2**4 = 16 不够，所以得开个连续 5 个的区间

/24即前面 24 位都是 1，然后最后 **11111**000

那么还有 2**3 个主机号，其中 2 个特殊地址不能给主机

```jsx
网络地址：表示这个子网本身
广播地址：发给这个子网所有主机
```

也就是最后剩 2**3-2 也就是 6 个主机号

来源：`SR03_Moez_TD3.readable.txt` Page 1；补充来源：`CM SR03_Partie 3_ME.readable.txt` IP 部分

**1. IPv4 是什么**

`IP` = `Internet Protocol`，互联网协议。

它负责给网络里的设备编号，让数据能找到目标。

`IPv4` 是 IP 地址的一种格式，长这样：

```
201.222.5.0
192.168.1.10
```

它由 4 段数字组成，每段 0 到 255。

本质上每个 IPv4 地址是 32 bits：

```
201.222.5.0 = 4 个 8 bits
```

**2. 主机 host / station 是什么**

`host` 或 `station` 就是网络里的终端设备，比如：

```
电脑、手机、服务器、打印机
```

题目说 `5 stations`，就是每个子网里要放 5 台设备。

**3. 网络地址是什么**

一个网络不是单台机器，而是一组机器的范围。

例如：

```
201.222.5.0/24
```

可以理解为一个大网络，里面有很多可用地址：

```
201.222.5.1
201.222.5.2
...
201.222.5.254
```

其中：

```
201.222.5.0
```

通常表示“这个网络本身”，不能分给主机。

**4. 子网 sous-réseau 是什么**

`子网`就是把一个大网络切成多个小网络。

比如把：

```
201.222.5.0/24
```

切成很多小块：

```
201.222.5.0/29
201.222.5.8/29
201.222.5.16/29
...
```

为什么要切？

因为不同部门、不同区域、不同链路需要分开管理。

**5. netmask 是什么**

`netmask` = 子网掩码。

它用来告诉我们：

```
IP 地址中，哪一部分是网络号，哪一部分是主机号
```

例子：

```
255.255.255.0
```

也写作：

```
/24
```

意思是前 24 bits 是网络部分，后 8 bits 是主机部分。

```
201.222.5.10/24
网络部分：201.222.5
主机部分：10
```

**6. IP 地址分类是什么**

旧式 IPv4 有类别：

| 类别 | 第一段范围 | 默认掩码 | 常见写法 |
| --- | --- | --- | --- |
| Classe A | 1-126 | 255.0.0.0 | /8 |
| Classe B | 128-191 | 255.255.0.0 | /16 |
| Classe C | 192-223 | 255.255.255.0 | /24 |

题目地址：

```
201.222.5.0
```

第一段是 `201`，在 `192-223`，所以是：

```
Classe C
默认 /24
```

**7. 为什么主机数要减 2**

一个子网里有两个特殊地址不能给主机：

```
网络地址：表示这个子网本身
广播地址：发给这个子网所有主机
```

所以如果主机位有 `h` bits：

```
可用主机数 = 2^h - 2
```

**回到 TD3 Ex1 的思路**

题目要：

```
20 个子网
每个子网 5 台主机
```

从 C 类默认 `/24` 开始，主机部分有 8 bits。

要至少 20 个子网：

```
2^5 = 32
```

所以借 5 bits 做子网号。

剩下主机 bits：

```
8 - 5 = 3
```

可用主机数：

```
2^3 - 2 = 6
```

够 5 台主机。

所以：

```
/24 + 5 = /29
netmask = 255.255.255.248
```

**法语答案模板**

`201.222.5.0 est une adresse de classe C, donc le masque par défaut est /24. Pour obtenir au moins 20 sous-réseaux, il faut emprunter 5 bits car 2^5 = 32. Il reste 3 bits pour les hôtes, donc 2^3 - 2 = 6 hôtes utilisables. Le masque est donc /29, soit 255.255.255.248.`

中文：

201.222.5.0 是 C 类地址，默认掩码 /24。为了得到至少 20 个子网，需要借 5 位，因为 2^5=32。剩下 3 位给主机，所以每个子网有 6 个可用主机地址。掩码为 /29，即 255.255.255.248。

**小练习**

`192.168.1.0/24` 中，`/24` 表示前多少 bits 是网络部分？剩下多少 bits 给主机？
# duo-page数据来源

从当前已生成的 `duo-page` 页面来看，`duo-page` 里的数据不是单一来源，而是被 `DuoEngine` 统一编排出来的。主链路可以概括成：

1. 页面启动时，`DuoEngine` 接管整页的数据流
在 [src/index.tsx:64](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/index.tsx#L64) 里，页面把这些东西都交给了 `DuoEngine`：
- `pageQuery`
- `queryConfig`
- `requestInfo`
- `onRequest`
- `commonParamsManagerRef`
1. 真正的请求入口由页面级配置决定
在 [requestInfo.ts:14](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/requestInfo.ts#L14) 里，请求目标是：
- `baseURL = <https://rpc.meituan.com/grouptrade/precreate/`>
- 再带上 `pageId/pageProtocolId/pageProtocolVersion`

也就是说，`duo-page` 不是传统那种每个组件自己写一个 `request.ts` 拉主数据，而是页面协议驱动的统一请求。

1. 请求前会被 `onRequest` 再加工一层
在 [onRequest.ts:16](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/onRequest.ts#L16) 里，`onRequest` 会：
- 给请求补参数
- 处理 `pnMatch`
- 本地调试时把 `draftProtocol` 塞进 `mockData.pageProtocol`
- 最后执行 `defaultRequest()`

当前这个页面没有额外的 JS 预请求，`prefetchRequestMap` 是空的，见 [jsPrefetch/index.ts:2](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/jsPrefetch/index.ts#L2)。

1. 页面通用数据来自“query + 端能力 + 存储”`COMMON_PARAMS` 是通过 [useCommonParams.ts:6](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/hooks/useCommonParams.ts#L6) -> [baseParams.ts:55](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/baseParams.ts#L55) 组出来的，里面主要有：
- `userInfo`
- `location`
- `cityInfo`
- `fingerprint`
- `systemInfo`
- `storage`

这些值的来源分平台：

- MRN 侧主要走 MSI/KNB/原生同步桥，见 [index.mrn.ts:30](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/utils/api/index.mrn.ts#L30)
- Web 侧优先取 URL query，其次走 KNB / Web 容器能力，见 [index.web.ts:60](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/utils/api/index.web.ts#L60)

所以 `duo-page` 的“基础数据”首先来自：

- 跳链参数 `PAGE_QUERY`
- 用户/定位/城市/指纹/系统这些端侧能力
1. 业务请求参数不是手写函数拼的，而是协议里表达式算出来的
这一点是 duo-page 最关键的地方。
在 [onRequest.ts](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/_Home/page/onRequest.ts) 里，`draftProtocol` 是一大段协议字符串，里面有：
- `dynamicDataConfig`
- `dataSourceMap`
- `reqProps`

这些 `reqProps` 会用下面几类输入去算真实请求参数：

- `PAGE_QUERY`：跳链参数
- `COMMON_PARAMS`：用户/定位/城市/指纹/系统
- `PAYLOAD`：组件交互时传上来的 payload
- `NODE.xxx.props`：当前页面上各组件的状态
- `DATA_SOURCE / PREV_DATA / CONST`

比如这个页面里就能看到：

- `mopPreOrderParam`：优先 `PAYLOAD.mopPreOrderParam`，其次 `NODE.MopDataContainer.props.mopTempData`，再其次 `PAGE_QUERY.mopPreOrderParam`
- `cartParam`：会从 `PAGE_QUERY.cartItemList`、`NODE.MeishiGroupBuyTab.props.cartTempProductList`、`NODE.MopContainer.props.mopTempCartTempProductList` 里取
- `productParam`：会从 `PAYLOAD.quantity`、组件 props 里取数量

也就是说，`duo-page` 的业务数据是“协议表达式计算结果”，不是单纯某个 TS 文件里 `fetch('/xxx')` 写死出来的。

1. 组件交互后，会再触发一轮 `update/submit`，重新拿数据
在 [submit-miaoti-container/src/index.tsx:23](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/page/submit-miaoti-container/src/index.tsx#L23) 里，组件会组装 payload，然后调用：
- `__$$projectUtils.update(...)`
- `__$$projectUtils.submit(...)`

而这个 `update/submit` 实际来自 [utils.ts:12](https://www.notion.so/Users/ceilf6/Desktop/duo-page/page-latest/.duo-tmp/preview-12413/src/page/submit-miaoti-container/src/utils/utils.ts#L12) 里的 `@meishi/common-duo-lifecycle`。

所以交互后的新数据来源是：
“组件 payload -> duo lifecycle update -> 协议重新算 reqProps -> 页面请求 -> 返回新 `DATA_SOURCE` -> 组件重新渲染”。

**一句话总结**

`duo-page` 里的数据，本质上是这 4 类输入汇总后，由 `DuoEngine + 页面协议` 统一算出来并请求回来的：

- `PAGE_QUERY`
- `COMMON_PARAMS`
- `PAYLOAD`
- `NODE 当前组件状态`

如果你愿意，我下一步可以继续帮你把“某个具体字段，比如 `mopOperateCartVO` / `mopCartItemInfoVO` 在 duo-page 里是怎么从协议一路传到组件 props 的”这条链再单独追出来。
# 进程模型（事件循环）

[进程和线程](进程和线程%203230f8d8d1fd80b09e1af7f8be055301.md) 

进程：程序运行需要的内存空间

进程互相隔离

**线程：进程中负责运行代码的**

进程开启后会自动创建一个线程运行代码，即**主线程**

当需要执行多块代码时就自动会启动更多的线程执行代码

![截屏2025-12-21 14.01.09.png](截屏2025-12-21_14.01.09.png)

# 浏览器有哪些进程和线程

浏览器是一个多进程多线程的应用程序

浏览器内部工作复杂，为了避免相互影响，为了减少**连环崩溃**的几率

![截屏2025-12-21 14.09.51.png](截屏2025-12-21_14.09.51.png)

> 可以在浏览器的任务管理器中查看当前的所有进程: 更多工具 - 任务管理器
> 

**一个标签页一个渲染进程**

## 1. 浏览器进程

内部有多个线程处理不同的任务

不负责具体页面的展示，而是负责浏览器用户界面 - 上面导航栏例如打开了几个标签页、后退前进按钮，还有用户交互：例如滚动条

子进程管理，创建了网络进程、渲染进程等

## 2. 网络进程

加载网络资源

## 3. Worker Thread 工作线程

[Worker](Worker%2031a0f8d8d1fd8063a9d3dd052fbcf44f.md) 

## 4. 渲染进程

**一个标签页一个渲染进程，确保不相互影响**

渲染进程启动后会启动一个**渲染主线程：负责执行HTML、CSS、JS**

### 渲染主线程是如何工作的

渲染主线程是浏览器中最繁忙的线程，处理任务包括

- 解析 HTML
- 解析 CSS
- 计算样式
- 布局
- 处理图层
- 每秒绘制页面 60 次（FPS=60帧
- 执行全局 JS 代码
- 执行事件处理函数
- 执行计时器的回调函数

> 为什么不开多个线程分担渲染主线程的任务？
共享 、 顺序
> 
> 1. **DOM树是强共享、强顺序的数据结构**，同时被 JS, CSSOM, Layout, Paint 共同依赖，支持同步读写，存在大量**副作用**API。所以简单加锁是无法优雅解决其竞态问题的
> 2. JS语义依赖“立即生效”，所以如果多线程的话会阻碍其他线程JS这是不符合**JS的同步语义**
> 3. 渲染是一条有**严格顺序**的**流水线**，从 JS → CSSOM → Layout → Paint 再最后合成，拆散反而会导致不断的回滚、重排重绘

如何调度？

![截屏2025-12-21 14.29.35.png](截屏2025-12-21_14.29.35.png)

1. 渲染主线程会进入一个无限循环 for( ; ; )
    
    [事件循环一直循环不会导致一直占用资源吗](%E8%BF%9B%E7%A8%8B%E6%A8%A1%E5%9E%8B%EF%BC%88%E4%BA%8B%E4%BB%B6%E5%BE%AA%E7%8E%AF%EF%BC%89%202b50f8d8d1fd80e084e1e05092ea4eb8.md) 
    
2. 每一次循环会检查消息队列中是否有任务存在，如果有就取出第一个任务执行，执行完一个后进入下一个循环；如果没有则进入**休眠**
3. 其他所有线程（包括其他进程的线程）可以随时向消息队列添加任务。新任务会加到消息队列的末尾。添加时如果**渲染主线程休眠就会唤醒其继续执行**（任务管理器

### 何为异步

无法立即执行：

- 计时
- 网络通信
- 事件

不等待，防止阻塞主线程

同步：

![截屏2025-12-21 18.41.05.png](截屏2025-12-21_18.41.05.png)

异步：加入到任务列**末尾**，那么渲染主线程用不阻塞

![截屏2025-12-21 18.45.11.png](截屏2025-12-21_18.45.11.png)

**单线程是异步产生的原因**

**事件循环是异步的实现方式**

> 面试题：如何理解 JS 的异步？
> 
> 
> 参考答案：
> 
> JS是一门单线程的语言，这是因为它运行在浏览器的渲染主线程中，而渲染主线程只有一个。
> 
> 而渲染主线程承担着诸多的工作，渲染页面、执行 JS 都在其中运行。
> 
> **如果使用同步**的方式，就极有可能导致主线程产生阻塞，从而导致消息队列中的很多其他任务无法得到执行。这样一来，一方面会导致繁忙的主线程白白的消耗时间，另一方面导致页面无法及时更新，**给用户造成卡死现象**。
> 
> 所以浏览器采用异步的方式来避免。具体做法是当某些任务发生时，比如计时器、网络、事件监听，主线程将任务交给其他线程去处理，自身立即结束任务的执行，转而执行后续代码。当其他线程完成时，将事先传递的回调函数包装成任务，加入到消息队列的末尾排队，等待主线程调度执行。
> 
> 在这种异步模式下，浏览器永不阻塞，从而最大限度的保证了单线程的流畅运行。
> 

JS单线程、执行在渲染主线程中，如果同步就会发生主线程阻塞问题，明明任务队列有任务但却浪费时间等待。所以采用异步的方式，到时候会把**回调函数包装成任务**（对象）加入到任务队列末尾，等待主线程调度。这种异步模式下浏览器渲染主线程用不阻塞，最大限度保证单线程的流畅运行

### JS 为什么会阻塞

![截屏2025-12-21 19.40.01.png](截屏2025-12-21_19.40.01.png)

h1.textContent 赋值会产生一个**绘制任务**，所以是**先 delay 然后再绘制，所以 JS 会阻塞是因为任务队列的顺序（React 的 Fiber 机制就是为了解决长时间阻塞，在 协调阶段会不断中断询问）**

### 任务没有优先级，都是先进先出

### 任务队列有优先级

宏任务、微任务

现在W3C最新规范中不使用“宏任务”概念，

每个任务都有一个**任务类型**（chrome有一个头文件存放其枚举）同一个类型的任务必须在一个队列1，不同类型的任务可以分属不同的队列，在一次事件循环中，浏览器可以根据实际情况从不同的队列中取出任务执行

浏览器会准备好一个微任务队列 microtask queue ，**微任务中的任务优先所有其他任务**

- 微任务队列 - 最高：需要最快执行的任务
- 交互队列 - 高：用户操作后产生的事件处理任务
- 延时队列 - 中：计时器到达后的回调任务
    
    **用户交互比计时器更重要**
    

### 微任务有

- Promise.then , catch ,finally 这些方法（Promise( )里面的是同步的！）
- fetch.then , await （fetch会启动一个**网络请求，立即返回一个Promise**）
- **queueMicrotask**
- nextTick 优先级最高，在Promise之前

当在执行微任务过程中，如果产生了新的微任务，那么就进入队尾，并在这一轮微任务队列清空，不回等到下一轮微任务

**在执行下一个宏任务之前，微任务队列必清空**

直接将函数放入队列，到时候回调，这样做起来更直观

[实践真题](实践真题%2031a0f8d8d1fd803ab6dae184e7d11c83.md)

**requestAnimation 会在下一次宏任务之前**

帧 同步性

（确保了不会合并DOM操作，且不需要像读属性那样reflow，其本质就是等操作完成后）

```tsx
requestAnimationFrame(() => {
  requestAnimationFrame(() => {
    // 这里设置结束状态
  });
});
// 第一个 rAF：在本帧渲染前执行
// 第二个 rAF：在下一帧渲染前执行
```

![截屏2025-12-21 19.56.06.png](截屏2025-12-21_19.56.06.png)

将任务添加到微任务队列：

1. Promise的方法：引擎创建 **PromiseReactionJob** ，然后把该 Job 放到微任务队列

```tsx
Promise.resolve().then(()=>{})
```

1. **queueMicrotask**(fn)

![截屏2025-12-21 20.04.40.png](截屏2025-12-21_20.04.40.png)

注意：在 a 中**又加了一个微任务**，所以 setTimeout 仍然得后延，因为微任务是最高优先级！所以 2 在 3 前面

> 面试题：阐述一下 JS 的事件循环
> 
> 
> 参考答案：
> 
> 事件循环又叫做消息循环，是浏览器渲染主线程的工作方式。
> 
> 在 Chrome 的源码中，它开启一个不会结束的 for 循环，每次循环从消息队列中取出第一个任务执行，而其他线程只需要在合适的时候将任务加入到队列末尾即可。
> 
> 过去把消息队列简单分为宏队列和微队列，这种说法目前已无法满足复杂的浏览器环境，取而代之的是一种更加灵活多变的处理方式。
> 
> 根据 W3C 官方的解释，每个任务有不同的类型，同类型的任务必须在同一个队列，不同的任务可以属于不同的队列。不同任务队列有不同的优先级，在一次事件循环中，由浏览器自行决定取哪一个队列的任务。但浏览器必须有一个微队列，微队列的任务一定具有最高的优先级，必须优先调度执行。
> 

> 面试题：JS 中的计时器能做到精确计时吗？为什么？
> 
> 
> 参考答案：
> 
> **不行**，因为：
> 
> 1. **计算机硬件没有原子钟，无法做到精确计时**
> 2. 操作系统的计时函数本身就有少量偏差，由于 **JS 的计时器最终调用的是操作系统的函数，也就携带了这些偏差**
> 3. 按照 W3C 的标准，浏览器实现计时器时，如果嵌套层级**超过 5 层，则会带有 4 毫秒的最少时间**，这样在计时时间少于 4 毫秒时又带来了偏差
> 4. 受事件循环的影响，计时器的回调函数只能在主线程空闲时运行，因此又带来了偏差

同理在 NodeJS 中由于生命周期中 poll阶段执行其他回调函数也可能导致计时器回调函数无法及时执行

[https://github.com/ceilf6/Lab/commit/250942bcadd5d955af5961619f329abfc122f7c4](生命周期%20事件循环%203320f8d8d1fd8010937ff1f56cd3acaa.md) 

## 事件循环一直循环不会导致一直占用资源吗

https://github.com/ceilf6/Lab/commit/5987ee361893be3a42db7652a4fd25a9fe1d143c

https://github.com/ceilf6/chromium/commit/00b88a7454585e0e78d9fbcf5a4e17e121ed84e7

在任务队列为空时，**渲染主线程不会空转循环**，而是会进行休眠，有两种休眠方式，

1. 当没有延时任务时是长期休眠
2. 有延时异步任务时是定时休眠、睡到最近一个延迟任务到期

唤醒对应也有两种

1. 外部 PostTask 通过任务唤醒
2. 延时任务到期时自动醒

```jsx
// message_pump_default.cc
if (next_work_info.delayed_run_time.is_max()) {
    event_.Wait();                               // ① 无限期休眠
} else {
    event_.TimedWait(next_work_info.remaining_delay()); // ② 定时休眠
}
```

![image.png](工程师沉淀/浏览器/进程模型（事件循环）/image.png)

浏览器本质是一个 多进程 + 多线程 的架构

- 多进程 Process ：
    - 浏览器有 Browser 进程（只有一个），
    - NetWork 进程，
    - GPU 进程，
    - 多个Render进程（Render 渲染进程内部是多线程模型

JS 运行在渲染进程中的 单线程，主要负责主线程的逻辑

# 浏览器主进程

浏览器最顶层的控制：

- UI绘制（地址栏、前进后退按钮等）
- 管理各个渲染进程
- 资源下载协调
- 文件访问、安全策略

# 渲染进程

**一个页面一般对应一个独立的渲染进程**，内部包含

- **JS引擎线程 V8**
- **GUI渲染**线程
- **定时器**线程
- 网络线程（这只是部分，HTTP在网络进程中）
- 合成线程
- **GC 垃圾回收**线程

渲染进程负责页面的生命周期

```tsx
HTML → DOM → 布局 Layout → 绘制 Paint → 合成 Compositing
```

# 网络进程

专门负责 HTTP 请求和缓存

- 强缓存 / 协商缓存处理
- Service Worker 网络代理
- DNS
- TCP/TLS
- HTTP 请求

# GPU进程负责

- GPU合成
- 2D / 3D绘制 - **Canvas**
- 加速渲染 Pipeline

# 渲染进程中的线程

## JS 引擎线程

- 单线程，只有一个线程能执行 JS 避免同时修改 DOM 造成混乱
- 同一时刻只能执行一个任务
- 消息队列 + 事件循环（宏任务、微任务调度）

**!! JS引擎线程与GUI渲染线程互斥**

**JS修改DOM 与 渲染 两者不能同时发生**

# GUI渲染线程

通过 CSS 属性绘图

- DOM树构建
- CSSOM构建
- 布局
- 绘制
- 合成

```tsx
HTML → DOM
CSS → CSSOM
DOM + CSSOM → Render Tree
Render Tree → Layout
Layout → Paint
Paint → Composite → GPU 显示
```

# 事件触发线程（Event Loop 中宏任务管理者）

负责：

- 将 mousemove、click、scrol 等事件加入任务队列
- 将其他线程的回调加入队列

例如：用户点击 click → 事件线程捕获 → 加入 JS EventLoop

**JS 不监听硬件，是通过事件线程作为中间层**

# 定时器线程

浏览器提供 **setTimeout** / setInterval：单独线程计时，在定时器线程到时间后将任务加入宏任务队列

**如果主线程中同步的代码仍然在执行会有延迟，不一定马上执行**

# 网络线程

- 发起 HTTP 请求
- 响应回调加入 Event Loop 宏任务，比如 fetch() 的 then()

# 合成线程

- 将多个图层合成
- 控制滚动、动画
- GPU加速

**CSS属性的动画**动画不经过主线程：

- transform
- opacity

**不触发重排重绘，不需要JS主线程参与，直接在GPU层合成，非常省性能**

```tsx
CSS transform / opacity → 直接交给合成线程 → 超级流畅
```

[事件循环源码](事件循环源码%203160f8d8d1fd8090b54ee484e98a2ebe.md)
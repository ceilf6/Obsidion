# 第三章 组合逻辑电路设计

一般设计过程：

功能 - 真值表 - 卡诺图化简 - 表达式 - 逻辑图（电路图）

下面给出一种常见且清晰的做法（用 3→8 译码器 + 4 位二进制加法器 构成一位 8421 BCD 加法器）。说明中全部用中文注释并给出关键逻辑表达式与连接方法。

## 思路概述

1. 先用一个 4 位加法器把两个 BCD 数（A3A2A1A0，B3B2B1B0）与进位 Cin 相加，得到初步和 `S3 S2 S1 S0` 和初级进位 `C1`（即第一级加法器的进位输出）。
2. 判定是否需要做 BCD 校正（当和 >9 或者初级进位 C1=1 时需要加 6（0110））。判定式常用形式：
    
    `Corr = C1 OR (S3 AND (S2 OR S1))`
    
    （等价于“和为 1010~1111”时为真）
    
3. 用 3→8 译码器把 `S3 S2 S1`（或 S3S2S1 的某个排列）译出各模式，选择对应 101、110、111 三个输出（译码器第 5、6、7 输出），把它们 OR 后与 C1 做 OR，得到 Corr。
    
    译码器只用来检测 `S3=1 且 (S2|S1)=1` 的三种情况，简化判定电路。
    
4. 若 `Corr=1` 则将初步和 `S3..S0` 再加上二进制 6（0110），得到最终 BCD 码和最终进位 Cout；若 `Corr=0` 则最终和即初步和，Cout 为 0（或者由第二级加法器直接输出）。

> 实际上通常实现方式是把第二步加 6 的操作交给第二个 4 位加法器：第一级计算 A+B+Cin，第二级根据 Corr 把 0000 或 0110 加到第一级的和上（使用 Corr 作第二级加法器的一个输入位的选择——用多路器选择 0110 或 0000 作为第二级的另一路输入）。
> 

---

## 具体电路连接（元件：两个 4-bit 加法器 + 一个 3→8 译码器 + 一个 OR 门组）

（若只有一个 4 位加法器，可用复用或时分两次运行，但一般用两级加法器更简单）

### 第一级（主加法）

- 输入：A3..A0，B3..B0，Cin
- 输出：S3 S2 S1 S0，C1

### 判定电路（用 3→8 译码器）

- 把第一级和的高三位 `S3 S2 S1` 送入 3→8 译码器（输入顺序按译码器引脚），译码器产生 D0..D7（D0 表示 000, D1 表示 001, …, D7 表示111）。
- 需要的三种模式为 `101`、`110`、`111`，即译码器输出 `D5`、`D6`、`D7` 分别为 1。
- 将 `D5, D6, D7` 经一个 3 输入 OR 门合取： `X = D5 OR D6 OR D7`。
- 校正请求： `Corr = C1 OR X`。

（这样利用译码器简洁检测出和在 10~15 的情况）

### 第二级（校正加法）

- 第二级 4 位加法器的被加数：来自第一级的 `S3..S0`。
- 第二级的另一输入：当 `Corr=1` 时接 `0110`（即 +6），否则接 `0000`（即 +0）。可以用 4 个 2:1 选择器（或用门电路由 Corr 驱动）选择位：
    - B'_3 = 0
    - B'_2 = Corr ? 1 : 0
    - B'_1 = Corr ? 1 : 0
    - B'_0 = 0
        
        （即只有第二位和第一位在 Corr=1 时为 1）
        
- 第二级加法器输出即为最终 BCD 和 `T3 T2 T1 T0`，第二级的进位输出为最终 Cout（表示十位进位）。

---

## 逻辑表达式总结

- `判定（不借助译码器的纯逻辑）`：
    
    `Corr = C1 + S3·(S2 + S1)`
    
- 用译码器实现判定：
    
    `X = D5 + D6 + D7`（译码器输入为 S3S2S1）
    
    `Corr = C1 + X`
    
- 最终输出：
    
    `If Corr = 0 => 输出 = S3S2S1S0, Cout = 0`
    
    `If Corr = 1 => 输出 = S + 0110 (通过第二级加法器), Cout = C2（第二级进位）`
    

---

## 举例（检验）

- 例： 7 (0111) + 6 (0110), Cin=0
    
    第一级和： 0111 + 0110 = 1101 (13)，C1=0
    
    S3S2S1S0 = 1101，S3=1 且 (S2|S1)=1 → X=1 → Corr=1
    
    第二级加 6：1101 + 0110 = 1 0011 -> 输出 0011（3），Cout=1（即进位到十位，结果是 13 表示为 十位 1 和个位 3）
    
    结果符合 BCD：1|0011（表示 13）
    

---

## 可选的优化与说明

- 如果器件允许，用一个可并行输出的译码器检测也可把 `D5-D7` 直接 OR；若只允许基本门，也可以用表达式实现 `S3·(S2+S1)`，不必译码器。题目指定使用 3→8 译码器，故用译码器实现检测更贴合题意。
- 部分实现会把 `S3S2S1` 分别接入译码器的三个输入（注意输入位次顺序要对应译码器说明书）。
- 有时书面上把 `Corr` 称为“需要加 6 的判断（Add 6）信号”。

---

如果你需要，我可以：

- 画一张简明的电路图（ASCII 或手绘式块图）把每条连线标清楚；或者
- 给出用 74xx 系列芯片（例如 7483 四位加法器 + 138/148 型 3→8 译码器）实现所需的具体连线表与器件脚位连接。

你想要哪种补充？要图还是具体芯片引脚连线？
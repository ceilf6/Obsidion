# 本质

**神经网络**本质就是 **线性变换套上激活函数**

以前符号主义认为 f[x] = y 

想要找到 f 这个函数

后面转为 **联结**主义

不再关注过程，关注于得到 **条件和结果的关系**

那么在得到 新的输入 之后推测、输出**概率**最高的选项

> 这种 结果导向 也就是导致具体领域的结果能力出色，但是如果遇到泛化问题，AGI还是比较棘手的。就以写代码来说，也许在很多优质代码的训练下模型具体输出能力很不错，但是如果需要结合业务层面、产品层面的理解，效果不一定好
> 

通过 **入**参 和 **出**参 确定函数的参数使得均方误差最小，即线性回归的过程

例如基础的 线性拟合 f = wx + b

如果关系很复杂那可能需要一个激活函数转为非线性关系

f = g(wx + b)

常见的激活函数 g 有

![image.png](%E6%9C%AC%E8%B4%A8/image.png)

![image.png](%E6%9C%AC%E8%B4%A8/image%201.png)
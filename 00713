# SGD Momentum

**Stochastic Gradient Descent，随机梯度下降**

它是一种训练模型的优化算法。核心思想是：不用每次都拿全部数据计算梯度，而是随机抽取一个样本或一小批样本来更新模型参数，从而更快地逼近最优解。

防止过拟合

在 **SGD / 机器学习优化** 里，**momentum（动量）** 是一种让梯度下降“带惯性”的技巧，用来加速收敛、减少震荡。

普通 SGD 每一步只看当前梯度：

[

\theta_{t+1}=\theta_t-\eta \nabla L(\theta_t)

]

加了 momentum 后，会保留过去更新方向的一部分：

[

v_t = \beta v_{t-1} + \nabla L(\theta_t)

]

[

\theta_{t+1} = \theta_t - \eta v_t

]

其中：

- (\theta)：模型参数
- (\eta)：学习率
- (\nabla L(\theta_t))：当前梯度
- (v_t)：累积的“速度”或“动量”
- (\beta)：动量系数，常见是 **0.9**

直观理解：

如果连续几次梯度都指向差不多的方向，momentum 会让参数更新得更快；如果梯度方向来回抖动，momentum 会抵消一部分震荡。

可以把它想成小球下山：普通 SGD 像每一步都重新决定方向；momentum 像小球有惯性，会沿着长期趋势滚得更稳、更快。

它的好处主要是：

1. **加快收敛**
2. **减少左右震荡**
3. **更容易穿过较平坦的区域**
4. **对噪声梯度更稳定**

一句话：**momentum 是给 SGD 加上“惯性记忆”，让优化过程更稳、更快。**
# 2024秋招

还得是大题、编程题分值多

# 状态码

![image.png](2024%E7%A7%8B%E6%8B%9B/image.png)

5开头：服务器错误

**500 Internal Server Error** 表示服务器内部错误

# 二分查找

l = mid + 1

r = mid -1

注意：是跳过 mid 的！

然后取 Math.floor( )

# after

伪元素，作为被选中元素的最后一个**子级**

:link表示尚未被访问的元素，匹配每个具有 href 属性的未访问的

[<a>](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/a)

或

[<area>](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/area)

元素

:root
匹配文档树的根元素

:checked代表选择每个**选中**的输入元素

:enabled 选择每一个已启用的输入元素

能否被操作

# CORS

的单位是 秒

# defer

浏览器对于带有type="module"的<script>，都是异步加载，不会造成堵塞浏览器，即等到整个页面渲染完，再执行模块脚本，等同于打开了<script>标签的defer属性，也就是说模块1、模块2、模块3的加载行为都是等同一致的。

# 浏览器解析

CSS和JS两者更多时候是并行加载的情况，当然也会受资源大小等其它因素的影响，不过并不会有一定的优先级

# 插入排序

从第 2 个元素开始，把当前元素插入到前面“已经有序”的部分
从后往前比较，遇到比它大的就继续往前找
一旦遇到 <= 它的元素，就停止

# weakmap

1. 第一，**WeakSet 里面的成员必须是对象**，不能是基本类型。
2. 第二，`WeakSet()` 构造函数接收的参数必须是**可迭代对象**，比如数组、Set 等。

# 刷新不会清 session

Window.sessionStorage用于临时存储数据，它的生命周期伴随着整个session，当关闭浏览器窗口时，对应存储的数据就被删除，而刷新页面仍然会保持原来的session，故数据不会被删除

# !==

严格不等

比较**值**的前提上还会判断**类型**
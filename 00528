# 剩余元素

# abbr

Abbreviation 缩写词

悬停得久一点才会显示

# time

给浏览器、搜索引擎阅读的时间

# b

bold

以前是一个无语义元素，主要用于加粗字体

# q

标识一小段引用文本

q元素自带引号 - 伪元素

# blockquote

大段引用文本

q和 blockquote都支持 cite 贴上来源网址

# br

无语义，用于在文本中换行

但是没法调样式，不推荐

# hr

无语义，用于分隔

更推荐用 边框实现

# meta

用于给网页添加元数据

SEO

# link

链接外部资源 - CSS, 图标

rel 属性 relation 资源和当前网站的关系

值为 icon 表示设置为该网站的图标

或者放在根目录下 “favicon.ico”

type 链接的MIME类型
# 作用域链

> **VO/AO 是执行上下文里存变量的地方**
> 

> **作用域（Scope）是函数对象创建时就绑定的外部环境引用**
> 

> **函数执行时，执行上下文用 VO + 函数作用域共同组成作用域链**
> 

1. VO中包含一个额外的属性，该属性指向**创建该VO的函数本身**
2. 每个**函数在创建时**，会有一个隐藏属性`[[scope]]`，它**指向创建该函数时的当前执行上下文AO**
    
    ```jsx
    例如
    function A() { }
    
    那么会给 A 赋值->一个对象，这个对象就是函数对象
    函数对象中又可以通过 [[scope]] 属性指回来
    （下图中的黑色箭头）
    ```
    
3. 当访问一个变量时，会先查找自身VO中是否存在，如果不存在，则依次查找`[[scope]]`属性。

某些浏览器会优化作用域链，函数的`[[scope]]`中仅保留需要用到的数据。

现在的 [[scope]] 属性叫 [[Environment]]

```html
        var g = 0;

        function A() {
            var a = 1;

            function B() {
                var b = 2;
                var C = function () {
                    var c = 3;
                    console.log(c, b, a, g);
                }
                C();
            }

            B();
        }

        A();
```

![扫描全能王 2026-2-7 14.40_1.JPG](扫描全能王_2026-2-7_14.40_1.jpg)
# DUO 开发 - For 大客前端

目前大客一共有两个跟 DUO 相关的mrn bundle：
- rn_meishi_food-mop-cart-list 购物车页
- rn_meishi_c-group-order-submit 囤点提单页

**↓加群获取 DUO 官方文档访问权限↓↑加群获取 DUO 官方文档访问权限↑**

## 目录

- 
    1. FEDO 与 DUO
    - 1.1 一个完整的创建工作流的例子
    - **1.2 上线**
- 
    1. （必学）DUO 前置 🧀
    - 2.1 初步了解
    - 2.2 认识 DUO 页面
        - 2.2.1 切分支与 FEDO 相关操作
        - 2.2.2 物料树
        - 2.2.3 物料属性面板
        - 2.2.4 bundle 页面
        - 2.2.5 页面操作栏
        - 2.2.6 工具栏
    - 2.3 多人协同
        - 2.3.1 DUO 页面上修改和提交
        - 2.3.2 多刷新、多提交
        - 2.3.3 DUO 页面对应的 git 仓库
- 
    1. 我想修改大客的 npm 包
    - 3.1 目前的大客侧 npm 包
        - 3.1.1 提单页
        - 3.1.2 购物车页
    - 3.2 本地启动 DUO 页面与调试 npm
    - 3.3 升级包版本（发包/升版/ ka-mop-max-duo 研发标准流程）
    - 3.4 集中发包能力
    - 3.5 公共npm
- 
    1. 我想修改/阅读 DUO 协议
    - 4.1 DUO 的生命周期
    - 4.2 使用 Groovy 修改 DUO 协议
    - 4.3 DUO 的特殊 props
    - 4.4 为组件添加 props
- 
    1. 囤点提单页 ka-mop-duo-shelf-mix 解读专区
    - 5.1 简介
        - 5.1.1 囤点提单页
        - 5.1.2 仅秒提页
        - 5.1.3 购物车页-修改规格
    - 5.2 props 参数
    - 5.3 与外界的通信
        - 5.3.1 数据暂存
        - 5.3.2 发起请求
        - 5.3.3 通信相关组件 props 解析
    - 5.4 提单页相关路径

## 1. FEDO 与 DUO

DUO 已经接入 FEDO，全部开发流程都有 FEDO 的工作流做卡控，跟着工作流走就行。
DUO 页面的 FEDO 需要建在 “[到餐用户前端研发组](https://www.wangjinghong.com/group/143/sprint/64855?groupId=143)” 下，这里可以看看实际的工作流例子：[一个示例工作流](https://www.wangjinghong.com/group/143/sprint/64855/task/209953/workflow/226943/node/309998?groupId=143)

### 1.1 一个完整的创建工作流的例子

**Step1. 在到餐用户前端研发组里面创建一个新的迭代Step2. 创建任务**
创建任务时的工作流模板选择：[【DUO三端】 开发模板(分支版2.0)](https://www.wangjinghong.com/group/143/workflow/template/detail?templateId=32989)**Step3. 启动工作流**
这里会弹一个窗，让我们选择构建产物和 pageId。
我们根据不同的 bundle 有不同的选择：
- rn_meishi_food-mop-cart-list 购物车页
- pageId：12443
- 构建产物：只需要选 MRN，购物车页暂时没有 web 侧
- rn_meishi_c-group-order-submit 囤点提单页
- pageId：12413
- 构建产物：MRN、web 均有。但是关于本次发布需不需要发 web，建议向 chenliang54 确认。（一般来说不需要发，因为只有 MRN 有囤点的功能，发 web 是出于双端代码同步的考虑）
**Step4. 进入你本次开发的 DUO 页面**
开发准备这里可以看到这次你的 DUO 页面的 release 分支和 feature 分支（均为自动创建）。
你可以根据你本次开发的 bundle 和分支自己拼接 DUO 页面的跳链

```
<https://duo.example.com/portal/page/detail2/${对应的pageId}?branch=${你的分支名}>
举个 🌰：<https://duo.example.com/portal/page/detail2/12413?branch=feature/fedo-210117>
```

或者直接在现成的 DUO 页面里面切到你的分支
**Step5. 升级依赖**
如果本次开发改动了大客侧的 npm 包，并且完成了[发包流程](https://km.example.com/collabpage/2707983480#b-57e9f62be071498fa616b90d97d1586b)。那么需要在上一步中的 duo 页面更新对应的依赖项版本， CMD+S **保存**并右上角**提交**之后，再进入下一步的FEDO打包流程。
PS：由于是你的分支，所以依赖也只会在你的分支生效；当保存提交之后， FEDO 才会检测到有新的 commit。
**Step6. 打包**
【DUO】选择 duo-builder 版本，选择 测试版本，版本见：[https://km.example.com/collabpage/2741401008](https://km.example.com/collabpage/2741401008)。
后续正常走流水线进行打包即可，但是有一点要注意：
在合 release 之前，维持在 RD 开发自测阶段即可，需要重新打包就直接重试"MRN 开发"。
**注意：不要点“自测通过”**。
**为什么不要点“自测通过”？**
DUO 的这个模板，你自测结束之后直接就让你合 release 了，随后就是打正式包。为了避免麻烦，维持在这个阶段，等到准备合 release 了之后再点自测通过。
**我不小心点了咋办？**
直接重试整个 02. RD开发自测阶段
另外，如果你已经在提测环境准备阶段了，准备重新打包，请**直接重试整个“03. 提测环境准备”为什么不能直接重试“MRN 测试环境部署”？**
<u>“MRN 测试环境部署”</u>的 talos 模板跟上面 <u>“02. RD开发自测阶段”里面的“MRN 开发”</u>的 talos 模板不一样，少了拉取 DUO 协议的步骤，因此直接重试 “MRN 测试环境部署”的话，无法获取更新后的 DUO 协议。而直接重试整个“03. 提测环境准备”，在“注入DUO环境变量”里面会拉最新的 DUO 协议。

### **1.2 上线**

囤点提单页归属于交易侧，上线一般跟随交易的周迭代（周一、周三）。记得**提前一天**（周五、周二）把迭代迁移到交易对应的 fedo 周迭代（周迭代的具体链接，可以问 chengliang54），并跑完开发流水线。另外因为项目本身隶属于交易，所以每次发布前的自动化需要找张晴(zhangqing54)看一下，确认后发布（PS：交易侧的自动化测试比较完整，所以自动化令人比较安心）
购物车页归属于大客侧，仅大客侧使用，上线建议跟随交易周迭代。自动化测试负责人是谢豪(xiehao13)
交易周迭代可以在 **交易迭代群** 里面咨询。

## 2. （必学）DUO 前置 🧀

悄悄放置一个 DUO 官方文档，可供随时查阅
[https://km.example.com/collabpage/1369701555](https://km.example.com/collabpage/1369701555)

### 2.1 初步了解

官方引导视频，供参考

### 2.2 认识 DUO 页面

### 2.2.1 切分支与 FEDO 相关操作

这里可以切换当前 page 的分支。
点击“任务”按钮可以快捷跳转到该分支对应的 fedo 流水线页面，从此不必再在一堆迭代里面翻找，非常方便。

### 2.2.2 物料树

下文中，**组件**和**物料**是同一个意思，根据 Br 的心情（哪个顺口）用哪个
这块就是页面上相关的物料。
点击右侧的小齿轮图标可以弹出菜单
- 查看组件：查看该物料的资产信息，包括是谁创建的，物料对应的 npm 包名，最新是什么版本，物料在哪个仓库里面，仓库的 git ssh 等等
- 修改标签：修改物料在物料树上的名称
- 升级物料：可以便捷的升级该物料对应 npm 包的版本。如果一个物料有新的版本可以升级，那么会在左侧有一个 ↑ 红色小箭头
- 删除：在物料树上删除这个物料

### 2.2.3 物料属性面板

在这里配置各个物料
**属性**
传给这个物料 npm 的 props
小 tip：DUO 里面类似这种输入框，都可以通过点击旁边的 "</>" 按钮来唤起**更人体工学的编辑框高级设置**
一般只填写里面的 displayRule，用来控制该物料什么时候在页面上展示。
每次从 true 到 false 或者从 false 到 true 的更新会触发组件的挂载/卸载。
**样式**
当你的 npm 包在 DUO 的物料描述文件(npm 包根目录的 description.json)中声明了 styleType 不为 null 的 props，就可以在这里用更友好方式填写 style

```json
{
  "name": "@meishi/br-test",
  "title": "test",
  "props": [
    {
      "name": "wrapperStyle",
      "required": false,
      "styleType": "view",
      "label": "容器样式",
      "desc": "自定义容器的样式配置",
      "defaultValue": null,
      "type": "Object"
    }
  ],
  "slots": [],
  "events": {
    "emit": [],
    "on": []
  }
}
```

**触发事件**
在这里配置 DUO 里面的组件通信。你可以在这里配置该组件向另一个组件发送事件/触发相应的函数。
具体实现方法、细节、原理请参考官方文档：[https://km.example.com/collabpage/2592258233](https://km.example.com/collabpage/2592258233)

### 2.2.4 bundle 页面

因为大客侧特殊的开发方式（大客相关逻辑均不使用 yooz 低代码平台创建组件，纯手写 npm），因此我们开发一般都是本地启动 + 真机预览。
所以可以先不管这块。

### 2.2.5 页面操作栏

同 2.2.4，不用管
用到的只会有本地调试的按钮
使用方法详见后文的本地启动 DUO 页面部分

### 2.2.6 工具栏

**常量**
这里定义的常量，都可以在组件的属性配置中用 [CONST.xxx](http://const.xxx/) 的方式来访问。
**数据 API**
这里用来存放跟 API 请求相关的配置，用的基本上是“preview/update/submit入参配置”，用来描述 DUO 的 preview/update/submit 三种请求的参数。三种请求的参数是统一的。
进一步了解这三种请求和数据 API 中其他的配置，请参照：[02. DUO 页面生命周期配置](https://km.example.com/collabpage/1735774570)**依赖**
点开后出现这个界面
<u>勾上左上角的“proCode显示beta版本”，否则你打的 beta 版本 npm 包不会在这里显示。</u>
让我们来盘一盘这里的几个依赖的意思：
- 直接依赖：页面上组件对应的 npm 包的版本
- 间接依赖：可以看做是 package.json 里面的 dependencies
- 自配置依赖：相当于 pnpm 里面的 overrides，yarn 里面的 resolutions
改完依赖之后记得在主界面按 cmd+S 保存并 commit 提交
如果你发现 npm_a 的版本和你在 npm_b 的 package.json 里面实际锁的版本不一致，那很可能是因为**自配置依赖**里面已经通过全局的 overrides 锁了 npm_a 的版本。
举个实际踩过坑的例子：
**事件-**你升级了购物车页 @meishi/ka-mop-shop-cart-modify** **包 package.json 里面的 @meishi/ka-mop-duo-shelf-mix 版本并重新发版了 @meishi/ka-mop-shop-cart-modify，随后也正常的在 duo 页面里面升级了 @meishi/ka-mop-shop-cart-modify 的版本。但是你重新打包之后发现新打出的 bundle 里，@meishi/ka-mop-shop-cart-modify 中依赖的 @meishi/ka-mop-duo-shelf-mix 的版本并没有升级，还是老的。
原因-这是因为购物车页已经在自配置依赖里面锁死了 @meishi/ka-mop-duo-shelf-mix 的版本，所以@meishi/ka-mop-shop-cart-modify** **包 package.json 里面的 @meishi/ka-mop-duo-shelf-mix 版本被 overrides 覆盖了。
**解法-**你需要在自配置依赖里面升级 @meishi/ka-mop-duo-shelf-mix 的版本。重新打包即可。

### 2.3 多人协同

### 2.3.1 DUO 页面上修改和提交

当你修改了 DUO 页面上的内容，记得按 cmd+S 来保存。点击右上角的提交按钮将你的修改进行 commit
弹这个弹窗的话可以不用管，他的意思是你的 commit 中用了非正式版的 npm，记得之后打正式包之前，把所有 npm 包升正式版。
勾上“今日不再提醒”之后点继续提交
随后会进入这个页面。
这个页面描述了你本次的修改。**请注意：一定要仔细重新 check 这里的内容是否符合你的修改**
因为 DUO 孱弱的多人合作机制，当两个人同时在这个分支上开发的时候，极有可能导致两人出现了 conflict。**或者因为你修改 DUO 页面前没有刷新导致你没有把 git origin 的修改拉下来**，从而你的本次修改会覆盖远端的协议（并非 merge，而是直接覆盖），具体表现到这个页面上就是：你会发现这里莫名其妙改了一些你没有动过的配置。
所以一定要仔细重新 check 这里的内容，以防出现问题（然而事实上依然经常出现问题，因为总有人修改页面前不刷新）。
当你发现打的包里面不知道为什么你的修改突然失效了，不妨去检查一下现在远端的 DUO 协议，说不定是其他人没有拉你的 commit 就提交，把你的 commit 的内容给覆盖了。
如果你在 DUO 上修改了协议，只要你进行了 commit 操作，对应的测试包就会直接使用最新的协议，不需要重新打包。
原理就是——这里的协议本质上是保存在后端的，由后端进行解析，所以你 commit 了之后测试包就会直接生效。
也正因如此，如果你更改了 npm 包的版本而非改动协议内容，还是得重新打包的。因为后端解析肯定管不到你 bundle 里面的 npm 包代码。

### 2.3.2 多刷新、多提交

记得经常刷新 DUO 页面（指的是 F5 浏览器页面的那种刷新），因为 DUO 不会自动拉取你所在分支 origin 上的变更，所以你需要经常进行手动刷新来把其他人在你这个分支上的修改给拉下来。
当你把其他人的修改拉下来时，如果你的页面本地还有未提交的修改，极有可能和你本地出现了 conflict，届时需要自己看着解决。但是因为 Br 刷新的非常勤快，而且一旦有修改，就会直接提交，所以 Br 还未尝到 conflict 的苦。
因此，一定要<u>**多刷新、多提交**</u>。

### 2.3.3 DUO 页面对应的 git 仓库

DUO 页面有自己的 git 仓库，用来储存 DUO 页面协议
- 购物车页：[DevTools](https://dev.example.com/code/repo-detail/nibfe/food-cart/file/list)
- 囤点提单页：[DevTools](https://dev.example.com/code/repo-detail/nibfe/duo-food-order-submit/file/list)
可以看到这些仓库里面就是协议本身。
有关协议的所有历史记录查看，conflict 解决，各种 merge 均推荐把仓库 clone 到本地，用 git 手动解决。不建议使用 DUO 页面上的 git 功能解决（因为这块功能做的比较弱）。
本地 commit 与 DUO 上面直接 commit 拥有同样的效果。commit 完记得刷新 DUO 页面。

## 3. 我想修改大客的 npm 包

### 3.1 目前的大客侧 npm 包

数据有效时间：2025.05.08

### 3.1.1 提单页

**@meishi/ka-mop-duo-shelf-mix**
提单页主 npm 包，逻辑复杂，详见下文“提单页 ka-mop-duo-shelf-mix 解读专区”
**@meishi/ka-mop-check-shop-modal**
秒提门店二次确认弹窗-即买即核销，秒提门店二次确认弹窗

### 3.1.2 购物车页

**@meishi/ka-mop-cart-discount-detail**
融合购物车优惠明细
**@meishi/ka-mop-deal-card-list**
融合购物车场景下团购页面团购卡片列表
**@meishi/ka-mop-duo-submit-remind-modal**
融合购物车提单提示弹窗
**@meishi/ka-mop-shares**
融合购物车页公共 util，type，组件
**@meishi/ka-mop-shop-cart**
本店购物车核心包，卡片列表
**@meishi/ka-mop-shop-cart-bottom**
本店购物车底 Bar，同时也在food-poi使用
**@meishi/ka-mop-shop-cart-modify**
本店购物车修改规格浮层，实际是包裹了ka-mop-shelf-mix组件
**@meishi/ka-mop-shop-cart-title**
本店购物车购物车头部信息区

### 3.2 本地启动 DUO 页面与调试 npm

完成[前置准备](https://km.example.com/collabpage/2708260969)后，根据自己的需求完成[ka-max-duo X 囤点提单页开发调试](https://km.example.com/collabpage/2708411332)或者[ka-max-duo X 购物车页开发调试](https://km.example.com/collabpage/2708171137)
目前线下没有稳定的囤点品牌/门店数据，因为囤点和购物车需要依赖融合货架，而融合货架在线下主干上没有配置，只有在每次相关需求开发的特定泳道上有。
所以如果要本地启动查看效果，可以使用线上的一点点、库迪、良品铺子（良品大厦店）门店

### 3.3 升级包版本（发包/升版/ ka-mop-max-duo 研发标准流程）

ka-mop-max-duo 代码仓库的开发需要在 fedo 我们自己的研发组（大客户前端组）里面走 fedo 流程。
选择的项目和模板如下：
该模板的使用教程如下：（一般直接点击发布按钮即可，不用调整参数）

<details>
<summary>使用 duo-cli 的老流程，目前只支持发布测试版，已废弃，请走上方的 FEDO 流程</summary>

这里以发布 @meishi/ka-mop-deal-card-list 为例。

1. 
    - 修改对应的 package.json 文件（packages/ka-mop-deal-card-list/package.json），将版本 version 改为你想发的 0.x.y-beta.z
2. 
    - 在 ka-mop-max-duo 仓库里 cd 到对应的包目录 packages/ka-mop-deal-card-list
3. 
    - 执行 duo publish-procode 命令
4. 
    - 一路 yes 回车即可
    常见发包问题：
    **提示不能 resolve **`@meishi/ka-mop-shares`
    需要在本地先 yarn build 一次 `ka-mop-shares`目录。原因：目前的构建脚本会寻址本地的lib文件目录而不是 node_modules, 第一次开发这个仓库会被卡住
    **提示没有登录**
    使用`mnpm whoami`命令确认自己是否已经登录。没有登录的话，执行 `mnpm login --auth-type=sso` 命令进行 SSO 登录后，随后尝试重新发包
    **提示没有对应包的发布权限**
    找包负责人，让负责人执行 `mnpm owner add your_mis package_name`来给你加权限，示例命令如下：

```
mnpm owner add liyuqian06 @meishi/ka-mop-shares
```

</details>

### 3.4 集中发包能力

当前可以通过 talos 进行发布[talos](https://talos-better.example.com/release?appId=21219&env=test&menuKey=release&templateId=526826)，在发布的地方填上对应的包名字，即当前 ka-mop-max-duo 仓库的子包文件夹名字，另外需要关注的是当前还是不支持依赖的依赖发布，所以如果 A 和 B 依赖了 C，那需要先发布 C，再更新 A 和 B 依赖的 C 的版本后，同时可以发布 A，B，也可以通过本地执行 duo publish-procode，一路回车（这种只能一个终端一次发布一个）
需要注意的点

1. 
    - 需要将当前的新版本在子包的 package.json 文件的name那里更新为你要发布的版本号再进行发布操作
2. 
    - 最好类似这样设置自己发布的包，举个🌰: 0.0.17-yuanlu.1

### 3.5 公共npm

因为现在 ka-mop-max-duo 目录下的所有子包其实同属于秒提业务，所以各个子包之间有部分ts定义、比如 mopShopInfoVO 等，还有一些通过能力比如埋点、日志或者一些基础方法、组件等都是一样的，所以我们有一个 ka-mop-shares 包，用于放置所有项目共用的东西， 后续类似多子包共用的，需要放置在这里面，避免多处写同样的代码
记得给 @meishi/ka-mop-shares 以及所有用到 @meishi/ka-mop-shares 的 npm 包发版。
后续的计划**已完成**：把 @meishi/ka-mop-shares 放到 DUO 的自配置依赖里面，这样以后就不用给所有包都升版了，只需要升 @meishi/ka-mop-shares 的版本，然后在 DUO 的配置页面上升级 @meishi/ka-mop-shares 即可。

## 4. 我想修改/阅读 DUO 协议

除了<u>**上文必会的 DUO 前置知识**</u>，如果你还想要对 DUO 协议进行修改和阅读理解，那么以下知识也是必须学会的。

### 4.1 DUO 的生命周期

经过上面的 DUO 必会知识学习，想必你已经熟练掌握了[https://km.example.com/collabpage/1735774570](https://km.example.com/collabpage/1735774570)。
接下来我们来实际抓包看一下一个 preview/update 请求的参数。
我们一般只需要看 APPMOCK 里面，接口的 Response 部分即可。由于 DUO 的协议在后端解析的特性，Response 部分包含了所需要的所有数据内容，包含这个请求的实际入参。
以这个购物车页的 preview 为例，我们关心的有：
- data：DUO 页面实际用来渲染的数据
- nodeDataMap：DUO 页面上各个组件接收到的数据，里面包含了这个组件实际接受的 props，如果要查看前端某个组件实际拿到的数据，应该查看这里而不是下面的 bizRes。对比这里的数据和后端的实际返回（下面的 bizRes.data）可以看出 DUO 协议里面的组件入参是不是写错了。
- bizReq：实际提交给后端的请求数据，里面是在 DUO 页面配置的入参，最后根据表达式计算出来的值。
- bizRes：后端实际返回的数据
- data：数据本身
bizReq 和 bizRes 仅在 APPMOCK 中存在，是为了调试方便而添加的字段。实际网络包中并不存在这两个对象。
submit 这个请求比较特殊，通过抓包我们可以发现他没有 nodeDataMap。
因为 submit 作为终态的提交接口，他不会再去做页面组件的卸载/挂载。
小声嘀咕：DUO 是这么设计的，但是我觉得这个设计不合理，凭什么 submit 就不能走正常的组件渲染流程？！这会使得你无法在 submit 的时候处理一些组件的生命周期 。比如你想在 submit 失败的时候弹一个自定义弹窗，你就得确保 submit 之前这个弹窗就已经挂载在页面上了，否则这个弹窗无法在 submit 的时候挂载。

### 4.2 使用 Groovy 修改 DUO 协议

DUO 在编辑框里面采用的是 [groovy 语法](https://km.example.com/collabpage/1466739592)，并辅以一些[保留字](https://km.example.com/collabpage/1733888444)。
梁哥的推荐范式：先写好 js 的版本，再用 AI 帮忙改成 groovy 语法，从而避免因为不熟悉 groovy 语法导致的问题。

### 4.3 DUO 的特殊 props

DUO 框架会给页面上的一级组件注入一个特殊的 props "**duo**"，这个 prop 里面包含了很多 DUO 的高级设置和参数，可以用来获取 rootTag，获取 commonParams，触发高级 DUO 能力等等。如果你发现你需要在组件里面获取一些 DUO 页面相关的参数 or 方法，可以在这里翻找一下。
详见[https://km.example.com/collabpage/2647276429](https://km.example.com/collabpage/2647276429)

### 4.4 为组件添加 props

如果你想要给暴露给 DUO 的组件添加 props，例如下图的“属性”，“触发事件”等等
除了理所当然的给 npm 包 export 的组件添加 props 外，还需要更改 DUO 的描述协议（npm 包同目录下的 description.json 文件)
协议的格式详见[https://km.example.com/collabpage/2700767357](https://km.example.com/collabpage/2700767357)

## 5. 囤点提单页 ka-mop-duo-shelf-mix 解读专区

### 5.1 简介

数据更新时间：2025.05.13
这个 npm 包目前存在于三种地方

### 5.1.1 囤点提单页

秒提的大部分功能都在这个组件内，包括选规格、到店自取方式、备注信息等。

|  | 使用场景 | 图示 | 说明 |
| --- | --- | --- | --- |
| currentPage= 10 | 可囤可点的选规格 |  | 除顶部双Tab外，都在packages/ka-mop-duo-shelf-mix/src/ShopCustomization 组件内实现 |
| currentPage= 20 | 提单页 | 单团购下单                                   购物车多品提单                           西快随心配提单 | 大客只负责部分组件，都在packages/ka-mop-duo-shelf-mix/src/SubmitComp组件内实现，这个大组件内又拆分多个小组件 |
| --- | --- |  |  |
| 门店信息组件（必出） |  |  |  |
| 就餐方式组件 （按需展示） |  |  |  |
| 商品信息 （必出） |  |  |  |
| 备注（按需展示） |  |  |  |

### 5.1.2 仅秒提页

仅秒提页归属于大客（ka-mrn/ka-mop-mix-order-submit），没有使用 DUO ，而是在 ka-mop-duo-shelf-mix 的基础上套了一个壳子。整体功能与囤点提单页保持一致，只是去除了双 Tab。

| 选规格页 | 提单页 |
| --- | --- |
|  |  |

### 5.1.3 购物车页-修改规格

购物车页的修改规格浮层复用了 ka-mop-duo-shelf-mix 中选规格的功能。

| 修改按钮 | 修改页面 |
| --- | --- |
|  |  |

### 5.2 props 参数

| props | 描述 |
| --- | --- |
| onlineOrderPropsVo | 即[https://km.example.com/collabpage/2706503377](https://km.example.com/collabpage/2706503377) |
| wrapCardStyle | 传入样式，目前只在选规格页用到height，这个高度是当前提单页的高度-双Tab的高度 |
| groupOrderParams | 即commonParams，这里有经纬度、用户登录、城市等信息 |
| onChange | 使用秒提壳容器(submit-miaoti-container，duo 上的一个组件，用来做 ka-mop-duo-shelf-mix 和 duo 的中间人)立刻发起update请求，并在秒提壳容器中暂存秒提参数 |
| onUserAction | 在秒提壳容器中暂存秒提参数，不发起请求 |
| onGoGroupBuy | 切换到买团购 Tab，发起 update 请求 |
| pageQuery | 页面跳链上的参数 |
| isModify | 是否是购物车修改规格场景（会根据这个做一些 UI 上的改变） |

### 5.3 与外界的通信

### 5.3.1 数据暂存

ka-mop-duo-shelf-mix 与 DUO 之间有一个壳子——@meishi/submit-miaoti-container（由交易侧开发），用来做 ka-mop-duo-shelf-mix 与 DUO 页面的通信，包揽了暂存状态，发送请求等等功能，使得 ka-mop-duo-shelf-mix 可以成功的收束为一个较为原子化的 UI 组件。
**为什么要做数据暂存？**
首先后端肯定是做不了状态维持的，而 DUO 架构中的前端也没有类似全局的 store 这种机制来维持页面的状态。
那么如果我们要做状态恢复（典型例子就是提单页切 Tab 之后再切回来，要恢复用户的选择），就得用一些 trick 方法。提单页给出的解决方案就是使用 submit-miaoti-container 秒提壳容器暂存秒提的状态数据（useState)，并且在每个请求中把暂存的数据发送给后端，后端 response 中原样返回这些数据。这样每次与后端的请求交互都可以恢复状态，且暂存的状态在页面的整个生命周期里都可以保存和更新。
与此同时，DUO 页面上还有另一个组件 @meishi/group-submit-data-container（数据保存组件）。这个组件用来在 DUO 协议上接受（transferData)。
**为什么额外需要一个数据保存组件？**
因为对于 DUO 的每次请求，并不是后端依赖的所有数据都会放在业务直接发起请求的负载（payload）里面，那些后端依赖的通用数据 bizRes 都是要用 groovy 语法在 API 请求那块那边计算出来的。
在这里写的 groovy 表达式，能拿到的数据只有页面组件（NODE) 上的 props，换句话说，在这里没法直接从 submit-miaoti-container 的 state 里面把我们暂存的数据给掏出来，一旦这次请求的 payload 里面没有携带我们要更新的暂存数据（mopPreOrderParam)，那就只能从 group-submit-data-container 上的 props 把上次请求后端传回的暂存数据扣下来了。
想要查看整个提单页数据暂存与状态流转的全过程，可以看[提单页跨 Tab 数据暂存和状态流转逻辑梳理](https://km.example.com/collabpage/2710863245#b-e54eea4a99bc44f89667c04bfdc9ff4e)

### 5.3.2 发起请求

ka-mop-duo-shelf-mix 内有三种与 DUO 请求相关的函数，他们的作用如下：

| ka-mop-duo-shelf-mix |  |
| --- | --- |
| onChange | 向秒提壳容器暂存秒提参数，并在壳容器中立刻使用参数发起 update 请求 |
| onUserAction | 向秒提壳容器暂存秒提参数，下次壳容器发起请求时会携带暂存的参数 |
| onGoGroupBuy | 让秒提壳容器发起切换买团购的请求流程，等效于点击双 Tab 上的团购 Tab |

### 5.3.3 通信相关组件 props 解析

| submit-miaoti-container 秒提壳容器 |  |  |
| --- | --- | --- |
| 本地状态存储 | mopTempData | 暂存ka-mop-duo-shelf-mix 传过来的秒提参数 |
| props | onlineOrderPropsVo | 透传mopPreOrderVO到ka-mop-duo-shelf-mix |
|  | transferData | 透传transferDataVO，和mopTempData结合使用 |
|  | tabInfoList | 自身使用，判断在不在双Tab场景 |
|  | commonParam | 透传commonParam到ka-mop-duo-shelf-mix |
|  | pageQuery | 透传mopPreOrderParam到ka-mop-duo-shelf-mix |
|  | tabHeight | 双Tab高度 |
|  | effectiveScreenPer | 结合pageQuery判断在不在浮层内，算出浮层本身内容区高度后-tabheight传给ka-mop-duo-shelf-mix |
|  | wrapCardStyle | 设置自身容器的一些样式 |
| event | onOrderDataChanged（不暴露） | 即传给ka-mop-duo-shelf-mix的onChange事件，暂存数据到mopTempData，并根据transferData和mopTempData组装payload，立即调用duo的update，成功后调用onSubmitSuccess |
|  | userAction（不暴露） | 即传给ka-mop-duo-shelf-mix的onUserAction事件，暂存数据到mopTempData |
|  | onSubmitSuccess | duo请求成功回调，配置在duo协议里 |
|  | onGoGroupBuy（不暴露） | 即传给ka-mop-duo-shelf-mix的onGoGroupBuy，根据transferData和mopTempData，再拼接团购的tabSelect参数组装payload，立即调用duo的update，成功后调用onSubmitSuccess |
| 仅能通过ref访问的方法 | miaoTiUpdateData | 双Tab组件切换Tab的时候，通过此方法拿到秒提的参数并立即调用duo的update，成功后调用onSubmitSuccess |
|  | onMiaoTiSubmitData | 提单页提交订单的时候，通过此方法拿到秒提的参数 |
| @meishi/group-submit-data-container 数据保存组件 |  |  |
| --- | --- | --- |
| props | mopTempData | 即DATA_SOURCE.data?.transferDataVO?.transferData?.mopTempData，在每次update的时候都会读这个哪里来的？（submit-miaoti-container的 onOrderDataChanged和onGoGroupBuy的时候 传给后端又传回来的） |
|  | quantity | 即DATA_SOURCE.data?.transferDataVO?.transferData?.quantity?.toString();在每次update的时候都会读这个哪里来的？团购Tab切回秒提Tab的时候传给后端又传回来的） |

### 5.4 提单页相关路径

[https://km.example.com/collabpage/2707407193](https://km.example.com/collabpage/2707407193)

---
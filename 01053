# LoRA、蒸馏、微调、SFT

劳ra

蒸馏、微调 json 训练

这三个词都常出现在大模型训练里，可以把它们理解成三种“让模型变得更适合某个目标”的方法，但层级不同。

## 1. 蒸馏是什么意思？

**蒸馏**，也叫 **知识蒸馏**，意思是：

> 用一个更强、更大的模型当“老师”，训练一个更小、更快的模型当“学生”。
> 

比如 GPT-5.5 很强但运行成本高，可以让它给很多问题生成答案、推理步骤或评分，然后用这些数据去训练一个小模型。小模型学的是大模型的“行为模式”。

类比一下：

> 老师很厉害，但不可能每次都亲自上场；于是把老师的经验整理成教材，让学生学会类似的解题方式。
> 

蒸馏的目的通常是：

降低成本、提高速度、减小模型体积，同时尽量保留大模型的能力。

---

## 2. 微调是什么意思？

**微调**，英文是 **fine-tuning**，意思是：

> 在一个已经训练好的基础模型上，再用特定数据继续训练，让它更适合某个任务或风格。
> 

比如一个通用大模型本来什么都懂一点，但你想让它更擅长：

客服回复、法律文书、医学问答、代码审查、公司内部知识问答、某种写作风格。

就可以拿相关数据继续训练它，这个过程叫微调。

类比一下：

> 基础模型像一个大学毕业生，微调像入职培训。它已经有通用能力，但还需要学习某个公司的流程、话术和标准。
> 

微调通常会改变模型的一部分参数，或者通过特殊方法让模型学到新行为。

---

## 3. LoRA 是什么意思？

**LoRA** 全称是 **Low-Rank Adaptation**，中文常叫 **低秩适配**。它是一种更省资源的微调方法。

普通微调可能要改动模型里大量参数，成本高、显存需求大。LoRA 的思路是：

> 不直接大规模改原模型，而是在模型旁边加一小组可训练参数，只训练这小部分。
> 

原模型主体基本不动，LoRA 像一个“外挂适配器”。

类比一下：

> 不重新装修整栋楼，只在关键位置加几个可调节装置，让它适配新的用途。
> 

LoRA 的优点是：

训练成本低、显存占用小、可以为不同任务保存不同 LoRA 适配器，切换方便。

---

## 简单对比

| 概念 | 核心意思 | 类比 | 主要目的 |
| --- | --- | --- | --- |
| 蒸馏 | 大模型教小模型 | 老师教学生 | 做出更小、更快、更便宜的模型 |
| 微调 | 让已有模型继续学习特定任务 | 入职培训 | 让模型更适合某个领域或风格 |
| LoRA | 一种轻量微调方法 | 给模型加外挂适配器 | 用更低成本完成微调 |

一句话总结：

**蒸馏是“老师带学生”，微调是“模型再培训”，LoRA 是“低成本再培训的方法”。**

**SFT** 是 **Supervised Fine-Tuning** 的缩写，中文通常叫 **监督微调**。

意思是：

> 用“人工整理好的示范数据”继续训练一个已经预训练好的模型，让它学会按期望方式回答。
> 

比如训练数据长这样：

```
用户：请帮我写一封请假邮件
理想回答：您好，我因身体不适，想请假一天……
```

模型会学习：遇到类似输入时，应该输出类似的高质量答案。

它和普通“微调”的关系是：

**SFT 是微调的一种，而且是大模型对齐训练里很常见的一步。**

简单类比：

> 微调像“入职培训”，SFT 像“拿标准答案做培训”。
> 
> 
> 老师给你很多题目和参考答案，你照着学怎么答。
> 

在大模型训练流程里，常见顺序是：

**预训练 → SFT → RLHF / DPO 等对齐优化**

一句话总结：

**SFT 就是用带标准答案的数据来教模型“应该怎么回答”。**
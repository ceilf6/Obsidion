# 2

git checkout --orphan release && git rm -rf . && git commit --allow-empty -m "create empty release branch" && git push origin release

git add tasks/session-2/training_data.csv && git commit -m "$(cat <<'EOF'

feat: 添加新训练数据触发 CI/CD 验证

Co-Authored-By: Claude Opus 4.7 [noreply@anthropic.com](mailto:noreply@anthropic.com)
EOF
# 淘天2024春

# 20

![image.png](%E6%B7%98%E5%A4%A92024%E6%98%A5/image.png)

单调栈不适合本题，因为单调栈适合局部，但是本体要求全局合法

简单问题出发：可以移除多少后缀

发现：“必须选择一段”移除后，剩下 左边 和 右边 连接，要求左右都是单调的并且 左边最后一个小于右边第一个

由于左右两边单调的性质，加上衔接处的关系，**一边的扩张必然导致另一边的缩减**

那么**先让一边到达极限，然后再让另一边逐步压迫**，就能得到所有过程可能

```jsx
/**
 * @param {number[]} nums
 * @return {number}
 */
var incremovableSubarrayCount = function(nums) {
    const LN = nums.length
    let ans = 0
    let i = 0
    while(i<LN-1 && nums[i+1]>nums[i]){
		    // 寻找左边极限
        i++
    }
    if(i===LN-1) // 全体单调的情况
        return (LN+1)*LN/2
    ans += i+2 // 没有后缀

    // 枚举后缀，即右边开始扩张
    let j = LN-1
    while(j>0){
        // 保证后缀单调
        if (j < LN - 1 && nums[j] >= nums[j + 1]) break;

        while(i >= 0 && nums[i] >= nums[j])
            i-- // j 扩张导致的 i 缩减
        ans += i+2
        j-- 
    }
    return ans
};
```

# 21

![image.png](%E6%B7%98%E5%A4%A92024%E6%98%A5/image%201.png)

**堆的操作是 O(log n)**

局部有序

**链表 / 数组维护有序结构，本质是 O(n)**

全局有序

线性遍历会导致特殊情况复杂度过高

由于本题只需要对最小的进行操作，不需要其余部分强制，所以可以利用小顶堆

```jsx
const rl = require("readline").createInterface({ input: process.stdin });
var iter = rl[Symbol.asyncIterator]();
const readline = async () => (await iter.next()).value;

void async function () {
    const [n,m] = (await readline()).split(' ').map(it => Number(it))
    const heap = (await readline()).split(' ').map(it => Number(it))
    const ups = (await readline()).split(' ').map(it => Number(it))
    function Down(i){
        while(true){ // 不断往下
            const lIdx = 2*i+1
            const rIdx = 2*i+2
            let curSmall = heap[i]
            const l = lIdx<n ? heap[lIdx] : null
            const r = rIdx<n ? heap[rIdx] : null
            let smallIdx
            if(l!==null && l<curSmall){
                smallIdx = 2*i+1
                curSmall = heap[smallIdx]
            }
            if(r!==null && r<curSmall){
                smallIdx = 2*i+2
            }
            if(!smallIdx) // 无需交换、无需再往下
                break
            [heap[i],heap[smallIdx]] = [heap[smallIdx],heap[i]]
            if(smallIdx===lIdx)
                i = lIdx
            else if(smallIdx===rIdx)
                i = rIdx
        }
    }
    for(let i=Math.floor(n/2)-1;i>=0;i--){
        Down(i) // 建堆
    }
    function Pop(){
        const head = heap[0]
        heap[0] = heap[heap.length-1] // 出堆时将最后一个提到头然后往下调整
        // heap.splice(heap.length-1,1)
        heap.pop()
        Down(0)
        return head
    }
    function Up(i){
        while(true){ // 向上调整也需要不断往上
            if(i<=0)
                break
            const fatherIdx = Math.floor((i-1)/2)
            const father = heap[fatherIdx]
            if(father>heap[i]){
                [heap[i],heap[fatherIdx]] = [father, heap[i]]
                i = fatherIdx
            } else {
                break // 没有交换就无需再往上调整
            }
        }
    }
    function Push(newVal){
        heap.push(newVal)
        Up(heap.length-1)
    }
    let maxNum = Math.max(...heap)
    for(const up of ups){
        const head = Pop()
        const newVal = head + up
        if(newVal>maxNum) // 最大值只可能受新来的威胁
            maxNum = newVal
        Push(newVal)
        // console.log(Math.max(...heap))
        console.log(maxNum)
    }
}()
```

import heapq

heapq.heapify

heapq.heappop

heapq.heappush

```python
import heapq

n, m = map(int, input().split())
heap = list(map(int, input().split()))
ups = list(map(int, input().split()))

# 建堆（原地 O(n)）
heapq.heapify(heap)

# 维护当前最大值
max_val = max(heap)

for up in ups:
    # 取最小值
    smallest = heapq.heappop(heap)

    new_val = smallest + up

    # 放回堆
    heapq.heappush(heap, new_val)

    # 更新最大值
    if new_val > max_val:
        max_val = new_val

    print(max_val)
```

# 22

![image.png](%E6%B7%98%E5%A4%A92024%E6%98%A5/image%202.png)

我刚开始想着从 m 这个限制出发

假如从 k 出发，**二分答案** 然后 **贪心**看是否符合m的条件

```jsx
const rl = require("readline").createInterface({ input: process.stdin });
var iter = rl[Symbol.asyncIterator]();
const readline = async () => (await iter.next()).value;

void async function () {
    let [n,m] = (await readline()).split(' ').map(it => Number(it))
    const str = await readline()
    const Ws = []
    let idx = str.indexOf('W',0)
    while (idx !== -1) {
        Ws.push(idx);
        idx = str.indexOf('W', idx + 1);
    }

    if(!Ws.length){
        console.log(0) // 注意特殊情况
        return
    }
    function check(k){
        let idx = 0 // 当前已经覆盖第 i 个 W
        let cnt = 0 // 已经使用了刷的次数
        while(idx<Ws.length){
            cnt++
            const curLast = Ws[idx]+k-1
            while(idx<Ws.length && Ws[idx]<=curLast) // 覆盖了的
                idx++
        }
        return cnt<=m
    }
    let l = 1
    let r = n
    while(l<=r){
        const k = Math.floor((l+r)/2)
        if(check(k)){
            r = k-1 // 尝试更小
        }else{
            l = k+1
        }
    }
    console.log(l)
}()

```

# 2

[锁高速缓存](../../../%E6%B5%8F%E8%A7%88%E5%99%A8/%E5%AE%A2%E6%88%B7%E7%AB%AF%E7%BC%93%E5%AD%98%20%E6%B5%8F%E8%A7%88%E5%99%A8%E7%BC%93%E5%AD%98%20%E7%A1%AC%E7%9B%98%E7%BC%93%E5%AD%98%20HTTP%E7%BC%93%E5%AD%98/%E7%BC%93%E5%AD%98%E5%B9%B6%E5%8F%91%E6%8E%A7%E5%88%B6%203270f8d8d1fd800ca556d44d12a8daed.md) 

# 3

基类有 有参构造函数 ，派生类必须在其构造函数中显式调用

# equals 和 ==

**equals 引用内容**

- A：未重写 equals() 时继承自 Object，行为等同于 ==（引用相等）。
- B：对引用类型，== 判断是否指向同一对象。
- C：对基本类型，== 比较数值是否相等。
- D：重写 equals() 时应同时重写 hashCode()，否则在 HashSet/HashMap 等哈希容器中可能出现逻辑错误（如 put 后 get 不到、Set 中出现“看似重复”）。

| **比较方式** | **作用对象** | **比较内容** |
| --- | --- | --- |
| == | 基本类型 | 值 |
| == | 引用类型 | **地址** |
| equals() | 对象 | **内容**（如果重写了） |

```tsx
String a = new String("abc");
String b = new String("abc");

System.out.println(a == b); // false

String s1 = new String("abc");
String s2 = new String("abc");

System.out.println(s1.equals(s2)); // true（内容一样）
```

# 6

Linux 的 less 命令用于大文件的分页查看

/ 表示向前、向下

? 表示向后、向上

n代表重复前一个查找（与 / 或 ? 有关），N代表逆向重复前一个查找（与 / 或 ? 有关）

# 7

WebSocket 在 HTTPS 环境使用协议 wss:// 443

HTTP ws:// 80

HTTP 协议在开发环境中的备用端口 8080

# 8

[**外观模式（Facade）**：为**复杂子系统提供一个统一的简单接口**。](../../../%E6%A8%A1%E5%BC%8F/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%2031f0f8d8d1fd804f96d7ec0e1eabc674.md) 

是简化接口 - 外观模式

代理模式 更加侧重中间代理**控制**

# 13

Java 的抽象方法 有点像 C++ 中的纯虚函数

抽象类可以包含非抽象方法

抽象类的子类只有在是 非抽象类 的情况下才需要实现所有抽象方法

# 视图

使用视图可以达到保护数据的作用，用户只能访问他们被允许查询的结果集

视图可以简化复杂的SQL操作，用户不需要关心表的结构、关联条件和筛选条件

保障数据安全性，提高查询效率是视图的主要作用

# 排序算法

稳定性 是指 如果两个元素相等，排序后它们的相对顺序不变

**“冒插归，计基桶”是稳定的**

冒泡、插入、归并、计数、基数、桶

# Linux权限

题目要求将权限从650修改为-rw-r----x,首先需要理解这两种权限表示:

- 650对应-rw-r-x---
- 目标权限为-rw-r----x

所以需要实现的权限变化是:

1. **用户权限(u):rw-** 保持不变

2. **组权限(g):r-x** 变为 **r-- (去掉x权限)**

3. **其他用户权限(o):--- 变为 --x (添加x权限)**

选项分析:

A. chmod a-x,o+x test

先去掉所有用户x权限,再给其他用户加上x权限,最终结果正确

B. chmod a+x,ug-x test

先给所有用户加x权限,再去掉用户和组的x权限,最终结果正确

C. chmod g-x,o+x test

去掉组的x权限,给其他用户加上x权限,最终结果正确

D. chmod u+x,g-x test

给用户加x权限,去掉组的x权限,这样用户最终会有x权限,与目标权限不符

因此ABC都能得到正确的最终权限结果,而D选项会导致错误的权限设置。这体现了Linux权限可以通过不同的命令组合实现相同的效果。

# 装饰器模式

装饰类和被装饰类可以独立发展，不会相互耦合，可以动态扩展一个实现类的功能

装饰器模式允许向一个现有的对象添加新的功能，同时又不改变其结构

装饰器模式可以嵌套使用多个装饰器，以实现更复杂的功能组合

虽然从外部看来只返回了一个对象，但是过程中是多个对象

```tsx
DecoratorC(
  DecoratorB(
    DecoratorA(
      ConcreteComponent
    )
  )
)
```
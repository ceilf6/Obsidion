# M端 Popcorn 变更

Source: [KM 2759434466](https://km.example.com/collabpage/2759434466)

“仅限公共纬度” 查询了 [https://km.example.com/collabpage/2715237101](https://km.example.com/collabpage/2715237101)后在 [https://luopan.example.com/solution/solution/shelf/solution/shelf/list?link=https%3A%2F%2Fassets-popcorn.example.com%2F2836%2Fshelf%2Flist%2Findex.html](https://luopan.example.com/solution/solution/shelf/solution/shelf/list?link=https%3A%2F%2Fassets-popcorn.example.com%2F2836%2Fshelf%2Flist%2Findex.html) 找到了页面 [https://assets-popcorn.example.com/2836/shelf/edit/index.html?id=386](https://assets-popcorn.example.com/2836/shelf/edit/index.html?id=386) 进行测试

```
// 代码块
1. 是的,当cornerLabelType为2时,传"text":""或不传都可以;
2. 文档中拼错了，已纠正为 RELATED_TO_FILTER
3. 对于历史数据,前端是否可以兼容CornerLabelVO为空,即不传CornerLabelVO的情况呢?其实{"cornerLabelType":1,"text":""}就相当于用户没有设置分组角标,目前这种情况数据库中就没有存cornerLabel,且绝大多数数据是这样的.
4. "分组角标类型"是必展示的,默认选"静态文本",展示输入框;选"与筛选项关联"时不展示ruleTag下拉了,也不请求ruleTag接口了.
```

[image.png](https://km.example.com/api/file/cdn/2759434466/238594871008?contentType=1&isNewContent=false)

```
// 代码块
豪哥，当前二级筛选项是否渲染，被一级筛选项的 recallType === MANUAL 控制，你说的 “若货架为公共维度的货架,则放开限制,一级筛选项和二级筛选项的团单配置都支持手动抓取和系统配置” 是不是就是解除这个限制、公共维度始终允许二级筛选
```

得到肯定答复

PRD：[https://km.example.com/collabpage/2757014658](https://km.example.com/collabpage/2757014658) - 罗盘改动

泉哥提供文档：[美团lowcode Popcorn 平台M端货架开发指南](https://km.example.com/collabpage/2716761191)

目标页面：[https://popcorn.example.com/manage/page/list?projectId=2836](https://popcorn.example.com/manage/page/list?projectId=2836)

页面仓库：[🔗 链接卡片](https://dev.example.com/code/repo-detail/meis/cross-popcorn-materials/file/list)

当前

[图片](https://km.example.com/api/file/cdn/2759434466/234626086002?contentType=1&isNewContent=false)

需要二级的也要支持和一级一样的配置界面

[图片](https://km.example.com/api/file/cdn/2759434466/234623203370?contentType=1)

## 技术方案

[https://km.example.com/collabpage/2759843442](https://km.example.com/collabpage/2759843442)

像一整个区块就是一个复杂组件

做修改需要去找到区块对应的组件名

在内容中可以看到渲染函数

[image.png](https://km.example.com/api/file/cdn/2759434466/234687552549?contentType=1&isNewContent=false)

去源码中搜索（页面上的源码负责逻辑处理）

[image.png](https://km.example.com/api/file/cdn/2759434466/234685654796?contentType=1&isNewContent=false)

接着就可以回到仓库中进行处理了

<span style="color:#383838">SetupMaterial 是一个大对象，内部包含了仓库中所有暴露的组件和方法，这次目标变动组件就是 ShelfABConfig</span>

<span style="color:#383838">由于页面是顶层，所以数据都是从页面传入组件中的</span>

<span style="color:#383838">测试时可以 talos 打包然后用这个包版本号</span>

<span style="color:#383838">但是推荐直接本地打包 </span>[https://km.example.com/collabpage/2716761191#b-0ea809f7f6ca4dd891d42f4c8eda7057](https://km.example.com/collabpage/2716761191#b-0ea809f7f6ca4dd891d42f4c8eda7057)

使用代理工具

```
// 代码块
npm install -g whistle
```

正常的话直接

```
// 代码块
w2 ca
```

即可，但是有时候会报错，也可以手动颁发证书

```
// 代码块
ceilf6@MBP-CKV4TN26M9-0045 ~ % rm -rf ~/.WhistleAppData/.whistle/certs
ceilf6@MBP-CKV4TN26M9-0045 ~ % w2 ca
Certificate installation failed: Error reading file /Users/ceilf6/.WhistleAppData/.whistle/certs/root.crt

ceilf6@MBP-CKV4TN26M9-0045 ~ % mkdir -p ~/.WhistleAppData/.whistle/certs
chmod -R 700 ~/.WhistleAppData/.whistle
ceilf6@MBP-CKV4TN26M9-0045 ~ % cd ~/.WhistleAppData/.whistle/certs

# 生成私钥
openssl genrsa -out root.key 2048

# 生成自签名证书
openssl req -x509 -new -nodes -key root.key -sha256 -days 3650 -out root.crt -subj "/CN=Whistle Root CA"
zsh: command not found: #
Generating RSA private key, 2048 bit long modulus
............................................................+++++
.....................+++++
e is 65537 (0x10001)
zsh: command not found: #
ceilf6@MBP-CKV4TN26M9-0045 certs % openssl genrsa -out root.key 2048
Generating RSA private key, 2048 bit long modulus
..................................+++++
...............................................+++++
e is 65537 (0x10001)
ceilf6@MBP-CKV4TN26M9-0045 certs % openssl req -x509 -new -nodes -key root.key -sha256 -days 3650 -out root.crt -subj "/CN=Whistle Root CA"
ceilf6@MBP-CKV4TN26M9-0045 certs % sudo security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain ~/.WhistleAppData/.whistle/certs/root.crt
ceilf6@MBP-CKV4TN26M9-0045 certs % w2 config set httpsCert ~/.WhistleAppData/.whistle/certs/root.crt
w2 config set httpsKey ~/.WhistleAppData/.whistle/certs/root.key
Type 'w2 help' for usage.
Type 'w2 help' for usage.
```

然后

```
// 代码块
w2 open
```

开启全局代理

```
// 代码块
w2 proxy --on
```

```
// 代码块
w2 start
```

本质就是去配置了 HTTPS, HTTP 代理

[图片](https://km.example.com/api/file/cdn/2759434466/237355721727?contentType=1&isNewContent=false)

接着书写代理规则

到目标页面中查找目标代理对象的请求地址

[https://assets-popcorn.meishi.test.example.com/test01/2836/shelf/edit/index.html?id=586](https://assets-popcorn.meishi.test.example.com/test01/2836/shelf/edit/index.html?id=586)

[image.png](https://km.example.com/api/file/cdn/2759434466/234758258659?contentType=1&isNewContent=false)

然后找到你想要代理其的文件地址

```
// 代码块
<https://s3.meituan.net/mnpm-cdn/@meishi-cross-detail-setup-material-0.5.21-beta.2/SetupMaterial.umd.js>  /Users/ceilf6/Desktop/workspace/cross-popcorn-materials/dist/SetupMaterial.umd.js
```

[image.png](https://km.example.com/api/file/cdn/2759434466/234757316474?contentType=1&isNewContent=false)

最后 Final URL 显示为最终代理地址就成功了

[image.png](https://km.example.com/api/file/cdn/2759434466/234757553074?contentType=1&isNewContent=false)

```
// 代码块
最新PRD描述：
一级筛选项的配置中：分组角标改为分组角标类型，选项为文本和动态文本（应该需要像“分组类型”一样 map => Radio）；增加一项名称为分组角标文本（应该是紧贴在{'分组角标：'}这个Item下面）：如果在分组角标类型中选择了文本，则和当前分组角标相同，手动输入，限制4个字以内，现在的提示词保持不变；如果选择动态文本，则在配置项中可以选择规则；分组角标类型和文本都是非必选项
```

这里的“协议”不是指“查询动态文案列表”的接口，而是指**保存到筛选项配置里的数据结构**。查询规则只是把可选规则拉回来；协议决定用户选中某个规则后，提交、回显、预览和 M 端渲染到底从哪个字段读它。

```
// 代码块
具体放到当前代码里是这样：

- **查询动态规则**：已有 `onDynamicTitleFilter/onDynamicTitleLoadMore`，会按 `RULE_TYPE.FILTER_ITEM_RULE` 拉可选规则列表。这个可以复用给“分组角标动态文本”的下拉选择。
- **保存配置协议**：当前只支持 `cornerLabel: { text: '角标' }`，所以只能保存手输文本。
- **为什么需要新协议**：如果角标选了“动态文本”，必须有地方保存选中的规则，比如 `ruleId/rule/desc`。否则编辑页刷新后无法回显，提交后后端/M 端也不知道要动态生成角标文案。

我推荐的协议是扩展现有 `cornerLabel`，例如：

```ts
cornerLabel?: {
  type?: 1 | 2; // 1 文本，2 动态文本
  text?: string;
  textGenerationRule?: {
    ruleId: number;
    rule: string;
    name?: string;
    desc?: string;
  } | null;
}
```

这样好处是：角标相关信息仍然收口在 `cornerLabel` 下，不会和现有分组动态文案使用的顶层 `textGenerationRule` 冲突。

如果后端已经定义了字段名，那就以服务端为准；如果没有，我会在方案里明确按“扩展 `cornerLabel`”作为前后端对齐点，并标注需要后端/M 端渲染同时支持。

```

动态文案可以理解成：**不是运营手输固定文案，而是选择一条规则，由后端/M 端在运行时根据商品、门店、用户等上下文算出最终展示文案**。

比如分组类型选择“动态文本”后，前端不是保存 text: "早餐" 这种固定值，而是保存一条规则：

<span style="color:rgb(152, 104, 1)">`textGenerationRule`</span>`: {   `<span style="color:rgb(152, 104, 1)">`ruleId`</span>`: `<span style="color:rgb(152, 104, 1)">`1473`</span>`,   `<span style="color:rgb(152, 104, 1)">`rule`</span>`: `<span style="color:rgb(80, 161, 79)">`"map(dealModel.mopMenus, ...)"`</span>`,   `<span style="color:rgb(152, 104, 1)">`name`</span>`: `<span style="color:rgb(80, 161, 79)">`"..."`</span>`,   `<span style="color:rgb(152, 104, 1)">`desc`</span>`: `<span style="color:rgb(80, 161, 79)">`"..."`</span>` }`

**当前分组类型如何处理动态文案**

1. 分组类型里有“动态文本”
在 lib/constants/index.ts (line 1) 里，TitleType.DYNAMIC_TEXT = 4，TITLE_TYPE_LIST 里 label 是“动态文本”。
2. 用户选择“动态文本”后，展示规则选择器
在 basic/index.tsx (line 111)，当 isShowDynamicText 为 true 时，会展示 formItemKey='dynamicText' 的 Select，用来搜索和选择规则。
3. 搜索规则时走已有回调
在 basic/hooks.ts (line 178)，handleDynamicTitleFilter 调用 onDynamicTitleFilter，参数是：

`{   `<span style="color:rgb(152, 104, 1)">`paging`</span>`: `<span style="color:rgb(152, 104, 1)">`DEFAULT_PAGING`</span>`,   `<span style="color:rgb(152, 104, 1)">`ruleType`</span>`: `<span style="color:rgb(152, 104, 1)">`RULE_TYPE`</span>`.FILTER_ITEM_RULE,   keyword,   `<span style="color:rgb(152, 104, 1)">`bizType`</span>`: `<span style="color:rgb(152, 104, 1)">`RULE_BIZ_TYPE`</span>` }`

也就是说，前端当前认为“分组动态文案”使用的是 FILTER_ITEM_RULE 类型的规则。

1. 选中规则后，前端保存到 textGenerationRule
在 basic/hooks.ts (line 153)，当 dynamicText 变化时，会把 value.originOption 写到 newData.textGenerationRule。
2. 提交时按动态文本协议输出
在 utils.ts (line 234)，当分组类型是动态文案时，最终提交的是：

<span style="color:rgb(152, 104, 1)">`textGenerationRule`</span>`: {   ruleId,   name,   rule,   desc }`

而不是普通 text。

1. **角标动态文本是否复用现有规则查询能力**
可以问：分组角标选择“动态文本”时，规则列表是否仍然使用当前分组动态文案的 RULE_TYPE.FILTER_ITEM_RULE + bizType=2？还是需要新的 ruleType / bizType？
2. **角标动态文本保存到哪个字段**
当前角标只有：

<span style="color:rgb(152, 104, 1)">`cornerLabel`</span>`: {   `<span style="color:rgb(152, 104, 1)">`text`</span>`: `<span style="color:rgb(193, 132, 1)">`string`</span>` }`

而顶层 textGenerationRule 已经被“分组类型=动态文本”占用了，所以不要再把角标规则也塞到顶层 textGenerationRule。

我建议和后端确认成这种结构：

<span style="color:rgb(152, 104, 1)">`cornerLabel`</span>`: {   `<span style="color:rgb(152, 104, 1)">`type`</span>`: `<span style="color:rgb(152, 104, 1)">`1`</span>`, `<span style="color:rgb(160, 161, 167)">`*// 1 文本，2 动态文本*`</span>`   `<span style="color:rgb(152, 104, 1)">`text`</span>`: `<span style="color:rgb(80, 161, 79)">`"角标"`</span>`,   `<span style="color:rgb(152, 104, 1)">`textGenerationRule`</span>`: `<span style="color:rgb(1, 132, 187)">`null`</span>` }`

动态文本时：

<span style="color:rgb(152, 104, 1)">`cornerLabel`</span>`: {   `<span style="color:rgb(152, 104, 1)">`type`</span>`: `<span style="color:rgb(152, 104, 1)">`2`</span>`,   `<span style="color:rgb(152, 104, 1)">`text`</span>`: `<span style="color:rgb(80, 161, 79)">`""`</span>`,   `<span style="color:rgb(152, 104, 1)">`textGenerationRule`</span>`: {     `<span style="color:rgb(152, 104, 1)">`ruleId`</span>`: `<span style="color:rgb(152, 104, 1)">`1473`</span>`,     `<span style="color:rgb(152, 104, 1)">`name`</span>`: `<span style="color:rgb(80, 161, 79)">`"..."`</span>`,     `<span style="color:rgb(152, 104, 1)">`rule`</span>`: `<span style="color:rgb(80, 161, 79)">`"..."`</span>`,     `<span style="color:rgb(152, 104, 1)">`desc`</span>`: `<span style="color:rgb(80, 161, 79)">`"..."`</span>`   } }`

1. **历史数据兼容**
现在线上已有 cornerLabel: { text: "角标" }。需要后端确认：老数据没有 type 时，是否默认按“文本”处理。

你可以直接这样问后端：

`本次分组角标要支持“文本/动态文本”两种类型。前端现有分组动态文案使用 onDynamicTitleFilter 查询规则，参数是 ruleType=FILTER_ITEM_RULE、bizType=2，选中后提交 textGenerationRule。  角标这里需要确认三点： 1. 角标动态文本的规则查询是否复用 FILTER_ITEM_RULE + bizType=2？ 2. 保存协议是否可以扩展到 cornerLabel 内部，例如 cornerLabel.type、cornerLabel.text、cornerLabel.textGenerationRule？ 3. 历史数据 cornerLabel: { text } 是否默认按 type=文本 兼容？  前端建议不要复用顶层 textGenerationRule，因为它已经表示“分组类型=动态文本”的展示文案规则，和角标规则语义不同。`

核心判断：**分组动态文案是筛选项主标题的动态生成规则；分组角标动态文案是角标的动态生成规则。两者可以复用查询接口，但保存字段最好分开。**

```TypeScript
// 代码块
Q - 新的PRD: 二级筛选项，团单配置中添加类型，该项必选，选项为手动配置或系统抓取；如果在类型中选择手动配置，二级配置项和线上相同，手动输入dealid，填写项中的提示文案不变；如果选择系统抓取，则在配置项中可以选择规则

# 二级筛选项团单配置类型改造

## Summary

本次改造目标是在二级筛选项的“团单配置”中展示必选的“类型”字段，支持“手动配置”和“系统抓取”。现有数据协议已包含 `recallType`、`deals`、`recallRule`，因此不新增接口字段，只调整二级表单展示规则和回归校验。

## Implementation Changes

- 在 [singleShelfConfig/hooks.ts](/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/singleShelfConfig/hooks.ts:491) 调整二级 `recallDealProps.blackList`：移除 `recallType`，保留 `dealTopReason`、`topDealId` 隐藏。
- 二级“类型”选项按 PRD 固定展示“手动配置 / 系统抓取”，建议二级场景使用全量 `RECALL_TYPE_LIST`，不再受 `ROLE_RECALL_TYPE_MAP[roleType]` 限制。
- 手动配置分支复用现有二级逻辑：仍展示“二级配置”，placeholder 保持“请选择一级筛选项里的 dealId，不同二级筛选项下不可重复选择”，不展示清空按钮和输入多个 dealId 的提示。
- 系统抓取分支复用现有规则选择逻辑：展示规则 Select，查询参数仍为 `RULE_TYPE.SUBJECT_RULE`、`subjectType='DEAL'`、`recallType=RecallType.SYSTEM`、`bizType=2`；提交仍落到 `recallRule`。
- 校验沿用现有规则：`recallType` 必填；手动配置校验 `dealIds`；系统抓取校验 `recallRuleId`。提交转换继续由 `convertRecallDeal` 按 `recallType` 输出 `deals` 或 `recallRule`。

## Test Plan

- 二级筛选项进入团单配置，确认“类型”展示且必选，选项包含“手动配置 / 系统抓取”。
- 选择“手动配置”，确认二级配置项、placeholder、可选 deal 范围与线上一致。
- 选择“系统抓取”，确认可搜索、加载更多、选择召回规则，提交结果包含 `recallType=SYSTEM` 和 `recallRule`。
- 校验切换场景：手动未选 deal 报错；系统未选规则报错；切回手动后按 deal 校验。
- 回归一级筛选项：一级团单配置展示和校验不受影响。
- 工程校验：执行 `pnpm lint`、`pnpm build`。

## Assumptions

- PRD 中“选项为手动配置或系统抓取”表示二级筛选项对所有角色都展示两个选项。
- 本次不改接口协议、不新增后端字段；后端已支持二级 `recallType` 和 `recallRule` 保存回显。
- 系统抓取下的“召回验证”沿用现有系统配置行为，不额外隐藏。

Q - “使用全量 RECALL_TYPE_LIST，不再受 ROLE_RECALL_TYPE_MAP[roleType] 限制” 那么你这不是改动了不应该角色分流相关的部分吗？

对，你这个质疑是对的。

“二级场景使用全量 `RECALL_TYPE_LIST`”本质上就是**绕开现有角色分流**，会让 BD 也看到“系统抓取”。如果 PRD 没明确说“二级筛选项不受角色权限限制”，那这个改法不稳。

更安全的实现应该是：

- 只把二级 `blackList` 里的 `recallType` 移除，让“类型”字段展示出来。
- `recallTypeList` 仍然使用现有 `ROLE_RECALL_TYPE_MAP[roleType]`。
- 这样 RD/PMOP 看到“手动配置 / 系统抓取”，BD 仍只看到“手动配置”。

也就是说，应该改为：

```ts
blackList: isSecondLevel ? ['dealTopReason', 'topDealId'] : [],
```

不改：

```tsx
const recallTypeList = ROLE_RECALL_TYPE_MAP[roleType];
```

如果产品明确要求“所有角色的二级筛选都必须有系统抓取”，那才需要单独加一个二级 override，并且在技术方案里标成**角色权限策略变更**。否则这次应该保持角色分流不变。

```

- 一级选“手动配置”：recallType === MANUAL，二级展示。
- 一级选“系统抓取”：recallType !== MANUAL，二级整体不展示。
- 二级选“系统抓取”：因为当前 selectedFilter 会变成二级筛选项，开关也跟着读二级的 recallType，于是二级列表也被整体关掉。

历史上这么写大概率是因为：二级手动配置依赖一级手动配置的 deal 列表，二级只能从一级已选 deal 里选子集；如果一级是系统抓取，前端没有显式 deal 列表，所以直接禁用了二级。

但新的 PRD 已经改变了前提：二级自身也可以选“系统抓取”。因此这个开关不能再简单依赖 selectedFilter.config.recallType === MANUAL，尤其不能读二级自己的 recallType。正确修复方向是把“是否展示二级筛选项”和“二级手动配置可选 deal 来源”拆开处理。

**团单配置**可以理解成：这个筛选项下面到底召回哪些商品/团单。

在这个组件里它有两种方式：

- **手动配置**：运营手动输入/选择 dealId。保存时走 `deals` 字段。
- **系统抓取**：运营选择一条召回规则，系统运行时按规则动态找 deal。保存时走 `recallRule` 字段。

也就是：

```TypeScript
// 代码块
// 手动配置
{
  recallType: MANUAL,
  deals: [{ id: 123 }]
}

// 系统抓取
{
  recallType: SYSTEM,
  recallRule: { ruleId, rule, name, desc }
}
```

**二级直接复用一级的“系统抓取”有没有问题？**

从前端表单和提交协议看，复用是没问题的：`recallDeal` 组件本来就已经同时支持手动配置和系统抓取，`convertRecallDeal` 也会按 `recallType` 输出 `deals` 或 `recallRule`。

但业务语义上有一个要确认的点：**二级筛选项和一级筛选项的商品集合关系**。

以前二级“手动配置”明显是从一级的 deal 里选子集，placeholder 都写着：

> 请选择一级筛选项里的 dealId，不同二级筛选项下不可重复选择
> 

所以旧逻辑默认二级依赖一级手动 deal 列表。

如果二级改为“系统抓取”，就会变成：

- 一级可能是手动配置，二级用规则抓取；
- 一级可能是系统抓取，二级也用规则抓取；
- 二级规则抓出来的商品，是否必须是一级商品集合的子集？

这是后端/业务侧要明确的关键。如果要求二级必须从属于一级，那么后端或 M 端需要在运行时做交集/约束；前端单纯选择一条规则无法保证它一定是一级规则的子集。

所以结论是：

- **技术复用没问题**：字段、表单、查询规则选择、提交转换都能复用。
- **业务关系需要确认**：二级系统抓取的结果是否需要被一级召回结果约束。
- **当前不能再用 一级 recallType === MANUAL 来决定二级是否展示**，否则二级选系统抓取会被隐藏，这和新 PRD 冲突。

你可以这样问后端/产品：

```
// 代码块
二级筛选项支持系统抓取后，二级规则召回的商品集合是否必须限定在一级筛选项召回结果内？

如果必须限定：
- 一级手动 + 二级系统抓取：二级规则结果是否需要和一级 deals 取交集？
- 一级系统抓取 + 二级系统抓取：二级规则结果是否需要和一级规则结果取交集？

如果不限定：
- 二级筛选项可以独立召回商品，前端仅提交二级 recallType 和 recallRule。
```

这件事不问清楚也能写前端，但上线后可能出现“二级筛选召回出了不属于一级筛选的商品”的业务问题。

已按 KM 接口文档更新完。核心变化是把之前临时拟定的 `cornerLabel.textGenerationRule` 改为正式协议 `cornerLabel.cornerLabelRule`，并保留旧临时字段的回显兼容，避免已有测试数据丢失。

改动点：

- [basic/hooks.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/filterForm/basic/hooks.ts)：回显读取 `cornerLabelRule || textGenerationRule`，提交只产出 `cornerLabelRule`；动态角标时 `text` 置空。
- [utils.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/utils.ts)：最终提交前统一规范化角标结构，最多两个角标统计同时识别新旧字段。
- [typing.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/filterForm/basic/typing.ts) / [index.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/typings/index.ts)：类型更新为正式 `cornerLabelRule`。
- [basic/index.tsx](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/filterForm/basic/index.tsx)：角标类型选项改为从 hooks 输出；明确非公共货架维度时不展示“动态文本”选项，符合文档里 `shelfActiveType` 的说明。

我用 citadel 读取了 [KM 2763330118](https://km.example.com/collabpage/2763330118) 及其子文档。验证已跑：

- `pnpm lint` 通过，仍有仓库既有的 `useAsyncSelect` hook 依赖 warning。
- `pnpm build` 通过，仍有既有的 Rollup/Sass warning。

```
// 代码块
[$karpathy-guidelines](/Users/ceilf6/.codex/skills/karpathy-guidelines/SKILL.md) 为什么要兼容之前你做的暂时字段？不应该是只需要保留接口文档的目标字段相关逻辑即可吗？
```

你判断是对的。之前我兼容 `cornerLabel.textGenerationRule` 的理由是“避免临时测试数据空回显”，但这不是接口文档要求，也不是稳定历史协议。按 `$karpathy-guidelines` 看，这是多余兼容，应该删除。

我已经改掉了：现在角标只保留文档目标字段 `cornerLabel.cornerLabelRule` 相关逻辑。

已调整：

- [basic/typing.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/filterForm/basic/typing.ts)：移除角标里的临时 `textGenerationRule`
- [basic/hooks.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/filterForm/basic/hooks.ts)：回显和表单转换只读写 `cornerLabelRule`
- [utils.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/components/shelfABConfig/utils.ts)：提交转换和“最多两个角标”统计只认 `cornerLabelRule`
- [typings/index.ts](https://app.notion.com/Users/ceilf6/Desktop/workspace/cross-popcorn-materials/lib/typings/index.ts)：接口类型只保留 `cornerLabelRule`

验证：

- `rg` 确认角标路径上不再有 `textGenerationRule` 临时兼容
- `pnpm lint` 通过，仍有既有 `useAsyncSelect` warning
- `pnpm build` 通过，仍有既有 Rollup/Sass warning

❯ 现在是否展示“动态文本”的逻辑是怎么样的？

⏺ 当前逻辑在 hooks.ts:127-131：

const cornerLabelTypeList = useMemo(
() =>
!shelfActiveType || shelfActiveType === ShelfActiveType.COMMON
? CORNER_LABEL_TYPE_LIST
: CORNER_LABEL_TYPE_LIST.filter((item) => item.value !== CornerLabelType.DYNAMIC_TEXT),
[shelfActiveType],
);

展示规则：

- shelfActiveType 未传入（undefined/null/0）→ 展示「文本」+「动态文本」
- shelfActiveType === 3（公共货架）→ 展示「文本」+「动态文本」
- 其他值（1=品牌、2=品类、4=门店、5=群体）→ 只展示「文本」，过滤掉「动态文本」

这和接口文档的要求一致：只有公共货架维度才支持动态角标，非公共维度前端直接隐藏选项，后端保存时也会校验兜底拒绝。

```
// 代码块
后端方案有变动：
属性type更名为cornerLabelType
CornerLabelVO取消属性cornerLabelRule
角标类别枚举即cornerLabelType改了其中一个 ruleTag 的type新增的一个枚举取消了
1. <https://km.example.com/collabpage/2762820983>
2. <https://km.example.com/collabpage/2762569763>

你读取最新文档内容、文档中评论后编写对当前代码进行更新的技术方案
```

```
// 代码块
# 分组角标协议变更适配方案

## Summary

- 按后端最新确认：`CornerLabelVO.type` 改为 `cornerLabelType`，移除 `cornerLabelRule`，角标类型枚举为 `STATIC_TEXT=1`、`RELATED_TO_FILTER=2`。
- 公共维度一级筛选项固定展示“分组角标类型”，默认选“静态文本”；选“静态文本”展示文本输入框，选“与筛选项关联”不展示输入框、不请求 RuleTag。
- 兼容历史空数据：`cornerLabel` 为 `null/undefined`、或 `{ cornerLabelType: 1, text: "" }` 都视为未配置角标，保存时不传 `cornerLabel`。

## Key Changes

- 更新协议类型与枚举：
  - `ICornerLabelVO` 改为 `{ text?: string; cornerLabelType?: CornerLabelType }`。
  - `CornerLabelType` 改为 `STATIC_TEXT = 1`、`RELATED_TO_FILTER = 2`。
  - 移除角标相关 `cornerLabelRule`、`cornerLabelRuleId`、`RULE_TYPE.CORNER_LABEL_RULE`。

- 更新表单行为：
  - 公共维度一级筛选项始终展示“分组角标类型”，默认值为 `STATIC_TEXT`。
  - `STATIC_TEXT` 时展示“分组角标文本”输入框，文本仍限制 4 字以内，提示文案保持不变。
  - `RELATED_TO_FILTER` 时不展示“分组角标文本/规则”配置项，不再调用 RuleTag 查询接口。
  - 非公共维度、非一级筛选项保持原作用范围，不新增入口。

- 更新回显与保存转换：
  - 回显优先读 `cornerLabel.cornerLabelType`，兼容旧 `cornerLabel.type`。
  - `cornerLabel` 为空时，表单默认显示 `STATIC_TEXT + 空文本`。
  - 保存时：
    - `STATIC_TEXT + 空文本`：不传 `cornerLabel`。
    - `STATIC_TEXT + 有文本`：传 `{ cornerLabelType: 1, text }`。
    - `RELATED_TO_FILTER`：传 `{ cornerLabelType: 2 }`；`text` 不传。
  - 旧数据 `{ type: 2, cornerLabelRule: ... }` 回显为“与筛选项关联”，保存为 `{ cornerLabelType: 2 }`。

## Test Plan

- 静态检查：
  - `rg "CORNER_LABEL_RULE|cornerLabelRule|cornerLabelRuleId|cornerLabel\\\\.type|DYNAMIC_TEXT" lib`，确认角标链路无旧协议残留；标题动态文案自身的 `DYNAMIC_TEXT` 除外。
  - `pnpm build`
  - `pnpm lint`
  - `pnpm test`
  - `git diff --check -- . ':!pnpm-lock.yaml'`

- 场景验证：
  - 历史 `cornerLabel: null/undefined`：默认显示“静态文本 + 空输入框”，保存不带 `cornerLabel`。
  - 历史 `{ text: "热卖" }` 或 `{ type: 1, text: "热卖" }`：回显静态文本，保存为 `{ cornerLabelType: 1, text: "热卖" }`。
  - 新建默认不输入角标文本：保存不带 `cornerLabel`。
  - 输入超过 4 字：前端校验拦截。
  - 选择“与筛选项关联”：不展示 RuleTag 下拉，不发 RuleTag 请求，保存 `{ cornerLabelType: 2 }`。
  - 最多两个角标校验：静态有文本算一个；`RELATED_TO_FILTER` 算一个；静态空文本不计数。

## Assumptions

- `cornerLabelType=2` 是完整配置，不需要额外规则 ID 或文本。
- 保存侧统一写新协议；读取侧临时兼容旧 `type/cornerLabelRule`，用于平滑历史/灰度数据。
- `{ cornerLabelType: 1, text: "" }` 与不传 `cornerLabel` 等价，前端选择不传，贴近当前数据库历史形态。
```
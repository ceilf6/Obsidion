# M端 Popcorn 总结文档

Source: [KM 2763015810](https://km.example.com/collabpage/2763015810)

❯ /karpathy-guidelines /pua 我需要你进行高强度 CR ，告诉我当前需求是否完全覆盖，是否有无关多余改动：PRD文档
[https://km.example.com/collabpage/2757014658](https://km.example.com/collabpage/2757014658) "一级筛选项的配置中

分组角标改为分组角标类型，选项为文本和动态文本

增加一项 名称为分组角标文本：如果在分组角标类型中选择了文本，则和线上相同，手动输入，限制4个字以内，现在的提示词保持不变；如果选择动态文本，则在
配置项中可以选择规则

分组角标类型和文本都是非必选项

点击二级筛选项后，团单配置中添加类型，该项必选，选项为手动配置或系统抓取

如果在类型中选择手动配置，二级配置项和线上相同，手动输入dealid，填写项中的提示文案不变；如果选择系统抓取，则在配置项中可以选择规则

以上改动只在公共维度的货架配置中生效"

接口文档在 [https://km.example.com/collabpage/2763330118](https://km.example.com/collabpage/2763330118) 下

⏺ Let me start by examining the current branch changes and the codebase structure.

⏺ Bash(git log --oneline -10)
⎿  5d1b7f0 fix: 分组角标 nowrap 改动点 影响范围
6d691e0 chore: 插件自动格式化 改动点 影响范围
664dac0 feat: 罗盘更新 改动点：一级罗盘角标支持动态文本 影响范围：二级支持系统抓取
… +7 lines (ctrl+o to expand)

⏺ Bash(git diff master...HEAD --stat)
⎿   lib/components/dragList/list.tsx                   |    9 +-
lib/components/groupedShelf/typing.ts              |   42 +-
.../filterForm/basic/cornerLabel/index.tsx         |   20 +-
… +11 lines (ctrl+o to expand)

⏺ Let me examine the actual code changes in detail.

⏺ Bash(git diff master...HEAD -- lib/ --stat)
⎿  diff --git a/lib/components/dragList/list.tsx b/lib/components/dragList/list.tsx
index 2951ca0..4ddd3df 100644
--- a/lib/components/dragList/list.tsx
… +211 lines (ctrl+o to expand)

⏺ Bash(git diff master...HEAD -- ':(exclude)pnpm-lock.yaml' --stat)
⎿  (No output)

⏺ Bash(git diff master...HEAD -- ':(exclude)pnpm-lock.yaml')
⎿  diff --git a/lib/components/dragList/list.tsx b/lib/components/dragList/list.tsx
index 2951ca0..4ddd3df 100644
--- a/lib/components/dragList/list.tsx
… +211 lines (ctrl+o to expand)

Read 1 file (ctrl+o to expand)

⏺ Now let me verify a few specific areas to complete the review.

Searched for 3 patterns (ctrl+o to expand)

⏺ Bash(git diff master...HEAD -- lib/ -- ':(exclude)pnpm-lock.yaml' | grep -n "CORNER_LABEL_RULE" )
⎿  237:+        ruleType: RULE_TYPE.CORNER_LABEL_RULE,
277:+        ruleType: RULE_TYPE.CORNER_LABEL_RULE,
863:+  CORNER_LABEL_RULE = 'CORNER_LABEL_RULE',

⏺ Bash(git diff master...HEAD -- lib/ -- ':(exclude)pnpm-lock.yaml' | grep -n "recallType")
⎿  641:-      blackList: isSecondLevel ? ['recallType', 'dealTopReason', 'topDealId'] : [],
645:+          : ['recallType', 'dealTopReason', 'topDealId']
888:   'deals' | 'recallRule' | 'recallType' | 'topDealId' | 'dealTopReason'

Searched for 3 patterns (ctrl+o to expand)

⏺ Bash(git diff master...HEAD -- lib/components/shelfABConfig/filterForm/basic/hooks.ts | grep -A2 -B2 "cornerLabel:")
⎿     backgroundColor: '',
textColor: '',
cornerLabel: undefined,
… +8 lines (ctrl+o to expand)

⏺ Now I have a complete picture. Here's my CR report.

---

CR Report: 需求覆盖度 & 无关改动审计

一、需求覆盖度检查

PRD 需求1: 一级筛选项 — 分组角标类型（文本/动态文本）

- 状态: 完全覆盖 ✅
- CornerLabelType 枚举、CORNER_LABEL_TYPE_LIST 常量、Radio.Group 渲染均正确实现

PRD 需求2: 一级筛选项 — 分组角标文本（静态输入 / 动态规则选择）

- 状态: 完全覆盖 ✅
- 静态文本: 复用原 CornerLabelFormItem 组件，加 valueMode='text'，4字限制 validator 保留
- 动态文本: MTD.Select + 规则筛选 + 滚动加载更多，逻辑完整
- 提示文案 "仅可配置两个分组角标，限制填写四字以内" 保持不变 ✅

PRD 需求3: 分组角标类型和文本都是非必选项

- 状态: 完全覆盖 ✅
- validator.ts 中 cornerLabelType: []、cornerLabelRuleId: [] 均无必填校验

PRD 需求4: 二级筛选项 — 团单配置中添加类型（手动配置/系统抓取）

- 状态: 完全覆盖 ✅
- singleShelfConfig/hooks.ts:493 判断 isPublicSecondLevel，从 blackList 移除 recallType，使该字段在公共维度二级筛选项可见

PRD 需求5: 以上改动只在公共维度的货架配置中生效

- 状态: 完全覆盖 ✅
- 一级: isPublicFirstLevel = shelfActiveType === ShelfActiveType.COMMON && filterLevel === FilterLevel.FIRST 控制 UI 分支渲染
- 二级: isPublicSecondLevel = shelfActiveType === ShelfActiveType.COMMON && isSecondLevel 控制 blackList 差异
- 非公共维度保持原有逻辑不变

---

二、无关多余改动

以下改动属于 插件自动格式化，与需求无关（commit 6d691e0 标注为 chore: 插件自动格式化）：

┌─────────────────────────┬───────────────────────────────────────────────────────────┐
│          文件           │                         变更内容                          │
├─────────────────────────┼───────────────────────────────────────────────────────────┤
│ dragList/list.tsx       │ interface IDataItem extends Pick<...> 缩进调整，纯格式    │
├─────────────────────────┼───────────────────────────────────────────────────────────┤
│ groupedShelf/typing.ts  │ IMenuListProps、IProps 接口缩进调整，纯格式               │
├─────────────────────────┼───────────────────────────────────────────────────────────┤
│ shelfABConfig/typing.ts │ IFilterListItem、IShelfABConfigProps 接口缩进调整，纯格式 │
└─────────────────────────┴───────────────────────────────────────────────────────────┘

这些格式化改动 没有功能性影响，但增加了 diff 噪音。建议后续 PR 中将格式化提交与功能提交分离。

pnpm-lock.yaml: 9449 行变更，无法确认是否与本需求相关（可能是依赖安装导致的 lockfile 波动）。建议确认是否有意新增/变更了依赖。

---

三、潜在风险点

1. cornerLabel.text 可选化可能影响非公共维度

原类型 cornerLabel?: { text: string } 改为 cornerLabel?: ICornerLabelVO（其中 text 是 string | undefined）。在 utils.ts:435
的非公共维度校验中：

// 改前
if (cornerLabel && cornerLabel.text?.length > 4)

// 改后
if (cornerLabel?.text && cornerLabel.text.length > 4)

逻辑上等价，但需确认非公共维度下 cornerLabel 是否仍保证有 text 字段。如果非公共维度也有可能传入无 text 的 cornerLabel 对象（比如 {type:
1}），校验会被跳过，可能导致静默通过。

1. convertCornerLabelFormData 在未选择类型时返回 undefined

// 改后
if (cornerLabel?.text && cornerLabel.text.length > 4)

逻辑上等价，但需确认非公共维度下 cornerLabel 是否仍保证有 text 字段。如果非公共维度也有可能传入无 text 的 cornerLabel 对象（比如 {type:
1}），校验会被跳过，可能导致静默通过。

1. convertCornerLabelFormData 在未选择类型时返回 undefined

当 isPublicFirstLevel && cornerLabelType === undefined 时，函数返回 undefined。这意味着如果用户在公共维度一级筛选项从未碰过角标配置，原有的
cornerLabel 数据会被清空。需要确认这是否符合预期——PRD 说"非必选项"，但是否允许"不选类型时保留原值"？

1. CORNER_LABEL_RULE 枚举值是否与后端约定一致

constants/index.ts 新增 CORNER_LABEL_RULE = 'CORNER_LABEL_RULE'，用于请求规则列表时的 ruleType 参数。需确认接口文档中后端确实接受此枚举值。

---

四、总结

┌──────────┬─────────────────────────────────┐
│   维度   │              结论               │
├──────────┼─────────────────────────────────┤
│ 需求覆盖 │ 5/5 完全覆盖，无遗漏            │
├──────────┼─────────────────────────────────┤
│ 无关改动 │ 3个文件的纯格式化改动，建议分离 │
├──────────┼─────────────────────────────────┤
│ 风险点   │ 3处需确认（见上方）             │
└──────────┴─────────────────────────────────┘

整体实现质量不错，逻辑分支清晰，isPublicFirstLevel / isPublicSecondLevel 的守卫条件正确覆盖了 PRD 的生效范围约束。

✻ Sautéed for 1m 6s

❯ 关于风险点，通过用 skill 读取接口文档进行确认

⏺ Let me try to fetch the interface documentation.

⏺ Fetch([https://km.example.com/collabpage/2763330118](https://km.example.com/collabpage/2763330118))
⎿  Interrupted · What should Claude do instead?

❯ 不应该用 fetch ，而是得用 citadel 这个 skill

⏺ Skill(citadel)
⎿  Successfully loaded skill

⏺ Bash(node -e "const cp=require('child_process'); try{cp.execSync('command -v oa-skills',{stdio:'ignore',shell:true});
console.log('OK')}catch{console.log('MISSING')}…)
⎿  OK

⏺ Bash(oa-skills citadel getMarkdown --contentId 2763330118 2>&1)
⎿  Error: Exit code 1

```
// 代码块
 ⚠️   当前认证策略: sso-ciba
    SSO CIBA 大象授权需要用户 MIS 信息来完成认证。

 💡 请使用以下方式之一提供 MIS 信息：
    1. 通过 --mis 参数指定: citadel getMarkdown --mis <YOUR_MIS>
    2. 在 ~/.config/clawdgw.json 配置文件中设置 defaultUserId
    3. 设置环境变量: export SSO_USER_ID=<YOUR_MIS>
    4. 或直接提供 --sso-ciba-token / SSO_CIBA_TOKEN 跳过交互式授权
 … +2 lines (ctrl+o to expand)
```

Read 1 file (ctrl+o to expand)

## ⏺ Bash(git config [user.name](http://user.name/) 2>/dev/null; echo "---"; echo $SSO_USER_ID 2>/dev/null)
⎿ ceilf6

⏺ Bash(oa-skills citadel getMarkdown --contentId 2763330118 --mis ceilf6 2>&1)
⎿  Interrupted · What should Claude do instead?

❯ 我的mis号为wangjinghong02

⏺ Bash(oa-skills citadel getMarkdown --contentId 2763330118 --mis wangjinghong02 2>&1)
⎿  文档标题：《接口文档》
文档ID：2763330118
内容长度：1024 字符
… +28 lines (ctrl+o to expand)
⎿  (timeout 30s)

⏺ 接口文档是一个索引页，包含4个子文档链接。让我逐一读取。

⏺ Bash(oa-skills citadel getMarkdown --contentId 2763330156 --mis wangjinghong02 2>&1)
⎿  文档标题：《C端查询货架配置信息接口文档（草案）》
文档ID：2763330156
内容长度：32218 字符
… +209 lines (ctrl+o to expand)
⎿  (timeout 30s)

⏺ Bash(oa-skills citadel getMarkdown --contentId 2762569763 --mis wangjinghong02 2>&1)
⎿  文档标题：《M端保存货架详情接口文档（草案）》
文档ID：2762569763
内容长度：33916 字符
… +208 lines (ctrl+o to expand)
⎿  (timeout 30s)

⏺ Bash(oa-skills citadel getMarkdown --contentId 2763380091 --mis wangjinghong02 2>&1)
⎿  文档标题：《M端查询货架详情接口文档（草案）》
文档ID：2763380091
内容长度：33357 字符
… +208 lines (ctrl+o to expand)
⎿  (timeout 30s)

⏺ Bash(oa-skills citadel getMarkdown --contentId 2762820983 --mis wangjinghong02 2>&1)
⎿  文档标题：《RuleTag 下拉列表查询接口文档（草案）》
文档ID：2762820983
内容长度：25927 字符
… +199 lines (ctrl+o to expand)
⎿  (timeout 30s)

⏺ Let me read the two larger docs for cornerLabel structure details.

Read 2 files, searched memories (ctrl+o to expand)

⏺ 接口文档已读取完毕。以下是三个风险点的确认结果：

---

风险点确认结果

风险点1: CORNER_LABEL_RULE 枚举值 — 已确认正确 ✅

接口文档 2762820983 中 RuleTypeEnum 明确定义：

┌───────────────────┬──────┬──────────────────────────┐
│      枚举值       │ 数值 │           描述           │
├───────────────────┼──────┼──────────────────────────┤
│ SUBJECT_RULE      │ 1    │ 主体筛选规则             │
├───────────────────┼──────┼──────────────────────────┤
│ FILTER_ITEM_RULE  │ 2    │ 筛选项适用规则           │
├───────────────────┼──────┼──────────────────────────┤
│ CORNER_LABEL_RULE │ 3    │ 分组角标规则（本次新增） │
└───────────────────┴──────┴──────────────────────────┘

代码中 RULE_TYPE.CORNER_LABEL_RULE = 'CORNER_LABEL_RULE' 与接口入参 ruleType 完全一致。bizType: 2 也与文档"固定传 2"吻合。

风险点2: cornerLabel 字段结构 — 已确认正确 ✅

接口文档中 CornerLabelVO 定义：

┌─────────────────┬─────────┬──────┬────────────────────────────────────────────────────────────────────────────┐
│      参数       │  类型   │ 必填 │                                    说明                                    │
├─────────────────┼─────────┼──────┼────────────────────────────────────────────────────────────────────────────┤
│ text            │ String  │ 否   │ 静态文本时传运营输入的文本；动态文本时不传                                 │
├─────────────────┼─────────┼──────┼────────────────────────────────────────────────────────────────────────────┤
│ type            │ Integer │ 否   │ STATIC_TEXT(1) / DYNAMIC_TEXT(2)；null 等同于 STATIC_TEXT(1)，兼容历史数据 │
├─────────────────┼─────────┼──────┼────────────────────────────────────────────────────────────────────────────┤
│ cornerLabelRule │ RuleVO  │ 否   │ type=DYNAMIC_TEXT(2) 时有值                                                │
└─────────────────┴─────────┴──────┴────────────────────────────────────────────────────────────────────────────┘

代码中 ICornerLabelVO 定义：
export interface ICornerLabelVO {
text?: string;
type?: CornerLabelType;
cornerLabelRule?: IRuleVO | null;
}
与接口文档完全对齐。 normalizeCornerLabel 中 cornerLabel.text ? { text: cornerLabel.text } : undefined（无 type
的兜底分支）也兼容了历史数据格式。

风险点3: 未选择类型时 cornerLabel 被清空 — 属于预期行为 ✅

保存接口文档明确说明：后端会校验非公共维度货架拒绝保存含动态文本角标或系统抓取团单的配置。而公共维度场景下：

- 用户从未选择 type → convertCornerLabelFormData 返回 undefined → 不下发角标配置 → 符合"非必选项"语义
- 用户选择 type=1 但未输入文本 → 返回 { type: 1 }（无 text）→ 后端收到 type=1 无 text，等同无角标
- 用户选择 type=2 但未选规则 → 返回 { type: 2 }（无 cornerLabelRule）→ 后端收到

这与 PRD"非必选项"一致：不选 = 不配置角标，不存在"保留历史值"的需求。

---

额外发现：保存接口的后端校验

接口文档 2762569763 中提到后端新增了校验逻辑：

▎ 当货架为非公共维度（shelfActiveType ≠ 3）时，拒绝保存含以下配置的筛选项：
▎ 1. cornerLabel.type == DYNAMIC_TEXT(2)
▎ 2. 父筛选项 materialType == EXECUTABLE_TEXT(4)（系统抓取团单）

这意味着前端目前 没有在保存前做对应的前端校验提示，而是依赖后端兜底。如果非公共维度用户通过某些途径配置了动态文本角标，保存时后端会拒绝并返回
"当前货架维度不支持该筛选项配置，请调整后重新保存"。前端通过 isPublicFirstLevel / isPublicSecondLevel 的 UI
守卫已经从源头阻止了这种配置，所以实际不会触发后端校验，这是一个合理的双层防护设计。

---

总结：三个风险点均无问题，接口文档与代码实现完全对齐。
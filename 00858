# 嵌入模型输出结果

校验过了，当前 embedding 输出是健康的。

在缓存里：

- [embeddings.json](https://www.notion.so/Users/a86198/Desktop/Lab/AI/3-Application/FrontAgent-app/.frontagent/rag-cache/embeddings.json)
- [index.json](https://www.notion.so/Users/a86198/Desktop/Lab/AI/3-Application/FrontAgent-app/.frontagent/rag-cache/index.json)

我核对到的结果是：

- `model=text-embedding-3-large`
- `baseURL=https://yunwu.ai/v1/embeddings`
- `dimensions=512`
- 向量数 `3888`
- 索引 chunk 数 `3888`
- `3888/3888` 全量落盘，没有缺块
- 所有抽样向量维度都一致是 `512`
- 没有空向量、`NaN`、`Infinity`
- 向量范数接近 `1`，说明归一化正常

你这次显示出来的 5 个命中文档，我也检查了，全部 chunk 都已经有 embedding，不是“文档命中了但没语义向量”。

我还做了一个语义邻近性 sanity check：拿 `React/sandboxs/src/components/Form/formContext.js` 的向量去找最近邻，返回的主要是：

- `React/sandboxs/src/components/Form/index.js`
- `React/sandboxs/src/test/Context/NewContext.js`
- `React/sandboxs/src/components/Form/FormInput.js`
- `React/sandboxs/src/components/Form/FormButton.js`

这说明向量空间本身是合理的，不是乱 embedding。

一个样本向量也正常，我看了：

- `chunk:React/sandboxs/src/components/Form/formContext.js:0`
- 前几维是正常浮点数，维度 `512`

需要说明的一点是：我这边当前执行环境里拿不到你的 `API_KEY/BASE_URL`，所以没法在本地复放“查询向量”那一步，因而没法直接把这次 `解释 React createElement` 的 `semanticScore/keywordScore` 明细打印出来。

但就“文档向量是否正确生成并可用”这件事，现在已经可以确认是成功的。

结论：

- embedding 已成功生成并完整落盘
- hybrid 现在不是假成功，语义向量链路是通的
- 当前剩下的问题如果有，更可能是“排序质量优化”，不是“嵌入模型没工作”

如果你要，我下一步可以直接把 CLI 改成把每条 RAG 结果的 `keywordScore` 和 `semanticScore` 一起打印出来，后面调效果会方便很多。
# this丢失问题

```jsx
import React from "react";

export default class This extends React.Component {
    childFunc = () => {
        console.log("===childFunc", this)
    }

    fatherFunc() {
        console.log("===fatherFunc", this)
        console.log("===this.childFunc", this.childFunc)
    }

    render() {
        return (
            <button onClick={this.fatherFunc}>
                Click
            </button>
        )
    }
}
```

React 只会将 this.fatherFunc 这个函数作为**回调函数**保存，**并不会管你是 this. 还是 that.**

```jsx
const handler = this.fatherFunc
然后调用时 handler(event)
```

而不是 this.fatherFunc(event)

并且 普通函数的 this 取决于调用方式

[](this%20指向%203000f8d8d1fd80789f87f53f74e74a58.md) 

所以在 fatherFunc 内部 this 是 **undefined**

得将 fatherFunc 弄成箭头函数
# Harness工程

落地

Harness Engineering 是为 AI 智能体构件一整套运行环境、约束规则和反馈闭环的工程实践，让模型从“能回答”到“可持续的、可靠地工作“

harness 规定格式

例如返回不是 JSON 格式时再去请求（处理逻辑思想）

**对输出校验** 是否符合预期

context 上下文：session，背景

[str1, str2]

prompt：分割的

str
# 3 命令行

# 语法

```tsx
命令名 [选项] [参数]
```

命令行由一个或多个空白字符分隔的若干「单词」组成。这里的「空白字符（blanc）」指水平制表符（tab）或空格字符

- 选项（option）通常以单个连字符开头（例如：-a），或按语法规则以两个连续的连字符开头（例如：--version）。
- 参数（arguments）指命令需要执行操作的目标对象。

多个命令用 ; 进行分隔
# 渲染原理

- 什么是渲染

像框架中的render是得到一个虚拟DOM

浏览器中的渲染是 **HTML字符串 → 像素颜色信息**（交给GPU

- 渲染时间点

当你输入URL回车后发生了什么

网络 …

然后渲染主线程拿到 HTML（包含CSS,JS）后包装成**渲染任务**放到消息队列中

![截屏2025-12-21 22.35.37.png](截屏2025-12-21_22.35.37.png)

![截屏2025-12-22 00.46.13.png](截屏2025-12-22_00.46.13.png)

![截屏2025-12-22 11.15.12.png](截屏2025-12-22_11.15.12.png)

![截屏2025-12-22 21.54.35.png](截屏2025-12-22_21.54.35.png)

![截屏2025-12-22 22.14.45.png](截屏2025-12-22_22.14.45.png)

![截屏2025-12-22 22.19.54.png](截屏2025-12-22_22.19.54.png)

![截屏2025-12-22 22.21.35.png](截屏2025-12-22_22.21.35.png)

![截屏2025-12-22 22.26.36.png](截屏2025-12-22_22.26.36.png)

![截屏2025-12-22 22.32.03.png](截屏2025-12-22_22.32.03.png)

![截屏2025-12-21 22.33.03.png](截屏2025-12-21_22.33.03.png)

![截屏2025-12-21 22.36.53.png](截屏2025-12-21_22.36.53.png)

![截屏2025-12-22 22.38.26.png](截屏2025-12-22_22.38.26.png)

# 浏览器是如何渲染页面的？

当浏览器的网络线程收到 HTML 文档后，会产生一个渲染任务，并将其传递给渲染主线程的消息队列。

在事件循环机制的作用下，渲染主线程取出消息队列的渲染任务，并开启**渲染流程**。

整个渲染流程分为多个阶段，分别是 HTML解析、样式计算、布局、分层、绘制、分块、光栅化、画

每个阶段都有明确的输入输出，上一个阶段的输出会成为下一个阶段的输入

这样，整个渲染流程就形成了一套组织严密的生产流水线

- 渲染第一步是**解析 HTML**
解析过程中遇到 CSS 解析 CSS ，遇到 JS 执行 JS 。为提高解析效率，浏览器在开始解析前，会启动一个**预解析的线程**，率先**下载** HTML 中的**外部 CSS** 文件和 **外部的 JS** 文件
如果渲染主线程解析到 **link** 位置，此时外部的 CSS 文件还没有下载、解析好，主线程**不会等待**，继续解析后续的 HTML 。这是因为 **下载和解析 CSS 的工作是在预解析线程中进行的**。这就是 **CSS 不会阻塞 HTML** 解析的根本原因
如果主线程解析到 **script** 位置，会停止解析 HTML ，等待预解析线程将 JS 文件下载好，并将全局代码解析执行完后才能继续解析 HTML 。这是**因为 JS 代码的执行过程可能会修改当前的 DOM 树**，所以 DOM 树的生成必须暂停，这就是 **JS 会阻塞 HTML 解析**的根本原因
在第一步完成后，会得到 DOM 和 CSSOM 树，浏览器的默认样式、内部样式、外部样式、内联样式均会包含在 CSSOM 树中
- 下一步是**样式计算**
主线程会遍历得到的 DOM 树，依次为树中的每个节点计算出它最终的样式，称之为 Computed Style 。在这一过程中，很多**预设值**会变成**绝对值**，例如 **red 会变成 rgb(255,0,0）**；**相对单位**会变成**绝对单位**，比如 **em 会变成 px**
这一步完成后，会得到一颗带**有样式的 DOM 树**进入下一步
- 接下来是**布局**，布局完成后会得到布局树
布局阶段会一次遍历 DOM 树的每一个节点，计算每个节点的几何信息，例如节点的宽高、相对包含块的位置
大部分时候，DOM树和布局树并非一一对应
例如 **display: none 的节点没有几何信息，因此不会生成到布局树中**；又比如使用了**伪元素选择器**，虽然 DOM 树中不存在这些伪元素节点，但是**他们拥有几何信息，所以会生成到布局树中**。还有匿名行盒、匿名块盒等等都会导致 DOM 树和布局树无法一一对应
- 下一步是**分层**
主线程会使用一套复杂的策略对整个布局树中进行分层
分层的好处在于，将来某一个层改变后，仅会对该层进行后续处理，从而提升效率
滚动条、堆叠上下文、transform、opacity 等样式都会或多或少的影响分层结果，也可以通过 will-change 属性更大程度的影响分层结果
- 再下一步是**绘制**
主线程会为每个层单独产生绘制指令集，用于描述这一层的内容该如何画出来
- 完成绘制后，主线程将每个图层的绘制信息提**交给合成线程**，剩余工作将由合成线程完成
合成线程首先对每个图层进行**分块**，将其划分为更多的小区域
它会从线程池中拿取多个线程来完成分块工作
- 分块之后进入**光栅化**
合成线程会将信息交给 GPU 进程，以极高的速度完成光栅化
GPU进程优先处理靠近视口区域的块
光栅化的结果就是一块一块的位图
- 最后进入**画**
合成线程拿到每个层、每个块的位图后，生成一个个 quad 指引信息
指引会标识出每个位图应该画到屏幕的哪个位置，以及会考虑到旋转、缩放等变形
**变形发生在合成线程，与渲染主线程无关，这就是 transform 效率高的本质原因**
合成线程会把 quad 提交给 GPU 进程，由 GPU 进程产生系统调用，提交给 GPU 硬件，完成最终的屏幕成像

（前五个是在渲染主线程，后三个是在合成线程，他们都在渲染进程中）

# 1. HTML解析

- 将HTML源文件**字符串**转换为DOM树 document object model（标签对应一个对象）
- CSSOM（CSS Object Model）树的根节点styleSheetlist连接着页面所有的样式表
a. <style> 内部样式表
b. <link> 外部样式表（ document.styleSheets[0].addRule(’div’, ‘border: 2px‘) ）
c. <div style=” “> 内联样式表（ dom.style ）
d. 浏览器默认样式表
然后根据样式表中的规则对应生成对象 { 选择器，{ 样式键值对 } }

![截屏2025-12-21 22.39.26.png](截屏2025-12-21_22.39.26.png)

![截屏2025-12-21 22.42.56.png](截屏2025-12-21_22.42.56.png)

![截屏2025-12-21 22.43.59.png](截屏2025-12-21_22.43.59.png)

## HTML 解析过程中遇到 CSS 代码怎么办

预解析线程快速浏览一遍，发现 CSS 去下载，然后预解析，后面渲染主线程走到 CSS 时候很快就能合并生成 CSSOM

所以 CSS 并不会阻塞 HTML 解析、DOM生成

![截屏2025-12-22 00.30.57.png](截屏2025-12-22_00.30.57.png)

## HTML 解析过程中遇到 JS 代码会怎样

预解析线程可以分担下载 JS 的任务

渲染主线程遇到 JS 时必须暂停一切，启动 V8 引擎去运行 JS 结束后才能往下，这是**因为 JS 中可能会有对 DOM 和 CSSOM 的操作**

![截屏2025-12-22 00.41.09.png](84f9914a-7ca2-4561-ae96-8bcea65c99bc.png)

# 2. 样式计算

在第一步得到 DOM 和 CSSOM 树后还需要知道每个节点受哪些样式影响，然后得到计算后的样式（ 开发者工具 样式 - Computed ）

![截屏2025-12-22 11.03.04.png](截屏2025-12-22_11.03.04.png)

- CSS属性值的计算过程
    - 层叠：优先级
    - 继承
- 视觉格式化模型
    - 盒模型
    - 包含块
    - BFC

# 3. 布局 Layout

视觉格式化模型的规则

得到的位置是相对**包含块**的

![截屏2025-12-22 11.18.55.png](截屏2025-12-22_11.18.55.png)

![截屏2025-12-22 12.36.23.png](截屏2025-12-22_12.36.23.png)

得到的是 Layout 树是需要展示的、包含盒子几何信息的，不一定对应 DOM 

所以 display: none 元素不会在 Layout 中

![截屏2025-12-22 12.37.02.png](c0219e01-a5cd-4e3c-8dca-a00500687e5d.png)

内容必须在**行盒**中

行盒和块盒不能相邻

![截屏2025-12-22 12.38.37.png](截屏2025-12-22_12.38.37.png)

Layout树在引擎C++中，只会暴露一些属性给JS，其他访问不到

document.body.clientWidth

# 4. 分层 Layer

用户操作会导致页面发生变化，如果一变化就所以重新画就开销太大了

将页面分层次，改动只会影响该层次

谷歌开发者工具 - 更多工具 - Layers

与堆叠上下文有关的CSS 属性会影响分层，例如 z-index ，opacity透明度，transform几何变换（动画），**will-change 告诉浏览器这个部分会变化最好单独一层（这样重绘的部分就缩小了**

![截屏2025-12-22 21.55.05.png](截屏2025-12-22_21.55.05.png)

# 5. 绘制 Paint

![截屏2025-12-22 22.10.48.png](截屏2025-12-22_22.10.48.png)

每一层生成如何绘制的指令

![截屏2025-12-22 22.11.41.png](截屏2025-12-22_22.11.41.png)

## **Canvas 用的就是浏览器内核的绘制功能 - 绘制指令**

![截屏2025-12-22 22.12.54.png](截屏2025-12-22_22.12.54.png)

# 6. 分块 Tile

![截屏2025-12-22 22.15.42.png](截屏2025-12-22_22.15.42.png)

越靠近视口的区域越先渲染

![截屏2025-12-22 22.17.32.png](截屏2025-12-22_22.17.32.png)

合成线程也是渲染进程的（活动监视器

![截屏2025-12-22 22.19.24.png](截屏2025-12-22_22.19.24.png)

# 7. 光栅化 Raster

光栅化是将每个块变成位图（内存中二维数组，存储每个像素颜色信息
优先处理靠近视口的块

![截屏2025-12-22 22.23.24.png](截屏2025-12-22_22.23.24.png)

![截屏2025-12-22 22.24.38.png](截屏2025-12-22_22.24.38.png)

合成线程在光栅化的时候会交给 GPU进程

# 8. 画 - 捉quad

quad: 指引信息 - 位图相对屏幕位置在哪里

合成线程在渲染进程中，经过浏览器GPU进程交给系统的显卡，这是因为**渲染进程是在沙盒中的**

transform 变化（数学运算符

![截屏2025-12-22 22.28.34.png](截屏2025-12-22_22.28.34.png)

![截屏2025-12-22 22.50.27.png](截屏2025-12-22_22.50.27.png)

# 什么是 reflow 重排?

reflow 的本质就是重新计算 layout 树。
当进行了会**影响布局树的操作后，需要重新计算布局树**，会引发 layout。
为了**避免连续的多次操作导致布局树反复计算**，浏览器会**合并**这些操作，当 JS代码全部完成后再进行统一计算。所以，改动属性造成的reflow 是**异步**完成的。（会加到消息队列，然后到时候主线程统一拿任务）
也同样因为如此，当 JS 获取布局属性时，就可能造成无法获取到**最新的布局信息**。(requestAnimation)
浏览器在反复权衡下，最终决定**获取属性立即 reflow （同步）**

当修改和几何尺寸、几何信息相关的

width → CSSOM

还有例如创建元素 → DOM

从 style 开始

![截屏2025-12-22 22.47.03.png](截屏2025-12-22_22.47.03.png)

# 什么是 repaint 重绘？

repaint 的本质就是**重新根据分层信息计算绘制指令**

当改动了**可见样式**后，就需要重新计算，引发 repaint

由于元素的布局信息也属于可见样式，所以 reflow 一定会引起 repaint

![截屏2025-12-22 22.55.23.png](截屏2025-12-22_22.55.23.png)

# 为什么 transform 效率高？

在合成线程中，不受主线程阻塞影响

滚动条同理

![截屏2025-12-22 22.57.31.png](截屏2025-12-22_22.57.31.png)
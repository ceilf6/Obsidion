# 类型

# 1. 基础类型

对应JS的原始类型

string

number

boolean

bigint

symbol

null

undefined

# 2. 对象类型

Object

Array

Tuple

Function

# 3. 特殊类型

any 放弃类型检查

unknown 更安全的any 在使用前必须进行类型判断

表示类型声明时未知，在使用前得做收缩

像 API/请求 返回值

```tsx
async function fetchUser(): Promise<unknown> {
  const res = await fetch("/api/user")
  return res.json()
}

const data = await fetchUser()
if (typeof data === "object" && data !== null) {
  console.log(data)
}
```

JSON.parse() 返回的数据在编译期无法确定类型

```tsx
const data: unknown = JSON.parse(json)

function parseJSON(json: string): unknown {
  return JSON.parse(json)
}
```

void 没有返回值

never 永远不会返回

像抛异常、死循环，类型穷尽检查

# 联合类型 union

 | 多个类型之一

```jsx
let id: number | string
```

# 交叉类型 intersection

 & 多个类型同时满足

```jsx
type A = { name: string }
type B = { age: number }

type C = A & B

let obj: C = {
  name: "Tom",
  age: 18
}
```

# 字面量类型

值本身就是类型

```jsx
let status: 200 | 404 | 500
```

# 枚举 enum

```jsx
enum Direction {
  Up,
  Down,
  Left,
  Right
}
```

# 类型别名 type

```jsx
type User = {
  name: string
  age: number
}
```

# 接口 interface

描述对象结构

```jsx
interface User {
  name: string
  age: number
}
```

## enum, type, interface区别

1. **enum 有运行时**、在编译后的 JS 中仍然存在，但是其他两个没有
    
    ```tsx
    enum Direction {
      Up,
      Down,
      Left,
      Right
    }
    
    =>
    
    var Direction;
    (function (Direction) {
      Direction[Direction["Up"] = 0] = "Up";
      Direction[Direction["Down"] = 1] = "Down";
    })(Direction || (Direction = {}));
    ```
    
    这也导致了现在前端很少用enum，因为其会使得**打包**结果JS变大，不适合树摇 - 构建工具看到的是一个 立即执行函数IIFE。由于函数内部修改了外部变量 `Direction`，工具很难百分之百确定这个函数在“没被调用”的情况下是否可以安全删除。为了保险起见，打包工具通常会选择**保留它**
    
2. type 可以描述任意类型，而 interface 主要用于描述对象结构
3. **interface 自动合并**
    
    ```tsx
    interface User {
      name: string
    }
    interface User {
      age: number
    }
    
    =>
    
    interface User {
      name: string
      age: number
    }
    ```
    
    type 不支持自动合并，必须得用 交叉类型
    
    ```tsx
    type User = {
    	name: string
    } & {
    	age: number
    }
    ```
    

# 泛型 generics

用于 类型参数化

```jsx
function identity<T>(arg: T): T {
  return arg
}
```

# 常见方法

## keyof

获取对象key

```jsx
type User = {
  name: string
  age: number
}

type Keys = keyof User
// "name" | "age"
```

## typeof

获取变量类型

```jsx
const user = { name: "Tom" }

type User = typeof user
```

## mapped types

```jsx
type ReadonlyUser = {
  readonly [K in keyof User]: User[K]
}
```
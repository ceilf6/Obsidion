# Vite原理

> vite: [https://github.com/vitejs/vite](https://github.com/vitejs/vite)
> 
> 
> 面试题：谈谈你对vite的理解，最好对比webpack说明
> 

webpack 原理图

![image.png](工程师沉淀/工程化/Vite/Vite原理/image.png)

vite 原理图

Vite 启动开发服务器时**不打包**、直接打开了服务器，在访问的时候再请求、响应，让开发服务器将编译给浏览器

转换一下资源路径，例如 vue ⇒ @module/vue

html页面

css ⇒ 字符串

.vue 通过 compiler-sfc 单文件组件编译器 ⇒ JS

模版<template> 通过预编译 ⇒ render函数

![image.png](工程师沉淀/工程化/Vite/Vite原理/image%201.png)

打包时可以直接用 webpack 或者 Vite自带的rollup

> 面试题答案：
> 
> 
> webpack会先打包，然后启动开发服务器，请求服务器时直接给予打包结果。
> 而vite是**直接启动开发服务器**，请求哪个模块再对该模块进行**实时编译**。
> 由于**现代浏览器本身就支持ES Module**，**会自动向依赖的Module发出请求**。vite充分利用这一点，**将开发环境下的模块文件，就作为浏览器要执行的文件，而不是像webpack那样进行打包合并**。
> 由于vite在启动的时候**不需要打包，也就意味着不需要分析模块的依赖、不需要编译**，因此启动速度非常快。当浏览器请求某个模块时，再根据需要对模块内容进行编译。这种**按需**动态编译的方式，极大的缩减了编译时间，项目越复杂、模块越多，vite的优势越明显。
> 在**HMR热更新**方面，当改动了一个模块后，仅需让浏览器**重新请求该模块即可**，不像**webpack**那样**需要把该模块的相关依赖模块全部编译一次**，效率更高。
> 当需要打包到生产环境时，vite使用传统的rollup进行打包，因此，vite的主要优势在开发阶段。
> 
> 另外，由于 Vite 利用的是“现代浏览器支持**ESM**”这一特性，在开发时将模块文件直接传输给浏览器，不需要打包，是实时编译
> 所以 CMJ 是不支持的
>
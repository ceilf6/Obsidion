# 在页面中使用flash

可以嵌入 flash 的元素

1. object 设置 data 路径导入，type设置MIME（多用途互联网邮件类型，例如 jpg 为 image/jpeg）为 application/x-shockwave-flash
可以通过 param 子元素传入参数
2. embed 通过 src 嵌入资源，type 仍然传的 MIME类型

他们都是可替换元素

一般是

```css
<object>
	<embed />
</object>
```

如果 object 识别成功，那么 embed 不能作为其子元素就不会被识别，否则就作为兜底
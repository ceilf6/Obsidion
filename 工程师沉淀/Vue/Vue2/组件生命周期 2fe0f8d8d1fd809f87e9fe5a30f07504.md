# 组件生命周期

# 组件生命周期

- 创建实例：new Vue() 或者使用组件 <Comp />
- 注入：将组件配置对象提取到实例上，同时实现 data 的响应式
- 生成虚拟vnode（render函数、模版、el…） → 挂载
- 然后在 data 变化时生成新的虚拟DOM，对比后应用到真实的DOM上

![image.png](%E7%BB%84%E4%BB%B6%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F/image.png)

hooks 钩子函数，通过在配置对象中写钩子函数拓展功能

- beforeCreate: **无法拿到data**
- created: 可以拿到**data**
- beforeMount: 有**虚拟DOM**节点了，但是**还没有挂载**到真实的DOM上
- mounted: 可以拿到 **真实DOM** 元素（$refs、DOM API…）
- beforeUpdate: 在数据变动之后、重新渲染之前
- updated: 重新渲染之后
- beforeDestroy
- destroyed: 记得析构组件管理的外部资源，例如计时器、事件监听，防止内存泄漏
    
    凡是在 mounted / created 中引入的“组件外部副作用”，都应该在 beforeDestroy 中对称清理
    

![image.png](%E7%BB%84%E4%BB%B6%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F/image%201.png)

![image.png](%E7%BB%84%E4%BB%B6%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F/image%202.png)

# 常见应用

> 不要死记硬背，要根据具体情况灵活处理
> 

## 加载远程数据 - created

```jsx
export default {
  data(){
    return {
      news: []
    }
  },
  async created(){
    this.news = await getNews();
  }
}
```

```jsx
async created() {
  const res = await getLab();
  console.log(res);
  // request 封装已经自动取了两层data
  this.branches = res.branches + this.offset; // 如果在 beforeCreated 阶段就取不到 this.

  // 同时 async 语法糖本质是 Promise 所以数据更新其实是异步回调，并不会等 created() 结束后再往下 beforeMount
},
```

## 直接操作DOM - mounted

```jsx
export default {
  data(){
    return {
      containerWidth:0,
    	containerHeight:0
    }
  },
  mounted(){
    this.containerWidth = this.$refs.container.clientWidth;
    this.containerHeight = this.$refs.container.containerHeight;
  }
}

```

## 计时器 - created启动、destroyed记得清除

凡是在 mounted / created 中引入的“组件外部副作用”，都应该在 beforeDestroy 中对称清理

```jsx
export default {
  data(){
    return {
      timer: null
    }
  },
  created(){
    this.timer = setInterval(()=>{
     ...
    }, 1000)
  },
  destroyed(){
    clearInterval(this.timer);
  }
}

```

如果不在 destroyed 阶段 clearInterval：

- 定时器闭包 → 持有 this
- this → 持有组件实例
- 组件永远无法 GC

```jsx
export default {
  mounted() {
    this.onClick = () => {
      console.log("document clicked");
    };
    document.addEventListener("click", this.onClick);
  },
  beforeDestroy() {
    document.removeEventListener("click", this.onClick);
  },
};
```
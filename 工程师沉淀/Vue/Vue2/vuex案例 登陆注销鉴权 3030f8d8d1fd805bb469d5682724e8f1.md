# vuex案例 登陆注销鉴权

vuex store 管理登陆态 ⇒ 影响组件状态

# 用户模块逻辑示意图

## 路由总体示意图

https://github.com/ceilf6/Lab/commit/5c77685db2f57937d161d20ac8152c08558d82a9

一开始并行做

whoAmI 登陆态恢复

路由匹配

根组件渲染 ⇒ 替换 router-view 占位

![image-20210130141625925](http://mdrs.yuanjin.tech/img/20210130141626.png)

## 鉴权守卫逻辑示意图

https://github.com/ceilf6/Lab/commit/290f01b2b9d7a15321b3823007e559d90d8babfc

鉴权守卫：类似于**拦截器**

![image-20210130144001709](http://mdrs.yuanjin.tech/img/20210130144001.png)

1. 判断是否需要鉴权
    
    在 routes 中路由对象上挂载额外信息
    
    ```jsx
    meta: { auth: true } // 挂上额外信息 meta 用于 to 路由鉴权
    ```
    
2. 根据仓库状态分流
    
    ```jsx
    // 导航守卫、鉴权拦截
    // 导航切换时（包含刷新页面的第一次）时运行
    /**
     * from: 之前的路由对象
     * to: 即将进入的路由对象
     * next: 确认函数，调用该函数：
        * 无参时会直接进入to，
        * 有参数时根据参数进入新的导航
            * 那么就可以通过 next 操控走向
     * 不调用时 next 就不会跳转
     */
    router.beforeEach((to, from, next) => {
        if (to?.meta.auth) { // 需要鉴权
            const status = store.getters["loginAdmin/status"]
            if (status === "login") {
                // feature: 例如添加判断 store 中 user 是否有权力等等、拓展性很强
                next()
            } else if (status === "unlogin") {
                next("/admin/login")
            } else if (status === "loading") {
                next("/loading")
            }
        } else {
            next() // 不需要鉴权直接走就好
        }
    })
    ```
    
3. 等待页需要监听仓库状态
    
    ```jsx
    export default {
      data() {
        return {
          loadingImg: defaultImg,
        };
      },
      // 不能直接 watch:{ ["$store.getters['loginAdmin/status']"] } 在组件实例上去找这个路径
      // 因为 无法解析JS表达式
      created() {
        this.unWatch = this.$watch(
          // 或者用 vuex 中的 this.$store.watch
          () => this.$store.getters["loginAdmin/status"], // 监听对象
          (newVal) => {
            if (newVal !== "loading") {
              // 如果结束了鉴权，那么就再次尝试进入目标页
              this.$router
                .push(this.$route.query.returnurl || "./admin/home")
                .catch(() => {}); // 防止多次重定向报错 uncatch
            }
          }, // 目标变化时的执行函数
          {
            immediate: true,
          },
        );
      },
      destroyed() {
        this.unWatch(); // 卸载监听
      },
    };
    ```
    
4. 如果结束了鉴权，那么就再次进入目标页、重新鉴权
    
    所以需要在 route 对象上携带 query 记录目标页地址
    

# 参考资料

## vue

[watch配置](https://cn.vuejs.org/v2/api/#watch)

[Vue.prototype.$watch](https://cn.vuejs.org/v2/api/#vm-watch)

等待页监控状态变化

## vuex

[mapState](https://vuex.vuejs.org/zh/guide/state.html#mapstate-%E8%BE%85%E5%8A%A9%E5%87%BD%E6%95%B0)

简化取命名空间中内容

map…(命名空间, [ 内容 ])

```jsx
import { mapState } from "vuex";
// mapState 会返回一个对象
/**
 * {
 *  loading(){
 *    return this.$store.state.loginAdmin.loading
 *  }
 * }
 */

export default {
  computed: {
    // 封装一下计算属性，避免长串链式调用
    // loading() {
    //   return this.$store.state.loginAdmin.loading;
    // },
    ...mapState("loginAdmin", ["loading"]),
  },
```

[getters](https://vuex.vuejs.org/zh/guide/getters.html)

计算属性

[mapGetters](https://vuex.vuejs.org/zh/guide/getters.html#mapgetters-%E8%BE%85%E5%8A%A9%E5%87%BD%E6%95%B0)

[modules](https://vuex.vuejs.org/zh/guide/modules.html)

解决一个 store 用于多个模块时的冗杂问题

![image.png](工程师沉淀/Vue/Vue2/vuex案例%20登陆注销鉴权/image.png)

还有 命名空间 防止命名冲突

[watch](https://vuex.vuejs.org/zh/api/#watch)

## router

[exact-path](https://router.vuejs.org/api/#exact-path)

[导航守卫](https://router.vuejs.org/zh/guide/advanced/navigation-guards.html#%E5%85%A8%E5%B1%80%E5%89%8D%E7%BD%AE%E5%AE%88%E5%8D%AB)

# 实践

## template

**<template> 本身不会在 DOM 中生成任何真实节点**

作为一个逻辑容器处理同级元素

```jsx
    <span v-if="status === 'loading'">loading...</span>
    <template v-else-if="status === 'login'">
      <span>当前登录管理员</span>
      <a href="">退出</a>
    </template>
    <router-link v-else :to="{ name: 'AdminLogin' }">Login</router-link>
```
# Hot 100

清除记录，不影响其他题单的状态

# 1

要求返回 下标

以 值为键，下标为值 

# 5

```jsx
/**
 * @param {string} s
 * @return {string}
 */
var longestPalindrome = function(s) {
    const t = '#' + s.split('').join('#') + '#'
    const LT = t.length;
    const LS = s.length;
    let r = 0; // 对称半径覆盖到的最远右侧
    let c = 0; // r 对应的 中心
    const p = Array.from({length: LT}, () => 0)
    let ansC = 0; // 答案最大中心
    let ansL = 0; // 答案最大半径长度
    for(let i = 0;i<LT;i++){
        if (i < r) {
            p[i] = Math.min(r - i, p[2 * c - i])
        } else {
            p[i] = 0
        }

        // 暴力拓展
        while(i+p[i]+1<LT && i-p[i]-1>=0 && t[i+p[i]+1]===t[i-p[i]-1]){
            p[i]++
        }

        if(p[i]>ansL){
            ansL = p[i]
            ansC = i
        }

        if(i+p[i]>r){
            r=i+p[i]
            c=i
        }
    }
    return s.slice(Math.floor((ansC-ansL)/2),Math.floor((ansC+ansL)/2))
};
```

# 3

```jsx
/**
 * 右拖左式双指针
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function(s) {
    let l = 0;
    let r = 0;
    const vis = new Map();
    const LS = s.length;
    let ans = 0;
    while(r<LS){
        if(vis.has(s[r])){
            ans = Math.max(ans, r-l)
            l = Math.max(l, vis.get(s[r])+1) // max 防止左回退
        }
        vis.set(s[r],r)
        r++
    }
    ans = Math.max(ans, r-l) // 最后一段
    return ans
};
```

# 146

[146 双向链表LRU缓存](146%20%E5%8F%8C%E5%90%91%E9%93%BE%E8%A1%A8LRU%E7%BC%93%E5%AD%98%203130f8d8d1fd8005b82ec34f5fdea2cc.md) 

# 49

```jsx
/**
 * 排序后作为 key
 * 或 如果只有小写 或 大写的话，直接 统计字母出现次数 作为键
 * @param {string[]} strs
 * @return {string[][]}
 */
var groupAnagrams = function(strs) {
    const vis = new Map()
    for(const str of strs){
        // const strArr = str.split('')
        // strArr.sort((a,b) => 
        //     a.**localeCompare**(b)
        //     // {
        //     //     if(a>b) return 1
        //     //     return -1
        //     // }
        // )
        // const key = strArr.join('')
        const Cnt = Array.from({length: 26},() => 0)
        for(const ch of str){
            Cnt[ch.**charCodeAt**(0)-97]++;
            // ch.charCodeAt(0) 即 ord(ch)
        }
        const key = Cnt.join('#')
        if(vis.has(key)){
            vis.get(key).push(str)
        }else{
            vis.set(key, [str])
        }
    }
    // const arr = Array.from(vis)
    // const ans = []
    // for(const key of vis.keys()){
    //     ans.push(vis.get(key))
    // }
    return Array.from(vis.**values**())
};
```

# 21

```jsx
/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * 返回当前链表头
 * @param {ListNode} list1
 * @param {ListNode} list2
 * @return {ListNode}
 */
var mergeTwoLists = function(list1, list2) {
    // 返回当前链表头
    if(list1 === null) return list2
    if(list2 === null) return list1

    if(list1.val < list2.val){
        // 小的先接 - 递归得到的后续链表头
        list1.next = mergeTwoLists(list1.next, list2)
        return list1 // 返回当前链表头
    } else {
        list2.next = mergeTwoLists(list1, list2.next)
        return list2
    }
};
```
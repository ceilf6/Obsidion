# 数据并行

**数据并行（Data Parallelism）** 的核心思想是：

> **把同一份计算任务应用到不同的数据子集上，并在多个处理单元上同时执行，从而加速整体计算。**
> 

也就是说：

- **数据被拆分** → 分成多个子数据块
- **计算逻辑相同** → 每个处理单元执行同样的程序
- **并行执行** → 在多个 CPU / GPU / 节点上同时处理
- **结果汇总** → 最终把各个部分结果合并

---

## **1️⃣ 简单理解**

假设你要处理 **100万条数据**：

- 单机串行：
    
    一个 CPU 一条一条算 → 很慢
    
- 数据并行：
    
    把数据分成 4 份
    

| **处理单元** | **数据** |
| --- | --- |
| CPU1 | 1–25万 |
| CPU2 | 25–50万 |
| CPU3 | 50–75万 |
| CPU4 | 75–100万 |

每个 CPU **执行同一段代码**，但处理 **不同的数据**。

最后再 **汇总结果**。

---

## **2️⃣ 用一句话概括**

数据并行的本质是：

**同一个算法 + 不同的数据 + 同时执行**

公式式理解：

```
数据 = 切分
计算 = 复制
执行 = 并行
结果 = 汇总
```

---

## **3️⃣ 在深度学习中的数据并行**

深度学习训练中常见的数据并行流程：

1. **复制模型到多个GPU**
2. **把一个batch的数据切分**
3. 每个GPU计算 **forward + backward**
4. **梯度同步（AllReduce）**
5. **更新模型参数**

示意：

```
GPU1  模型 + 数据1
GPU2  模型 + 数据2
GPU3  模型 + 数据3
GPU4  模型 + 数据4
        ↓
   梯度同步
        ↓
   更新同一模型
```

---

## **4️⃣ 数据并行 vs 模型并行**

| **类型** | **切分什么** | **适用场景** |
| --- | --- | --- |
| 数据并行 | 切分数据 | 数据量大 |
| 模型并行 | 切分模型 | 模型特别大 |
| 流水线并行 | 切分层 | 超大模型 |

---

✅ **总结**

数据并行的核心思想就是：

> **把数据拆分到多个计算单元上，让同一个程序同时处理不同的数据，从而加速计算。**
> 

---
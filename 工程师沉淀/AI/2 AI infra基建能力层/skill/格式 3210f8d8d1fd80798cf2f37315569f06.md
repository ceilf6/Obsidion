# 格式

比较稳妥的边界是这样：

1. [skill.md](http://skill.md/) 只保留能力定义
内容包括：**触发条件**、输入输出规范、处理规则、模板选择规则、每类模板的简短说明。
2. references 放**长模板**
内容包括：**完整**章节骨架、标准措辞、示例段落、不同文档类型的详细模版。
3. script 负责引用
像 generate_doc.py 这类脚本后续可以按模板类型去读取 references 里的对应文件。

对你当前文件，最合适的做法不是把模板信息全删掉，而是把 [skill.md](http://skill.md/) 这一段缩成“模板索引”：
会议纪要：适用于什么场景，核心字段有哪些。
项目方案：适用于什么场景，核心字段有哪些。
完整正文结构和示例措辞则放到 references 里。

只有一种情况我会建议留在 [skill.md](http://skill.md/)：模板非常短，只是字段清单级别，而且是模型必须始终看到的核心提示。如果模板已经开始出现完整段落、固定语气、示例文本，就应该进 references。

你这个 skill 现在已经到了该拆的时候。下一步建议：

1. 把会议纪要、项目方案、实验报告、简历、通知公告各自拆成独立模板文件放进 references
2. 在 [skill.md](http://skill.md/) 只保留模板索引和选择规则
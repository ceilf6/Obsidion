# 深拷贝

# 手写实现，兼容各类型

[深拷贝](../../%E5%AF%BF%E5%8F%B8/%E6%B7%B1%E6%8B%B7%E8%B4%9D%203190f8d8d1fd80bd9eb2fd917ba73bdb.md) 

# JSON

```jsx
export function deepCopy2(from) {
    return JSON.parse(JSON.stringify(from))
}
```

部分类型有问题

| **类型** | **结果** |
| --- | --- |
| function | 会消失 |
| undefined | 会消失 |
| Symbol | 会消失 |
| Date | 变成字符串 |
| Map / Set | 丢失 |
| RegExp | 丢失 |
| 循环引用 | 会报错 |

```jsx
const obj = {
    a1: () => {
        console.log()
    },
    sunObj: {
        a2: 2
    },
    a3: new Map()
}
```

得到

```tsx
{ sunObj: { a2: 2 }, a3: {} } // JSON
{ a1: [Function: a1], sunObj: { a2: 2 }, a3: Map(0) {} } // 手写的
```

## …展开运算符是浅拷贝

复制对象或数组时只会复制第一层
# Number(对象)

如果目标是一个对象，那么会先将 对象toString 之后再 toNumber

例如 [’0’,’1’] => ‘0,1’ => NaN

NaN 在 !!转为布尔值后 => false

```jsx
console.log(!!Number(['0', '1']))
```

得到 false

```tsx
Number([])
```

[].toString 得到 ‘’

Number(‘’) 得到 0

所以 Number([]) 得到 0
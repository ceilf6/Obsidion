# 为什么老生代不用复制 GC？

**核心矛盾：Cheney 算法的代价与存活率成正比**

```
新生代：存活率 ~10%  → 只需复制少量对象，代价小 ✓
老生代：存活率 ~80%+ → 需要复制绝大部分对象，代价极大 ✗
```

这就是**分代假说（Generational Hypothesis）**的本质：新生代的对象大多数会死，所以复制存活者划算；老生代恰恰相反。

---

## 具体代价分析

**1. 空间开销 — 2x 内存**

Cheney 需要 From + To 两块**等大空间**同时存在。新生代默认约 16MB，双倍 32MB 可接受。老生代可能是数百 MB 到几 GB，双倍完全不可接受。

**2. 复制带宽**

老生代大量对象存活，复制 GC 需要把它们全部 `memcpy` 一遍。这不仅慢，还会污染 CPU cache，影响整体性能。Mark-Compact 只移动**需要整理的对象**（有空洞的页），大部分情况下原地不动。

**3. 指针更新代价**

复制后所有指向这些对象的指针都要更新。老生代对象之间的互相引用极多，扫描和更新代价远高于新生代。

---

## 三色标记的真正价值不是"识别死对象"

你的直觉是对的——复制算法不需要标记死对象。但三色标记的核心价值是另一件事：

```
白 → 灰 → 黑  的状态机 = 可以随时暂停、随时恢复
```

这让 GC 可以**增量（incremental）+ 并发（concurrent）**执行，把数十毫秒的 Stop-The-World 切分成无数个 1ms 的小片，穿插在 JS 执行之间。Cheney 算法是 stop-the-world 的，无法做到这一点（除非用 Shenandoah/ZGC 那样的并发复制，需要读屏障，复杂度极高）。

---

## 总结

|  | 新生代 Cheney | 老生代 Mark-Compact |
| --- | --- | --- |
| 存活率 | ~10%（大多数死） | ~80%+（大多数活） |
| 2x 空间代价 | 可接受（空间小） | 不可接受（空间大） |
| 复制代价 | 小（只复制少数存活者） | 巨大（几乎全部要复制） |
| 增量/并发 | 难（需要读屏障） | 三色标记天然支持 |

用一句话概括：**复制 GC 是以空间换时间，且时间代价与存活量成正比。老生代存活量大、体积大，两个维度都不划算。**
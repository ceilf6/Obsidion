# 代理

[反向代理](%E5%90%8C%E6%BA%90%E7%AD%96%E7%95%A5%202f60f8d8d1fd805aa2add4064d274431.md) 

- **代理客户端出去访问** → 正向代理
- **代理服务端接收外部请求** → 反向代理

下面用直观方式讲清楚。

---

## **一、什么是代理**

“代理”就是中间人。

本来是：

**客户端 <-> 服务器**

加了代理之后变成：

**客户端 <-> 代理 <-> 服务器**

区别在于：这个代理到底是站在谁那一边。

---

## **二、正向代理**

### **1）定义**

正向代理是**替客户端去访问目标服务器**的代理。

也就是说，服务器看到的是“代理”来请求它，而不是你这个真实客户端。

### **2）关系图**

**客户端 -> 正向代理 -> 目标服务器**

### **3）谁主动配置它**

通常是**客户端这边配置**的。

比如浏览器、操作系统、脚本工具里设置一个代理地址。

### **4）服务器怎么看**

服务器通常会认为请求是从代理服务器发来的，不一定知道真实客户端是谁。

### **5）常见用途**

- 访问外部资源时做中转 科学上网**梯子VPN**
- **隐藏客户端真实 IP**
- 突破客户端网络访问限制
- 做**爬虫**时批量切换出口 IP
- 公司内网统一上网出口

### **6）一个生活类比**

你想买东西，不自己去店里，而是找“代购”帮你买。

店家面对的是代购，不是你。

这个“代购”就像正向代理。

---

## **三、反向代理**

### **1）定义**

反向代理是**替服务器接收客户端请求**的代理。

客户端以为自己在访问目标网站，其实先访问到了反向代理，再由它把请求转发给后面的真实服务器。

### **2）关系图**

**客户端 -> 反向代理 -> 应用服务器**

### **3）谁部署它**

通常是**服务器这一侧部署**的。

用户一般不需要专门配置，直接访问域名就行。

### **4）客户端怎么看**

客户端通常不知道后面到底有几台真实服务器，只知道访问的是一个统一入口。

### **5）常见用途**

- **开发代理**
- 负载均衡**CDN**
- **隐藏真实服务器**
- SSL/TLS 终止（HTTPS **证书放在代理层**）
- 缓存静态资源
- **网关**统一鉴权、限流、日志
- 灰度发布、蓝绿发布

### **6）一个生活类比**

你去一家大公司办事，先到前台。

前台根据你的需求，把你分配给不同部门处理。

你不需要知道后面到底有多少个部门、多少个人。

这个“前台”就像反向代理。

---

## **四、核心区别**

### **正向代理**

- 代理的是**客户端**
- 客户端知道自己用了代理
- 服务器未必知道真实客户端
- 常用于“我想怎么出去访问别人”

### **反向代理**

- 代理的是**服务器**
- 客户端通常不知道背后有代理
- 客户端未必知道真实服务器是谁
- 常用于“别人怎么来访问我的服务”

---

## **五、一个最直观的判断方法**

看“**谁在使用代理来隐藏自己**”。

- **客户端借助代理去访问别人**：正向代理
- **服务器借助代理来接待外部请求**：反向代理

---

## **六、举个实际例子**

### **正向代理场景**

你的电脑不能直接访问某个网站，于是配置了一个代理服务器：

- 你把请求发给代理
- 代理再去访问网站
- 网站返回结果给代理
- 代理再转给你

这里代理服务的是你，所以是**正向代理**。

### **反向代理场景**

你访问 example.com：

- 请求先到 Nginx
- Nginx 判断这个请求该转给哪台应用服务器
- 可能转到 A 机器，也可能转到 B 机器
- 再把结果返回给你

这里 Nginx 服务的是网站后端，所以是**反向代理**。

---

## **七、为什么很多人容易混淆**

因为两者都是“中间转发”。

真正的区分点不是“有没有转发”，而是：

**代理站在客户端一边，还是站在服务器一边。**

---

## **八、常见软件例子**

### **正向代理**

- Clash
- Squid（可做正向代理）
- 浏览器代理设置
- SOCKS5 代理

### **反向代理**

- Nginx
- HAProxy
- Envoy
- Apache（可做反向代理）

---

## **九、面试里可以怎么回答**

你可以用这一版：

> 正向代理是替客户端访问服务器，客户端需要主动配置代理，服务器看到的是代理；反向代理是替服务器接收客户端请求，客户端通常无感知，常用于负载均衡、缓存、隐藏源站和 HTTPS 终止。
> 

---

## **十、一句话总结**

**正向代理：帮“我”去访问别人。**

**反向代理：帮“别人”来访问“我”。**

要不要我顺手再给你画一个 **Nginx 场景图**，把两者放在一张图里对比？
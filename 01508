# 操作系统

ls -l 的结果各列表示

# 进程

## 三种状态

1. 运行：占用 CPU
2. 就绪：除 CPU 外已具备所有资源
3. 阻塞：等待某个事件，需要的某个资源尚未满足

新建 → 就绪 → 执行 → 阻塞：等待 I/O → 就绪 → 执行

不同进程之间由 **内核Kernel** 管理共享资源-调度

同一个资源如果并发操作会引发很多问题

- **临界资源（Ressource critique）**：同一时刻只能有一个进程访问的资源
- **互斥（Exclusion mutuelle）**：两个进程请求某临界资源时，只能一个进入，不能同时进入
- **临界区（Section critique）**：操作一个或多个共享对象、并要求独占访问的一段指令序列
- 每个进程都有自己的临界区
- 进入临界区之前，进程必须确保相关共享对象已被独占

## 互斥管理 信号量

信号量是一个整数计数器，表示可用访问许可的数量

- P(S) 申请资源
    - S > 0 时 S-=1
    - 否则阻塞
- V(S) 释放资源
    - 如果没有进程因为该资源而阻塞，就 S+=1
    - 否则唤醒一个被阻塞的进程 → 就绪态

通过信号量实现互斥，在一个进程使用和释放资源时，需要通过 P(S) → 临界区 → V(S)

```bash
while not finished:
    P(S)
    临界区
    V(S)
```

这样就确保管理了临界区的进程数量

[第 44 页：Linux 下的信号量](15%20操作系统体系理论%203440f8d8d1fd80448f18df582fdcc329.md) 

## 哲学家问题 全局限制申请数

[第 45 页：哲学家问题](15%20操作系统体系理论%203440f8d8d1fd80448f18df582fdcc329.md) 

- S[i] 第 i 个资源的信号量 
资源元素级别互斥
- plat 总控信号量，“允许同时尝试拿筷子的哲学家数量”
全局级别限制

```bash
penser();                 // 非临界区
P(plat);                  // 申请资源前
P(S[i]);
prendre_baguette(i);
P(S[(i+1)%5]);
prendre_baguette((i+1)%5);
manger();
poser_baguette(i);
V(S[i]);
poser_baguette((i+1)%5);
V(S[(i+1)%5]);
V(plat);                  // 离开临界区
```

通过 **总控信号量** plat设为4，防止5个人都在等待 导致**死锁**

限制同时申请资源的进程数

## 读者-写者问题

[第 49 页：图书馆问题](15%20操作系统体系理论%203440f8d8d1fd80448f18df582fdcc329.md) 

本质是 可访问可修改 资源的管理，像数据库

- 可以同时读
- 写的时候不能读，也不能由其他人写，也就是说 写的时候得独占

### **三类经典问题**

**第一类：读者优先**

只要没有写者正在写，读者就可以进入。

特点：

- 读者吞吐量高
- 但如果读者源源不断进来，写者可能一直等不到机会

这叫 **写者饥饿**

---

**第二类：写者优先**

一旦有写者在等待，就尽量不再放新的读者进去。

特点：

- 写者不会饿死
- 但可能导致读者等待很久

这叫 **读者可能饥饿**

---

**第三类：公平策略**

按到达顺序或某种公平机制调度。

特点：

- 尽量避免任何一方长期等待
- 更接近实际系统里的平衡方案

哲学家问题是防止 死锁，读者写者目标是平衡共享和互斥

设信号量

- mutex **保护读者记数** readcount
- rw 控制对共享资源的真正访问
- readcount 当前正在读的读者数量
    
    用于判断是否为最后一个离开的读者（需要释放 rw）
    

```bash
// reader
P(mutex);
readcount++;
if (readcount == 1)
    P(rw);     // 第一个读者锁住资源，阻止写者
V(mutex);

read();        // 多个读者可同时执行

P(mutex);
readcount--;
if (readcount == 0)
    V(rw);     // 最后一个读者释放资源
V(mutex);
```

**第一个读者把 “写锁”rw 拿到**，后续读者只需要增加 readcount ，然后**最后一个离开的读者释放 写锁rw**

写者必须拿到 rw 才能开始写，一旦拿到就说明没有读者了

```bash
// writer
P(rw);
write();
V(rw);
```

## 信号量用于进程间通信

像 P0 进程

```bash
P(S)
...
```

进程 P1 

```bash
...
V(S)
```

那么只有 进程P1释放资源后 P0 才能继续，也就是相当于一个 “可以继续”的信号

## 死锁

- 进程 `P1` 持有资源 `R1`，等待 `R2`
- 进程 `P2` 持有 `R2`，等待 `R1`

进程集合中每个进程都在等待资源

### 图归纳法检测死锁

- 为检测死锁，系统动态构造**资源分配图**
- 图中表示系统中资源的分配关系和请求关系
- 检测方式可以是：
    - 每次资源请求导致图变化时立即检测（CPU 开销大）
    - 周期性检测，或在 CPU 使用率低于某阈值时检测（但可能发现较晚）

资源分配图是一个二部图，包含两类节点和若干有向边：

- **进程**：圆形表示
- **资源**：矩形表示；若某资源有多个实例，则矩形内有多个点
- 从**资源指向进程**的有向边：表示资源已分配给该进程
- 从**进程指向资源**的有向边：表示进程在等待该资源

![image.png](工程师沉淀/操作系统/操作系统/image.png)

像上图中实线表示了资源被进程占有，虚线表示进程申请的资源。这图就是一个死锁

该图记录了：

- 每个进程持有哪些资源
- 每个进程还请求哪些资源

### 图归纳

检测通常通过**图归约**完成

归约规则：

- 若某资源只有流出边（没有等待请求），则删除这些边
- 若某进程只有流入边（说明它可执行完并释放资源），则删除这些边
- 对于每条请求边，若对应资源块中有空闲实例，则可先分配给该请求，也就是 资源实例满足该进程的请求的话

通过不断归约，若能消去所有进程，则说明无死锁；否则剩余部分即对应死锁集。

![image.png](工程师沉淀/操作系统/操作系统/image%201.png)

P process 进程

R resource 资源

R方块中的黑点 资源实例数

## 死锁恢复

检测到死锁后

- 临时从一个进程手中夺取某资源，转分配给另一个进程
- 恢复到先前某个安全状态（**回滚**），并避免再次落入相同局面
- 终止一个或多个进程

## 避免死锁

- 当进程请求资源时，系统必须判断这次分配是否**安全**
- 所谓安全，是指存在一个资源分配顺序，使所有进程最终都能完成
- 若安全，则允许分配
- 否则拒绝本次资源请求

### 银行家算法

> 系统在分配资源之前，不只看“现在够不够分”，还要看“分出去以后，系统是否仍然存在一条让所有进程都能最终完成的顺序”。
“假设性原则” 就像银行放贷一样，先评估风险再放出资源，确保未来的可持续性发展
> 

[使用**银行家算法（algorithme du banquier）验证该状态是否为安全状态**。](17%20!TD%20SE%203440f8d8d1fd807c9de1ff7aec97f345.md) 

Work Work表示的是当前进程进行之后的可用资源

设：

- 有 `n` 个进程 `P1 ... Pn`
- 有 `m` 类资源 `R1 ... Rm`

需要维护以下矩阵 / 向量：

- 最大需求矩阵 Max[n][m]
- 已分配矩阵 `Alloc (n × m)`：当前分配矩阵`Alloc[i,j]` 表示进程 `Pi` 当前持有的 `Rj` 类资源**数量**
- 尚需矩阵 `Req (n × m)`：需求矩阵`Req[i,j]` 表示进程 `Pi` 还需要多少 `Rj` 才能继续完成
    
    ```bash
    Req[i][j] = Max[i][j] - Alloc[i][j]
    ```
    
- 当前可用资源向量 `A (m)`：Available 当前可用资源向量
    
    ```bash
    Available = Total - Alloc总和
              = [10,5,7] - [7,2,5]
              = [3,3,2]
    ```
    
- 系统总资源向量 `E (m)`：系统中各类资源总量向量

**一. 安全性算法过程**

1. 找到一个**尚未标记**的进程 `Pi`，满足它的 `Req[i] <= A`
2. 如果找不到这样的进程，则当前状态**不安全**
3. 如果找到了：
    - 将该进程视为可完成
    - 把它占有的 `Alloc[i]` 加回到 `A`
    - 标记该进程
4. 若所有进程都能被标记，则状态安全；否则继续重复步骤 1

```bash
Work = Available
Finish[i] = false  (对所有 i)

while 存在某个 i 满足：
    Finish[i] == false 且 Need[i] <= Work
do
    Work = Work + Alloc[i]
    Finish[i] = true

if 所有 Finish[i] == true
    状态安全
else
    状态不安全
```

**二. 资源请求算法**

例如进程 Pi 申请

`Request[i] = [1, 0, 2]`

表示它还想申请：

- 1 个 `R1`
- 0 个 `R2`
- 2 个 `R3`

系统会判断

1. **请求不能超过它的最大剩余需求**
    
    `Request[i] > Need[i]`
    
    则出错，因为它申请超过了自己声明的最大需求
    
2. **请求不能超过当前可用资源**
    
    若：`Request[i] > Available`
    
    则当前不能立刻满足，进程必须等待。
    
3. **试探性分配**
    
    如果前两步都通过，系统**先假装把资源分给它**：
    
    - `Available = Available - Request[i]`
    - `Alloc[i] = Alloc[i] + Request[i]`
    - `Need[i] = Need[i] - Request[i]`
    
    然后运行一次**安全性算法**：
    
    - 如果结果安全 → 真正批准这次分配
    - 如果结果不安全 → 撤销试探分配，让进程等待

[银行家算法过程例子](银行家算法过程例子%203460f8d8d1fd8051914cd7fdfdfac4d2.md)

### **已知矩阵后：**

1. 先算 `Need = Max - Alloc`
2. 先算 `Available = Total - 各列Alloc之和`
3. 令 `Work = Available`
4. 找满足 `Need <= Work` 的进程
5. 找到后执行 `Work += Alloc`
6. 记录安全序列
7. 重复直到全部完成或卡住
8. 全部完成则安全，否则不安全

### **如果还有请求：**

1. 检查 `Request <= Need`
2. 检查 `Request <= Available`
3. 试探分配
4. 做安全性检查
5. 安全则批准，不安全则回滚

## 进程调度

- 计算机中的所有软件都可看作若干进程的集合，其执行由一个特殊过程管理：**调度器（scheduler）**
- 调度器负责：
    - 决定等待进程何时、按何顺序进入执行
    - 把处理器分配给就绪进程
- 进程执行通常是 CPU 计算与事件等待（I/O、等待信号量等）的交替序列
- 调度器负责**上下文**切换（如 PCB 表管理）：当前进程阻塞或被挂起时，把 CPU 分给另一个进程

### 目标

- 最大化单位时间内完成的进程数
- 最小化每个进程的等待时间
- 最大化 CPU 利用率
- 避免饥饿（无限等待）
- 优先照顾高优先级进程
- 最小化上下文切换次数

性能指标

- **周转时间（Temps de séjour）**：从进程到达到完成退出的总时间
- **等待时间（Temps d’attente）**：进程处于就绪态的累计时间
- **响应时间（Temps de réponse）**：从进程进入系统到第一次获得 CPU 的时间
- **吞吐量（Capacité de traitement）**：单位时间内处理的进程数
- **处理器利用率（Taux d’utilisation）**：CPU 被实际使用的比例

### 调度策略

**选哪个进程执行？**

- 先来先服务（FCFS）
- 最近最少使用（课件这里写 LRU，但调度上下文中更常见的是其他优先准则）
- 优先级最高者优先（静态或动态优先级）
- 最短作业优先

**给该进程分配多久 CPU？**

- 一直执行到结束或主动释放（非抢占式）
- 分配固定 / 可变时间片（抢占式）

**何时调用调度器？**

- 当前进程阻塞或主动交出处理器
- 分配给当前进程的时间片耗尽
- 有更高优先级进程到来

### 非抢占式调度

操作系统通常依据以下规则组合选择进程：

- FCFS（先来先服务）
    
    队列
    
- SPF（Shortest Process First，最短进程优先）
- 优先级最高者优先

一旦选中某进程：

- 它会**一直运行**到
    - 结束
    - 阻塞（等待事件）
    - 主动释放 CPU

整个过程中不会被系统强行剥夺处理器。

### 抢占式调度

> 有点像 React 的 Scheduler 的思想
如何利用执行一个任务结束后的剩余时间：像 React 是通过 while 循环不断判断；操作系统中时间片的长度代表一个任务最长占用时间，如果超过了这个时间就会归还 CPU 重新调度，但是如果提前结束的话那么当前时间片就提早结束了不会继续占着 CPU
也就是说 一个时间片只能给一个进程使用
> 
- 为了防止某个进程独占 CPU 太久，计算机会用电子时钟周期性地产生**中断**
- 每次时钟中断后，操作系统重新获得控制权，并决定：
    - 当前进程继续执行
    - 或暂停该进程，让另一个进程运行
- 若决定切换，则必须先保存当前进程的寄存器状态，再装入下一个进程的寄存器状态
这就是**上下文切换**
- **CPU 在多个进程之间轮流切换，每个进程执行几十到几百毫秒**
- 分配给一个进程的这段 CPU 时间称为**时间片（quantum）**
- 上下文切换必须足够快，即开销明显小于时间片
- 在**某一瞬间 CPU 实际只执行一个进程**
但**在 1 秒内它可以切换执行多个进程**，因此给人一种**“并行”的感觉（伪并行）**

### **SRT 最短剩余时间优先**

- 当一个新进程到达时，调度器会比较：
    - 新进程预计剩余执行时间
    - 当前正在运行进程的剩余执行时间
- 若**新进程更短，则立即抢占当前进程**
- 若剩余时间相同，优先继续执行刚刚正在运行的进程

### Round Robin 时间片轮转调度

![image.png](工程师沉淀/操作系统/操作系统/image%202.png)

用队列保存等待执行的进程，若时间片用完仍未结束，则该进程被挂起并重新插入队尾

### 多级优先队列调度

- 调度器给每个进程分配一个优先级（静态或动态）
- 相同优先级的进程位于同一个 FIFO 队列中
- 有多少个优先级层级，就有多少条队列
- 每条队列内部使用时间片轮转方式调度

示例

![image.png](工程师沉淀/操作系统/操作系统/image%203.png)

进程名 所需时间片数 到达时刻 初始优先级 访问页集合

> 到达时刻：开始有资格被调度。而开始时刻是调度器分配CPU给它的时刻
开始时刻 = 到达时刻 + 等待时刻
> 
- `T1`：执行 3，初始时刻 0，优先级 5，页 `1,2`
- `T2`：执行 2，到达 `ε`，优先级 6，页 `2,3`
- `T3`：执行 1，到达 `1`，优先级 7，页 `3,4`
- `T4`：执行 3，到达 `1+2ε`，优先级 8，页 `4,5`
- `T5`：执行 2，到达 `1+ε`，优先级 9，页 `5,6`

优先级会带来公平性问题：低优先级进程的执行可能不断被高优先级进程到来所**推迟**

- 可能出现**优先级反转**：
    - 低优先级进程持有高优先级进程所需资源
    - 低优先级进程却因为总有更高优先级进程活跃而无法运行
- 如果某个就绪进程等待资源过久，系统可以**临时提高其优先级**，以避免：
    - 饥饿
    - 优先级反转

像在 Linux 中

- Linux 调度器使用多条队列，每条队列对应一个优先级
- 在内存中的就绪进程按优先级分布到不同队列
- **用户态进程优先级通常为非负数 - 高优先级**
- **内核态**进程（或超级用户进程）优先级为负数，优先级更高
- 调度器采用时间片轮转原则
- 被选中的进程最多运行一个时间片（约 100ms）
- 若时间片结束后未完成也未阻塞，则会被挂起
- 被挂起的进程会重新插入其所属队列的队尾
- `nice` 命令可为进程设置更低的优先级（数值大于 0）
- `renice` 可修改正在运行进程的优先级
- 就绪进程的优先级会周期性重算，通常每秒一次，考虑因素包括：
    - CPU 使用时间
    - 缺页次数

# 虚拟内存

虚拟内存是一种内存管理机制，让每个进程看到 连续、比物理内存更灵活 的地址空间。分页机制是实现虚拟内存的常见方式之一

像运行大程序时，使用的是虚拟内存而不是直接访问物理内存，操作系统会把程序的地址空间切成一页一页，当前常用的页放在物理内存 **RAM** 中，不用的页放在磁盘，访问某一页时如果不在内存那就是缺页中断，操作系统再将其调入

分页机制除了扩容外，还有

- 为每个进程提供独立地址空间
- 实现内存保护
- 为程序提供”连续“地址，方便变成
- 支持共享内存、动态链接等

但是虚拟内存得转换为物理内存才能使用，页与页框之间的映射关系存储在**页表（table des pages）**中[第 22 页：虚拟地址转换](15%20操作系统体系理论%203440f8d8d1fd80448f18df582fdcc329.md) 

一页对应一个表项，问表项个数就是问页数。因为需要的信息是这个虚拟页对应是哪个物理页

```bash
页数 = 虚拟空间大小 / 页大小
```

在分页地址中，一个虚拟地址会被拆成两个部分

**页号 = 虚拟地址 // 页大小**

> 整除，取整数部分
> 

**页内偏移 = 虚拟地址 % 页大小**

- 页号
    
    虚拟内存会被分成很多固定大小的页，页号说明了这个地址属于虚拟地址空间的第几页
    
    操作系统和MMU会先拿这个页号去查页表，找到这一页对应哪一个物理页框
    
- 页内偏移
    
    说明在这一页的第几个字
    

若页号有 n 位，页内偏移有 m 位，则：

- 虚拟地址空间大小 = 2^(n+m)
- 页大小 = 2^m
- 页数 = 2^n

若 p 是页号，q 是页内偏移，则地址 = p * 2^m + q
若 p 是页号，q 是“该页第 q 个字”，则地址 = p * 2^m + (q - 1)

## 遍历文件

- 行
    
    ```bash
    while read curLine || [ -n "$curLine" ]
    do
    
    done < "$1"
    ```
    
- 单词: 先 cat 读取文件内容之后按照 IFS 分割一个个单词逐个赋值给 i
    
    ```bash
    for i in $(cat "$1")
    ```
    
- 直接通过目录遍历文件
    
    ```bash
    for file in "$dir"/*
    ```
    
- 直接通过 awk 读取文件，
    
    awk 默认读取就是按**行**遍历的 也就是 $0 
    
    通过 NR 在前面作为 selection 控制行号
    
    然后在 NF字段数 的范围内通过 $i 下标遍历**单词**
    
    ```bash
    awk '{
    	for (i = 1;i <= NF; i++){
    		echo $i
    	}
    }' "$fileName"
    ```
    

## 遍历输入

### 多行输入

像 

```bash
./script.sh '123323
sasas
sqwed2'
```

这几行内容都会作为 **$1同一个参数** 传入脚本

- 如果 `for i **in** $1` 会按 **IFS分词**也就是**空格、换行**一起拆分，也就是遍历**单词**
- 按行拆分可以仿照上面只不过输入不是从文件而是从标准输入
    
    ```bash
    while read curLine
    do
    
    done **<<< "$1"**
    ```
    

## 遍历命令结果

利用管道化

- 按行遍历
    
    ```bash
    ls -l | while read curLine
    do
    
    done
    ```
    
    ```bash
    ls -l | awk '
    NR > 1 { sum[$4] += $5 }
    END {
        for (g in sum) print g, sum[g]
    }'
    ```
    

可以通过 `${#str}` 得到字符串长度

通过参数展开截取子串，substr的用法

```bash
${str:开始下标:长度}
```

并且注意 str 需要是变量名不能是 $参数

```bash
    nom="$1"
    lowerDir=$(printf '%s' "${nom:1:3}" | tr 'A-Z' 'a-z')
```

## 收集错误信息

### 管道有

- 0 输入
- 1 输出
- 2 错误

```bash
err=$(命令 2>&1)
```

将 2 错误管道 重定向到 1 标准输出，这样才能赋值给 err

这里的 **&** 告诉 Shell：1 不是文件名，而是文件描述符

[01 Cour1&2](01%20Cour1&2%203410f8d8d1fd80ba8c04d7e7e7dd2108.md)

[02 TD1](02%20TD1%203420f8d8d1fd803c80b9c7741c3ff3fa.md)

[04 TD2](04%20TD2%203420f8d8d1fd8038b25ff92ddc83e6d7.md)

[06 grep sed](06%20grep%20sed%203420f8d8d1fd8000ababc051cc1279cd.md)

[07 Cours4 awk](07%20Cours4%20awk%203430f8d8d1fd80e7bfeae20d1326e5c4.md)

[08 TD3](08%20TD3%203430f8d8d1fd802d88edc350cae82839.md)

[09 ! TD4](09%20!%20TD4%203430f8d8d1fd806e8d26d39a51c993ff.md)

[10](10%203440f8d8d1fd807790b2c329543baffb.md)

[11 用户](11%20用户%203440f8d8d1fd803bb92dfab5951e40e7.md)

[12-01 日志、启动](12-01%20日志、启动%203440f8d8d1fd806b8e4bfb492a2510f2.md)

[12-02 ! 文件、网络](12-02%20!%20文件、网络%203440f8d8d1fd8094b9ebd97e497c002c.md)

[13 TD5 tr useradd](13%20TD5%20tr%20useradd%203440f8d8d1fd80c4b3becb4e997602a1.md)

[13 ! TD6](13%20!%20TD6%203440f8d8d1fd809099a6c65e96872ed2.md)

[15 操作系统体系理论](15%20操作系统体系理论%203440f8d8d1fd80448f18df582fdcc329.md)

[16 !8 TD7&8](16%20!8%20TD7&8%203440f8d8d1fd80be833bf87b6093496b.md)

[17 !TD SE](17%20!TD%20SE%203440f8d8d1fd807c9de1ff7aec97f345.md)

[真题 !!](真题%20!!%203450f8d8d1fd80b8a1bed421f984b002.md)
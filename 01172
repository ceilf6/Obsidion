# 常见 CSS 属性

CSS属性即样式声明

调试：

开发者工具 Elements 的 elements.style

user agent ：UA，用户代理，即浏览器（默认样式表）

1. color
元素内部的文字颜色
预设值：定义好的单词
三原色：色值，光学三原色 （红、绿、蓝）
  1. rgb表示法：每个颜色可以用 0~255 之间的数字来表示 256色
    
    ```html
    color: rgb( , , )
    ```
    
     2. hex 16进制表示法
    即两位16进制表示256色
    
    ```html
    color: #008c8c
    ```
    
    常见的：
    
    淘宝红 #ff4400 三种颜色个位和十位相同时可以简写为 # f40
    
    黑色 #000
    
    白色 #fff
    
    红色 #f00
    
    绿 #0f0
    
    蓝 #00f
    
    紫 #f0f
    
    青 #0ff
    
    灰 #ccc
    
    ## 透明
    
    1. opacity 设置整个元素盒子内部所有的透明（边框、背景…）
    2. 在颜色位置设置 alpha 透明通道
    1. **rgba(red, green, blue, aphla)** 最后一个 aphla 表示透明通道，取值 0～1 ，0完全透明，1是完全不透明
    2. hex: **#红绿蓝透**
        
        ```
        /* 半透明 - 一般都是黑色 */
        background-color: rgba(0, 0, 0, .5);
        ```
        
2. background-color
元素背景颜色
3. font-size
元素内部文字的尺寸大小
    
    单位：
    
    1. px 像素，简单理解为文字的高度占多少像素
    2. em 相对单位，用在 **font-size字体大小** 时相对于**父元素的字体大小**，最终会被换算成绝对单位 px（可以在 Elements 的 Computed 中查看）
    每个元素必须有字体大小，如果没有声明，则直接使用父元素的字体大小，如果没有父元素(html)，则使用基准字号（例如来自谷歌浏览器的设置）
        
        em 如果用于 **不是font-size字体大小** 的属性，是相对于**当前元素自身的字体大小**
        
    3. **% 继承**自**包含块**的相对大小
4. font-weight
字体粗细，可以取值为数字，一般用
1. normal 是 400
2. bold 加粗
strong 元素默认是加粗的
strong 表示重要的、不能忽略的内容
5. font-family
文字类型
例如 微软雅黑 等等电脑上带有的，可以同时设置多个（优先级等等）
    
    ```html
    font-family: consolas,微软雅黑,Arial,sans-serif
    ```
    
    snas-serif 非衬线字体，是一类字体，例如微软雅黑等，兜底
    
6. font-style
字体样式，通常用它设置斜体
    
    ```html
    font-style: italic
    ```
    
    i ，em 元素的默认样式是倾斜字体
    i 元素表达的语义是特殊的文本，例如术语，朗读时会有特殊的声调，通常用于表示一个图标
    
    em 表示强调的内容
    
7. text-decoration
    
    文本修饰：给文本加线
    
    line-through 穿过文字-文字中间加一条线
    
    例如在 del 元素中表示错误的内容
    
    s 元素表示已经过期
    
    underline 下划线
    
    a元素
    
8. text-indent
    
    首行文本缩进
    
    1. px 像素值
    2. em
9. line-height
    
    行高：每行文本的高度，该值越大，每行文本距离越大
    
    设置行高为**容器的高度 height** ，可以让**单行文本垂直居中**（但是只能单行，现在更多用 flex 即display:flex; align-items:center
    
    行高可以设置为纯数字，表示**相对于当前元素的字体大小（即 em）**
    
10. width
    
    宽度
    
11. height
    
    高度
    
12. letter-space
    
    文字之间的横向间隙
    
13. **text-align**
    
    **元素内部的行盒水平排列方式**
    
    left
    
    center 行盒居中
    
    right
    

# display

1. none : 不生成盒子 → 隐藏元素
2. block
3. inline
4. inline-block
5. flex
6. grid

# cursor 鼠标样式

1. pointer 小手指着
2. e-resize 重新调整尺寸的
3. 还能支持图片，一般是 .ico 或 .cur 的
    
    ```css
    cursor: url(”imgs/target.ico”), auto;
    ```
    
    auto 表示如果失效了就用默认的兜底
    

# 盒子隐藏

1. **display**: none **不生成盒子**，可能会影响其他盒子的排布
2. **visibility**: hidden 不影响盒子，只是从视觉上移除盒子

# 背景图

### 和 img 元素的区别

img 元素是属于 HTML 的概念

背景图属于 CSS 的概念

1. 当图片属于网页**内容**时，必须使用 img 元素
2. 当图片**仅用于美化页面**时，必须使用背景图

### 涉及的 CSS 属性

1. **background-image**: url(” “)
    
    路径是相对于 css 文件的
    

默认情况下，当背景图没有铺满的时候，会在横坐标和纵坐标中进行重复

1. background-repeat 速写属性
    1. background-repeat-x 
    2. background-repeat-y
    
    值为 no-repeat 或 repeat
    
2. background-size 类似于 object-fit
contain, cover
百分比：相对于包含块
3. background-position
横向排布值 纵向排布值
center 居中
left, right, top, bottom
    
    为数值时分别是 离左边 离上边 的距离
    （spirit图）
    
4. background-attachment
    
    fixed 相对于视口固定
    
5. background-color
    
    背景图没有填满的地方用颜色填满
    

上面都可以速写 background

由于 位置和尺寸 都可能为数字，所以浏览器要求 **position / size** （中间必须有斜杠 / ）

```css
background: [position] / [size]
```
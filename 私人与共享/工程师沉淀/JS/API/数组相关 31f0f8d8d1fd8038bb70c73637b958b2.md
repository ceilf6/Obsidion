# 数组相关

[数组作为key](%E6%95%B0%E7%BB%84%E7%9B%B8%E5%85%B3/%E6%95%B0%E7%BB%84%E4%BD%9C%E4%B8%BAkey%203190f8d8d1fd807187cdc245c3f6bf6a.md)

[splice, slice](%E6%95%B0%E7%BB%84%E7%9B%B8%E5%85%B3/splice,%20slice%2031f0f8d8d1fd80b7a831ec687fb878d1.md)

filter 不会改变原数组

**slice 不会影响原数组，splice 会**

通过**维护head变量**避开 **.shift()的严重性能损耗**（shift会平移整个数组）

[    // 两次BFS
    let queue = []
    let head = 0
    let vis = new Array(n).fill(0)
    let farNode;
    let maxD = -Infinity;
    function BFS(i){
        vis[i] = 1
        queue.push([i,0])
        while(head < queue.length){
            // shift 会整体移动数组性能过差
            const [cur,curD] = queue[head]
            head++
            // const [cur,curD] = queue.shift()
            let flag = 0 // 是否能往下走，即是否到头了
            for(const nxt of next[cur]){
                if(!vis[nxt]){
                    vis[nxt] = 1
                    queue.push([nxt,curD+1])
                    flag=1
                }
            }
            if(!flag && maxD < curD){
                // 注意一定要比较得到是最远的
                farNode = cur
                maxD = curD
            }
        }
    }
    BFS(0)
    // 刷新
    // queue = []
    vis = new Array(n).fill(0)
    BFS(farNode)](../../%E5%AF%BF%E5%8F%B8/%E7%9C%9F%E9%A2%98/%E9%98%BF%E9%87%8C%E7%9C%9F%E9%A2%98/%E9%98%BF%E9%87%8C%E4%BA%912025-3%203270f8d8d1fd80b29cc6efa04d01990c.md) 中对 BFS 用 head 维护队列头

shift() 是从左边出去，**unshift() 就是从左边插入**

**in** 判断的是下标是否存在

判断值是否存在得用 **.includes**

```jsx
console.log(6 in [6]) // false
console.log(0 in [6]) // true

console.log([6].includes(6)) // true
```
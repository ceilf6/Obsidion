# CSP

Content Security Policy 是一种 浏览器安全机制 ，用于**限制网页可以加载的内容**，防止注入

在相应头中设置

```jsx
**Content-Security-Policy**: 
```

例如设置

script-src 设置 JS脚本源

例如设置了 

```jsx
script-src 'self';
```

会禁止 inline script 嵌入脚本执行
# M端罗盘改动技术方案（带接口）

Source: [KM 2763493371](https://km.example.com/collabpage/2763493371)

## 公共维度筛选项配置改造计划

### Summary

在现有 `ShelfABConfig` 配置链路上做窄改动：公共维度一级筛选项支持分组角标类型/文本规则，公共维度二级筛选项支持团单配置类型；非公共维度保持现状。按已确认口径：以 `shelfActiveType === COMMON(3)` 生效，空角标不报错也不保存，角色权限沿用现有规则。

### Key Changes

- 扩展类型与常量：新增 `CornerLabelType`（`STATIC_TEXT=1`、`DYNAMIC_TEXT=2`）和 `RULE_TYPE.CORNER_LABEL_RULE`；将 `cornerLabel` 类型扩展为 `{ text?, type?, cornerLabelRule? }`。
- 改造分组角标表单：公共维度一级筛选项展示“分组角标类型”和“分组角标文本”；文本类型使用原输入框和原 placeholder，限制 4 字；动态文本类型复用 RuleTag 下拉查询，传 `ruleType: CORNER_LABEL_RULE`。
- 数据转换规则：静态角标保存 `{ type: 1, text }`；动态角标保存 `{ type: 2, cornerLabelRule }`；未填文本/规则时不输出 `cornerLabel`；历史 `{ text }` 回显为静态文本。
- 二级团单配置：公共维度二级筛选项对 RD/PMOP 展示必选“类型”，可选手动配置或系统抓取；BD 遵守现有权限仍只走手动配置。手动配置沿用现有 dealId 选择和提示文案，系统抓取沿用现有规则下拉与 `SUBJECT_RULE` 查询。
- 校验更新：角标不做必填校验，只保留静态文本 4 字限制；“最多两个分组角标”统计有效静态文本或动态规则；二级系统抓取按现有 `recallRule` 必填逻辑校验。

### Public APIs / Types

- `getFormData()` 输出的 `filterItems[].cornerLabel` 可能新增 `type` 和 `cornerLabelRule`。
- `IRuleFilterParams.ruleType` 支持新增值 `CORNER_LABEL_RULE`。
- 不新增外部回调；角标动态规则查询复用现有 `onDynamicTitleFilter` / `onDynamicTitleLoadMore`。

### Test Plan

- 运行 `pnpm build` 验证类型和打包。
- 本地表单验证：公共维度一级静态角标、动态角标、空角标输出符合预期。
- 回归验证：非公共维度仍显示旧角标输入并保持旧保存结构。
- 二级筛选项验证：公共维度 RD/PMOP 可切换手动/系统并分别输出 `deals` / `recallRule`；BD 不出现系统抓取入口。
- 验证二级手动配置原提示文案保持不变，系统抓取使用规则下拉并分页加载。

### Assumptions

- 当前工作区已有未归属改动：`pnpm-lock.yaml`，实现时不触碰、不回滚。
- “公共维度全部样式”按 `shelfActiveType === ShelfActiveType.COMMON` 判断，不额外限制 `shelfStyle`。
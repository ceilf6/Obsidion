# M端罗盘改动技术方案（新）

Source: [KM 2760080604](https://km.example.com/collabpage/2760080604)

## 技术分析

本次页面修改无需添加物料，也不调整 SetupMaterial 对外挂载结构

> <span style="color:#383838">SetupMaterial 是一个大对象，会挂载到页面的 window 上，内部包含了仓库中所有暴露的组件和方法</span>
<span style="color:#383838">这次目标变动组件就是它下面挂载的 ShelfABConfig</span>
> 

API 与 ts类型 也都无需变动

只收口 ShelfABContig 内部筛选项表单的展示规则

### 1、分组角标扩展

将一级筛选项中的“分组角标”升级为“分组角标类型 + 分组角标文本”两段式配置。分组角标支持固定文本和动态文本两种来源，字段均非必填
二级筛选项不纳入本次范围 [轻快货架新增活动筛选项](https://km.example.com/collabpage/2757014658?quote-id=2757014658--5ddc27b0-619e-4aa0-b244-927f6275d3a4&discussion-id=2049401018609590330&comment-id=2049401018626203650)
提交协议采用扩展后的 cornerLabel，避免复用顶层 textGenerationRule，从而和“分组类型=动态文本”的标题规则区分开。

新增角标协议

> 
> 

```
// 代码块
enum CornerLabelType {
  TEXT = 1,
  DYNAMIC_TEXT = 2,
}

cornerLabel?: {
  type?: CornerLabelType;
  text?: string;
  textGenerationRule?: {
    ruleId: number | string;
    name?: string;
    rule: string;
    desc?: string;
  } | null;
}
```

兼容规则：

- 历史数据 cornerLabel: { text: '角标' } 回显时按 type = TEXT 处理。
- 未配置角标时不提交 cornerLabel，或提交空对象时后端/M 端应按无角标处理。
- 动态角标规则查询复用现有动态文案规则查询能力：ruleType = FILTER_ITEM_RULE，bizType = 2。如后端后续提供角标专用规则类型，只替换查询参数，不调整保存结构。

### 变动点

- 常量与类型：新增 CornerLabelType 和 CORNER_LABEL_TYPE_LIST；扩展 cornerLabel 类型定义，新增表单临时字段 cornerLabelType、cornerLabelDynamicText，但最终 IFormResult 中只保留规范化后的 cornerLabel。
- 表单 UI：在 basic/index.tsx (line 155) 将原“分组角标”拆为：
    - 分组角标类型： Radio，选项由 CORNER_LABEL_TYPE_LIST.map 渲染。
    - 分组角标文本： 紧跟在类型下方；选择“文本”时展示现有输入框，placeholder 保持“仅可配置两个分组角标，限制填写四字以内”；选择“动态文本”时展示规则 Select。
- 表单逻辑：在 basic/hooks.ts 对角标相关变更统一归一化为 changeKey = 'cornerLabel'，避免 cornerLabelType/cornerLabelDynamicText 等临时字段污染上层 config。
- 动态规则：新增独立的角标动态规则搜索状态和 load more 逻辑，查询参数与现有分组动态文案一致，但不复用同一个 dynamicTextSearchInfo，避免两个下拉互相覆盖搜索结果。
- 数据转换：convertPropsToFormData 根据 cornerLabel.type/text/textGenerationRule 回填 Radio、输入框或 Select；convertFormDataToResult 只输出当前类型对应的字段，文本类型输出 text，动态类型输出 textGenerationRule。
- 提交转换：在 utils.ts 保持 cornerLabel 作为筛选项配置的一部分提交；标题动态文案仍使用顶层 textGenerationRule，角标动态文案只放在 cornerLabel.textGenerationRule。
- 校验与风控：分组角标类型和文本均不加 required；文本类型仅保留 4 字以内校验；提交前“最多两个分组角标”统计同时识别 cornerLabel.text 和 cornerLabel.textGenerationRule；任意角标配置变更都清理 riskScene 中的 cornerLabel。

同时为了不换行，样式方面调整了 <span style="color:#e50000">labelWidth</span><span style="color:#000000">=</span><span style="color:#0a3069">'7em' </span>，这个长度是仿照下方 <span style="color:#0a3069">'全部商品时不展示：' </span>设置的

### 2、团单配置扩展

本次改造目标是在二级筛选项的“团单配置”中展示必选的“类型”字段，支持“手动配置”和“系统抓取”。现有数据协议已包含 recallType、deals、recallRule，二级之所以没有显示是因为 singleShelfConfig/hooks.ts 给二级传了 blackList: ['recallType', ...] 。因此不新增接口字段，只调整二级表单展示规则和回归校验。

- 只把二级 blackList 里的 recallType 移除，让“类型”字段展示出来。
- 不影响角色分流：recallTypeList 仍然使用现有 ROLE_RECALL_TYPE_MAP[roleType]。这样 RD/PMOP 看到“手动配置 / 系统抓取”，BD 仍只看到“手动配置”。

关于二级需要筛选不属于一级的商品，我理解是这个规则应该是由后端把控的，因为具体召回内容取决于运行时上下文

### 变动点

将 <span style="color:#0a3069">dealTopReason</span> 开放给 <span style="color:#1f2328">blackList ，打开系统抓取</span>

## QA自测

> 🎬 **视频附件**：[录屏2026-04-30 14.51.00.mov](https://km.example.com/api/file/cdn/2760080604/235063990656?contentType=1)
> 

[https://km.example.com/collabpage/2759617947](https://km.example.com/collabpage/2759617947)
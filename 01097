# 真题 !!

# LO03 考试样卷（中文翻译）

## 练习 1

![image.png](工程师沉淀/操作系统/操作系统/真题%20!!/image.png)

现有一台由多个处理器组成的机器。希望在这些处理器上运行 9 个任务。

任务之间的依赖关系由下方的前驱关系图（precedence graph）定义。

每个任务的执行时间未知，并且要求每个处理器一旦可以使用就必须立即投入使用。

请借助**信号量（semaphores）**，将这 9 个任务分配到处理器上执行。

---

## 练习 2

某系统使用多个执行队列。

这些队列由一个**轮转调度器（Round Robin）**管理，时间片（quantum）的值为 **3 个时间单位**。

每个进程（记为 T1 到 T5）的初始优先级如下表所示。

该系统采用基于 **LRU（最近最少使用）算法** 的分页管理。

每个任务在执行时会访问若干页，页号从 **1 到 5**（见下表）。

系统始终为其分配固定的 **3 个物理内存页框**。

**每经过 2 个时间片**，进程的优先级会增加，增加值等于该进程产生的**缺页次数**。

| 进程 | τ（时间单位） | T（时间单位） | 初始优先级 | 访问页面 |
| --- | --- | --- | --- | --- |
| T1 | 5 | 0 | 1 | 1 和 5 |
| T2 | 1 | 2 | 2 | 2 和 5 |
| T3 | 4 | 4 | 1 | 5 |
| T4 | 3 | 5 | 3 | 4 |
| T5 | 2 | 6 | 1 | 1 和 3 |

其中：

- **T**：该进程第一次开始执行的时刻
- **τ**：该进程的执行持续时间

要求：

1. 给出各进程的执行顺序。
2. 计算进程的**平均周转时间**。
3. 计算进程的**平均等待时间**。

---

## 练习 3

某程序需要 **6 个虚拟页**，编号从 **0 到 5**。

在执行过程中，它按如下顺序访问页面：

```
0 1 2 3 4 5 0 5 4 3 2 1 0 1 2 3 4 0
```

如果该程序在内存中被分配了 **3 个空闲页框**，请指出其**缺页序列**。

已知页面替换算法采用**时钟算法（Clock / Second Chance，第二次机会算法）**。

---

## 练习 4

考虑一个字符文件，其中文本由**大写字母、小写字母和空格**组成。

编写一个 Bash 脚本，输出一条消息，用来判断该文本是否是一个 **“grobalogramme”**。

这里所谓的 **grobalogramme**，是指：

- 文本中的每个单词都以**同一个大写字母开头和结尾**
- 并且在这两个首尾大写字母之间，**不能再出现其他大写字母**

例如，下列文本就是一个 **grobalogramme**：

```
BoB EmpruntE TemporairemenT A RogeR SeS SoulierS
```

---

## 练习 5

1. 下列命令的作用是什么？

```bash
sed -e '4,6s/\\([^a-z][^a-z]*\\)/to\\1to/' fichier
```

1. 给出两种方法，将权限从：

```
rwxr-x--x
```

改为：

```
rwx--x---
```

1. 如何创建一个**符号链接（symbolic link）**？

---

## 练习 6

编写一个名为 **sauvegarde** 的 Bash 脚本，实现以下功能：

- 将一组文件移动：做法是先把它们复制到一个名为 **sauve** 的备份目录中
- 并且给这些备份文件加上**当前日期**
- 位于 **sauve** 目录中、**大小为 0** 且已存在**超过 7 天**的文件，应被删除

---

## 说明

这是根据原法语试卷内容整理的中文翻译版本，尽量保持原题意不变。个别术语（如 *grobalogramme*）保留原文写法，并在中文中作了说明。

# 1

# 2

1. 看当前时刻有哪些进程已经到达
2. 选**最高优先级**队列
3. 同队列按 RR 取队首
4. 跑一个时间片（最多 3），或者提前结束
5. 这个进程在本次执行时访问题目给的那些页，按 LRU 算缺页
6. 每满 2 个时间片，更新优先级
7. 继续

周转 = 完成 - 进入

等待 = 周转 - 运行

# 3 Clock

1. 页面被访问时：把该页的 `R=1`
2. 缺页要换页时，从指针当前位置开始检查
3. 若某页 `R=0`，直接淘汰它
4. 若某页 `R=1`，不给它淘汰，先把它改成 `0`，指针继续转
5. 找到第一个 `R=0`` 的页，把它换出

检查是在 **页框** 中轮转检查

存储页 [ , , ]

R   [ , , ]

指针 ↑

别忘记一开始什么都没有进去也算是缺页，并且 指针 指向的是下一个位置，例如像插入的是第一个、那么指针指向的是第二个

假如页框中有的话，那么只需要给该页 R 标为 1，不需要移动指针

[真题](真题%203470f8d8d1fd80e69bf6e1cff96cf8a1.md)
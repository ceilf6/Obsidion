# 智能体循环与沙箱执行分离

脑手分离不是为了架构上的“好看”，而是解决 Agent 系统在真实生产环境里会遇到的一系列问题。

- **弹性扩缩**：Agent Loop 和 Sandbox 可以独立扩缩容。高并发场景下 Sandbox 池可快速扩展、闲时回收，做到**极低成本**运行 Agent；而 Agent Loop 保持轻量，支持分布式**千万级**扩容；
- **多产品复用**：同一套 Agent Loop 引擎可以对接不同形态的 Sandbox（本地 CLI、远端 MicroVM、浏览器沙箱），从而同时支撑本地 **Desk 桌面 / Web 浏览器 / Server 后台任务等**业务场景。不同产品只需要接入适合自身场景的 Sandbox，就可以复用 CatX 的核心 Agent 能力。
- **极速响应**：在传统架构中，会话启动往往要等待执行环境加载完成，用户第一次进入时可能会有明显等待。CatX 将 Agent Loop 的会话状态和 Sandbox 的执行环境解耦后，Agent Loop 的启动不再依赖沙箱资源加载，首次会话加载时间从原来的 **15** 秒左右降低到最低约 **2** 秒，也就是基本只剩下模型首 token 的耗时。任务结束后，Sandbox 可以被销毁并释放资源，而 Agent Loop 的会话状态仍然可以保留。这样既提升了启动速度，也提升了资源利用效率。
- **安全隔离**：用户代码、终端命令、文件操作都发生在 Sandbox 中。即使代码运行异常，或者工具执行过程中出现问题，也不会影响 Agent Loop 本身的稳定性，更不会逃逸到宿主环境。对 Agent 系统来说，这相当于给所有高风险执行动作加了一层安全边界。

ReAct 风格的 Agent Loop。简单来说，Agent 会不断经历这样的过程：理解当前上下文，思考下一步该做什么，调用工具，等待工具返回结果，然后基于新的结果继续思考下一步。从逻辑上看，这像是一个持续运行的 `while loop`。但在 CatX 的实际架构里，这个 loop 并不是固定运行在某一台机器、某一个进程里，而是会在分布式系统中的不同节点之间流转。

底层其实是一个由事件驱动、可以在不同节点间恢复和继续执行的分布式 Agent Loop

在 CatX 中，不管是用户请求，还是工具结果上报，每一次抵达新服务节点的通常只有最新的一条 message。CatX Core 会根据 `sessionId` 找回这个会话之前的历史数据，把历史消息恢复出来，再把新的 message 追加进去，然后发起下一次模型请求。

- **session 事件驱动**：session 的 loop 整体由 user_message、tool_use、tool_result 这几类事件驱动，这些事件以及过程派生的状态事件、summary 事件、thinking 事件等也会入库入流以供 sdk 层消费；
- **session 会话数据**：session 的会话数据通常由 system/user/assistant/tool 等各类 messages 构成，每一次请求大模型都会将 session 内所有有效的 messages 取出，经过上下文模板拼装后再进行请求。如果触发 summary，则用于 summary 的历史 messages 会置为归档状态，并追加一条有效的 summary message。
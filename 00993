# 01. 入门文档

### 一、新建页面

### 1.打开DUO平台

平台地址: [https://duo.example.com/portal/page/list/duo-test-test-test](https://duo.example.com/portal/page/list/duo-test-test-test)

### 2.创建页面

操作路径：首页tab->选择业务线->新增页面
- 如果你没有权限，可以点击页面右上角“添加权限”进行申请。
- 后端appKey：可以咨询后端，用来做服务端登记。
- 研发组：下拉选择 Fedo 的研发组。
- mrn路径格式：rn_{biz}_{name}&{component}，例如： "rn_meishi_food-order-submit&Submit"
- h5路径：页面唯一标识，例如填写 "food-order-submit"，最终生成的 h5 地址为：[https://awp.meituan.com/dfe/duo-page/**food-order-submit**/web/index.html](https://awp.meituan.com/dfe/duo-page/**food-order-submit**/web/index.html)
- 仓库：是用于存储当前页面代码的git仓库的文件路径，填写之后不允许修改
- <u>仓库要求，需要添加以下成员权限</u>
1. - hfestash/hfe_stash
2. - FEDO小助手/it_fedo
3. - MCD/git_cd
- 文件路径：git仓库的文件路径,填写之后不允许修改
提交会自动创建一个 hfe_stash 发起的 commit，用于初始化仓库。合并 PR 即完成创建。

### 二、页面配置

创建完页面后，就可以进行页面配置了。

### 1.静态配置

```
- preview/update/submit 三个接口的前缀
- 页面跳链参数
页面入参配置仅用于校验并上报 Raptor 指标。
- 需要哪些系统环境参数
不需要的信息可以不勾选，以提升秒开
```

### 2.配置数据源

选择页面数据源，配置数据入参。
可选的数据源在“数据”tab下可以看到。需要注意，这个数据源需要先创建后端返回的数据，页面才可以配置使用。

### 3.配置视图和逻辑

```
- 可以从视图物料列表中拖拽视图物料到视图树中
- 可以从逻辑物料列表中拖拽逻辑物料到逻辑列表中。
```

### 三、mock 能力

**1）管理Mock 数据**
<u>a.数据->选择业务线->新增</u>
<u>b.数据->选择业务线->点击某个数据源->点击「管理Mock数据」</u>
在这里可以新增、编辑 mock 数据。
**2）编辑 mock 数据**
需要编写 preview 出参、update 出参、submit 出参，要求是 <u>**json**</u> 格式，编辑完成后「提交」。
**3) 使用 mock 数据**

### 四、调试能力

**1、本地环境准备**
- 克隆下载 duo-page 工程
- 本地启动项目

```bash
git clone ssh://git@git.example.com/dfe/duo-page.git
cd duo-page/page-latest
yarn dev

// 如果仅调试 mrn 页面，执行
yarn dev:mrn
// 如果仅调试 web 页面，执行
yarn dev:web
```

```bash
# 设置源
npm set registry <http://r.npm.example.com/>
yarn config set registry <http://r.npm.example.com/>
```

**2、启动在线调试**[DevTools](https://dev.example.com/code/repo-detail/dfe/duo-page/file/list)
此时本地启动的 duo-page 服务与配置平台链接。
打开调试按钮
会出现以下的界面
**3、页面预览**
可以使用手机扫二维码预览 mrn 页面，可以使用浏览器预览 h5 页面。

### 五、前端发布

参考[https://km.example.com/collabpage/2160077719](https://km.example.com/collabpage/2160077719)

### 六、页面测试

参考 [https://km.example.com/collabpage/1787610532](https://km.example.com/collabpage/1787610532)
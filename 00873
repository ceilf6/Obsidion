# 浏览器决定携带cookie过程

[chromium源码分析](%E6%B5%8F%E8%A7%88%E5%99%A8%E5%86%B3%E5%AE%9A%E6%90%BA%E5%B8%A6cookie%E8%BF%87%E7%A8%8B/chromium%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%203170f8d8d1fd80178319ce2d35aeff44.md)

https://github.com/ceilf6/Lab/commit/943abcd1735e16850054276d2f1ead0509cf19d5

- 首先，如果是跨站请求时，判断 fetch 是否开启了 **credential: include**
    
    [**默认情况下，ajax 的跨域请求并不会附带 cookie**，这样一来，某些需要权限的操作就无法进行](../%E5%90%8C%E6%BA%90%E7%AD%96%E7%95%A5%202f60f8d8d1fd805aa2add4064d274431.md) 
    

那么才会接着看哪些符合要求，如果一个cookie **同时** 通过以下条件筛选，则这个cookie会被附带到请求中

1. **HttpOnly** 过滤掉 JS 的操作
2. **Secure** 属性如果开启的话那么该 cookie 会从 HTTP 中筛出
3. 比对 **max-age** 看cookie是否过期
4. cookie中的域和这次请求的**domain域**是匹配的
    - 比如cookie中的域是`wangjinghong.com`，则可以匹配的请求域是`wangjinghong.com`、`www.wangjinghong.com`、`blogs.wangjinghong.com`等等（前面随意加子域）
    - 比如cookie中的域是`www.wangjinghong.com`，则只能匹配`www.wangjinghong.com`这样的请求域
    - cookie是不在乎端口的，只要域匹配即可
5. cookie中的path和这次请求的**path路径**是匹配的
    
    cookie 记录的是基路径
    
    - 比如cookie中的path是`/news`，则可以匹配的请求路径可以是`/news`、`/news/detail`、`/news/a/b/c`等等，但不能匹配`/blogs`
    - 如果cookie的path是`/`，可以想象，能够匹配所有的路径
6. **samesite** 在跨站请求**（目标页 和 发起页的URL(而不是domain)）**时，根据 Strict / Lax / None 决定是否携带
    
    [像发起 fetch 时的 credential: include 是前端想要带上 cookie ，这正是 CSRF 想要的，但是 cookie 被服务器响应时设置了 samesite 从而防止被携带](%E8%AE%BE%E7%BD%AE%20cookie%203170f8d8d1fd8003b7d7f98ea9b68e1e.md) 
    

如果一个cookie满足了上述的所有条件，则浏览器会把它自动加入到这次请求中，放入到请求头中 `键=值; 键=值; 键=值; ...`

![image.png](%E6%B5%8F%E8%A7%88%E5%99%A8%E5%86%B3%E5%AE%9A%E6%90%BA%E5%B8%A6cookie%E8%BF%87%E7%A8%8B/image.png)
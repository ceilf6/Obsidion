# openclaw

像其他Agent都在专注于如何通过 MCP, skill 等提高模型能力

openclaw 专注于将模型做成一个 **产品** ：统一网关、多渠道接入、会话与路由、设备节点、权限与沙箱

其 agentloop循环 中自动进行 上下文组装、推理、工具执行、流式输出、持久化，而且按 session 串行化

```tsx
龙虾能火，不只是因为它打通了多平台通信，
而是因为它把 AI 从“会回答”推进到了“会操作”，
并且以低摩擦方式嵌入现有工作流，
提供了更强的权限、工具、记忆、协作和执行闭环。
真正稀缺的不是模型，而是最后一公里的操作系统能力。

再进一步压缩成一句更适合聊天里的表达：

龙虾火，不是因为它更像 ChatGPT，
而是因为它更像一个能接系统、拿权限、调工具、跨平台干活的“AI 操作层”。
```

在能力层面，其他很多Agent 通过 MCP 协议限定了操作范围、提高安全性，并且也只能通过MCP，因为很多Agent是云端托管的，其运行环境是和本地机器分离的，而像不是云端托管的、大模型的参数很小思考决策能力有限

openclaw 直接通过 Node.js 调用 fs文件系统API，openclaw 不考虑远端托管，如果硬要将 fs相关安全策略 封装一个 MCP 反而导致增加了会议层受攻击的可能、受攻击面积增大

![image.png](工程师沉淀/AI/3%20AI%20应用开发层/openclaw/image.png)

通过 role 进行身份识别

user 使用者

assistant 大模型

toolResult 大模型进行Function Calling的结果

[统一的通信网关](统一的通信网关%203250f8d8d1fd804ba6f2da481fae4a86.md)

[实践](实践%2032b0f8d8d1fd80ec8c6dcccab82341f2.md)

[记忆体系](记忆体系%203360f8d8d1fd8059b10af65c4c85087e.md)
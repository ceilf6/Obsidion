# SSR原理分析与环境搭建

### CSR客户端渲染

原理：

![画板](../../assets/img/1735521481453-d4c7a550-62f0-4a84-9d75-b7fc2ab54f11.jpeg)

问题：

- 不利于**SEO**搜索引擎优化
- 浏览器需要等待响应后再渲染数据

### SSR服务端渲染

原理：

![画板](../../assets/img/1735521764762-b0e7a1df-45de-414c-b949-6f284c1dcd02.jpeg)

### 服务端处理

将React组件树渲染为一个HTML字符串

- **renderToString**

React18 之后换成返回管道化的Node.js流，这是因为前者需要等所有内容生成完毕之后一次性返回，会拖缓显示，于是后面都用了

- **renderToPipeableStream**

### 客户端处理

对服务端渲染的HTML结构，进行交互处理。

- hydrateRoot

将服务器已经生成好的DOM接管成**React管理的Fiber树**，并将事件系统、状态更新等全部对齐

> 原则：为了保证服务端和客户端能够完美的进行配合，需要React组件在服务端和客户端保持一致，即同构。
> 

### 环境搭建

采用create-vite脚手架，具备编译React的环境和SSR辅助方法，默认create-vite搭建的react环境是CSR模式。

```bash
npm create vite@latest
```
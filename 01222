# ka-mrn 知识库方案总结

### 背景与目标

本文档总结 ka-mrn 项目 AI 辅助开发知识库的完整设计方案，涵盖目录结构、文档规范、代码关联、索引策略、评测体系等核心内容，供后续维护和扩展参考。
知识库的核心目标是：让 AI（Cursor / OpenViking 等工具）在回答业务问题时，能精准检索到对应的流程文档，并直接定位到源代码，减少 AI 幻觉，提升开发效率。

### 知识库流转链路

### 一、仓库结构

知识库与代码仓库（比如本次的ka-mrn）**分开存放，**知识库作为一个单独的仓库，每个业务子包一个目录，互不干扰，相当于一个层级分类

```markdown
knowledge-base/                    ← 知识库根目录（独立仓库）
├── README.md                      ← 全局规范说明
├── conventions/                   ← 团队级统一规范（跨所有子包）
├── ka-mrn/                        ← ka-mrn 仓库知识库（按业务仓库分层）
│   └── ka-mop-order-detail/       ← 订单详情页（镜像 monorepo 包名）
│       ├── behaviors/             ← 业务流程（用户行为视角）
│       │   ├── README.md          ← 分类清单索引（Level 2）
│       │   ├── payment-flow.md
│       │   ├── cancel-order-flow.md
│       │   ├── refund-flow.md
│       │   └── ...（共 8 个流程文档）
│       ├── interfaces/            ← 接口/类型定义
│       ├── context/               ← 子包概览
│       └── raw/                   ← 源文件索引（不参与检索）
├── ka-mop/                        ← 后续 ka-mop 仓库知识库（待扩展）
├── evals/                         ← 知识库质量评测
│   ├── cases/                     ← 评测用例
│   ├── results/                   ← 评测结果
│   ├── run_eval.py                ← RAGAS 评测主脚本
│   └── friday_llm.py              ← Friday LLM 适配层
└── decisions/                     ← 跨包技术决策记录，记录为什么要这么设计（ADR），防止历史失忆，时间长了为什么要这样设计
    ├── adr-001-layered-index.md  // 为什么要进行分层索引结构
    └── adr-002-deviation-driven-update.md // 为什么用偏差驱动，而不是定时全量同步
```

### 二、代码关联方案

`source` 字段统一使用**仓库内相对路径**，精确到行号，不包含任何绝对路径或环境相关前缀，在开发时，因为我们打开的比如ka-mrn,根据source可以快速找到

```yaml
source:
  - ka-mop-order-detail/src/pages/utils/pay.ts#L6-L60
  - ka-mop-order-detail/src/pages/components/BottomArea/index.tsx#L95-L102
```

### 三、文档规范（Frontmatter）

每份语料文件包含以下标准字段，此处参考了[腾讯某文章](https://mp.weixin.qq.com/s/Xy8NwrHZRWv301eTZz4Dpw)

```yaml
---
type: process          # 知识类型：process / model / decision / guideline / pitfall
maturity: stable       # 成熟度：stable / has-todo / draft
source:
  - ka-mop-order-detail/src/pages/components/BottomArea/index.tsx#L56-L161
last-reviewed: 2026-05-06
confidence: reviewed   # inferred / reviewed / prd-verified
see-also:              # 跨包关联文档（可选）
  - ka-mop-home/behaviors/reorder-flow.md
---
```

**字段说明：**
- **type**：知识类型，5 种枚举：process（业务流程）/ model（数据模型）/ decision（技术决策）/ guideline（规范约束）/ pitfall（踩坑记录）
- **maturity**：成熟度标记，✅ stable（已验证）/ 🔶 has-todo（有待确认项）/ 🚧 draft（草稿）
- **source**：仓库内相对路径，精确到行号，格式为 `path/to/file.tsx#L起始行-结束行`
- **confidence**：置信度，三档：inferred（AI 推断）/ reviewed（人工核查）/ prd-verified（对照规范文档验证）
- **see-also**：跨包同名流程或强依赖关系的引用，避免重复维护

### 四、三级渐进式索引

通过分层组合以及文件命名提高索引效率，采用三级渐进式索引结构：
- **Level 1（全景目录）**：知识库根目录 `README.md`，列出所有子包和目录
- **Level 2（分类清单）**：各子包的 `behaviors/README.md`，每条知识一行摘要 + 类型 + 成熟度
- **Level 3（完整条目）**：各具体流程文档，包含完整的流程描述、异常分支、接口引用
以 `ka-mop-order-detail/behaviors/README.md` 为例，按业务域分组，每条一行：

| 文件 | 摘要 | 类型 | 成熟度 |
| --- | --- | --- | --- |
| [payment-flow.md](http://payment-flow.md/) | 去支付按钮防抖；MRN 原生端跳转；微信小程序 H5 端区分美团/点评 appid | process | ✅ 稳定 |
| [cancel-order-flow.md](http://cancel-order-flow.md/) | 待付款→关闭订单 vs 待接单→取消订单；错误码 10121 引导退款 | process | ✅ 稳定 |
| [refund-flow.md](http://refund-flow.md/) | 退款入口四条件；六步退款流程；极速退款差异（待确认）ni lai | process | 🔶 有 TODO |

### 五、错误纠正

主要两方面，一方面在于对话过程中人给出的反馈，比如不对，另一方面通过[RAGAS](https://docs.ragas.org.cn/en/stable/concepts/test_data_generation/rag/)评测，人工纠正后的内容会写入`evals/deviations/` 记录偏差事件，将偏差转化为新的评测用例加入 `cases.json`，第二部分是通过RAGAS根据现有case评测现有仓库的准确性完全性等

### 六、RAG 平台选型

公司内部已经部署了服务端，前期本地需要安装 open Viking，然后在本地cursor等通过mcp接入

```jsx
{
  "mcpServers": {
    "openviking": {
      "command": "node",
      "args": ["/Users/watery/workspace/catpaw-desk/.cursor/openviking-mcp.js"],
      "env": {}
    }
  }
}
```

```
- 支持代码仓库索引，与知识库的相对路径方案天然契合
- 美团内部已有共享服务（10.101.209.97:8080），无需自己部署，数据不出内网
```

接入路径：申请内部共享服务 API Key → 配置 `~/.openviking/ovcli.conf` → `ov add-resource` 上传知识库 → 在 Cursor 中配置 MCP 连接（`~/.cursor/mcp.json`）。
embeding

### 七、TODO

```
- git webhook 自动同步：push 到 knowledge-base 仓库时自动触发 ov add-resource 更新 OpenViking 索引
- 补充 ka-mop-home、ka-mop-order-submit 等其他子包知识文档
- refund-flow.md 中 3 处 TODO 待确认（极速退款差异、部分退款品牌范围、refundRejected 恢复机制）
- 建立偏差事件记录的标准化流程
```
# 删除第三行

“输出除第 3 行的所有内容”就是：

- 第 3 行不要
- 其他行全部输出

原文件是：

```
1  java
2  apple
3  python
4  test
5  c++
6  c#
7  now
8  coder
```

去掉第 3 行后，应该输出：

```
java
apple
test
c++
c#
now
coder
```

---

### **逐个解析选项**

### **A.**

### **sed '3d' file**

这个是 **对的**。

含义：

- 3 表示第 3 行
- d 表示删除

所以这条命令就是：

> 把第 3 行删掉，再输出剩下所有行
> 

---

### **B.**

### **awk 'NR=3' file**

这个是 **错的**。

原因：

- NR 表示当前行号
- 这里写的是 NR=3，这是“赋值”，不是判断

如果想判断第 3 行，通常应写成：

```
awk 'NR==3'
```

但这也只是处理第 3 行，不是“输出除第 3 行外的所有内容”。

如果想用 awk 正确写，应该类似：

```
awk 'NR!=3' file
```

意思是：

> 行号不等于 3 的都输出
> 

---

### **C.**

### **sed -n '3!p' file**

这个其实 **也是对的**。

含义拆开看：

- n：默认不输出
- 3!p：不是第 3 行就打印

所以效果正是：

> 只打印不是第 3 行的内容
> 

---

### **D.**

### **head -n 2 file; tail -n +4 file**

这个 **也是对的**。

含义：

- head -n 2 file：取前 2 行
- tail -n +4 file：从第 4 行开始输出到最后

两部分拼起来，正好跳过第 3 行。
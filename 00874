# 矩阵秩计算

这两个秩最常用的算法，就是 **高斯消元**。

你可以直接记成：

- `r(A)`：把矩阵 `A` 化成阶梯形，数 **非零行** 的个数
- `r(A|b)`：把增广矩阵 `(A|b)` 化成阶梯形，数 **非零行** 的个数

也可以等价地说：数 **主元个数**。

**怎么做**

设线性方程组是

[

Ax=b

]

先写出

[

A

\quad\text{和}\quad

(A|b)

]

然后分别做初等行变换：

- 交换两行
- 某行乘非零常数
- 某行加上另一行的倍数

把它们化成行阶梯形。

**例子 1：无解**

[

\begin{cases}

x+y=1\

2x+2y=3

\end{cases}

]

系数矩阵

[

A=

\begin{pmatrix}

1&1\

2&2

\end{pmatrix}

]

增广矩阵

[

(A|b)=

\left(

\begin{array}{cc|c}

1&1&1\

2&2&3

\end{array}

\right)

]

消元：

[

R_2\leftarrow R_2-2R_1

]

得到

[

\left(

\begin{array}{cc|c}

1&1&1\

0&0&1

\end{array}

\right)

]

看秩：

- 对 `A` 来说，化简后是

[

\begin{pmatrix}

1&1\

0&0

\end{pmatrix}

]

所以 `r(A)=1`

- 对 `(A|b)` 来说，化简后有两行非零，所以 `r(A|b)=2`

因此

[

r(A)\ne r(A|b)

]

所以 **无解**。

这里最后一行其实表示

[

0x+0y=1

]

显然矛盾。

**例子 2：无穷多解**

[

\begin{cases}

x+y=1\

2x+2y=2

\end{cases}

]

增广矩阵

[

\left(

\begin{array}{cc|c}

1&1&1\

2&2&2

\end{array}

\right)

]

消元：

[

R_2\leftarrow R_2-2R_1

]

得到

[

\left(

\begin{array}{cc|c}

1&1&1\

0&0&0

\end{array}

\right)

]

于是

- `r(A)=1`
- `r(A|b)=1`

而未知数个数 `n=2`，所以

[

r(A)=r(A|b)<2

]

因此 **无穷多解**。

**考试里怎么快速看**

你可以按这个顺序判断：

1. 先对增广矩阵 `(A|b)` 做高斯消元
2. 看左边系数部分有几个主元，这就是 `r(A)`
3. 看整个增广矩阵有几个主元，这就是 `r(A|b)`
4. 再套结论：

[

r(A)\ne r(A|b)\Rightarrow 无解

]

[

r(A)=r(A|b)=n\Rightarrow 唯一解

]

[

r(A)=r(A|b)<n\Rightarrow 无穷多解

]

你也可以记一个特别醒目的信号：

- 如果出现 `0 0 \cdots 0 | c`，且 `c\ne 0`，那就是 **无解**
- 如果出现 `0 0 \cdots 0 | 0`，说明有一行冗余，要再看主元数是否小于未知数个数

如果你愿意，我可以下一条给你做一个 **3元一次方程组** 的完整消元例子，把 `r(A)` 和 `r(A|b)` 一步一步算出来。